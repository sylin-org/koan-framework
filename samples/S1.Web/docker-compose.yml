services:
  s1:
    container_name: Koan-s1
    build:
      context: ../..
      dockerfile: samples/S1.Web/Dockerfile
    ports:
      - "${HOST_PORT:-5044}:5044"
    volumes:
      # Persist JSON data in sample-local .Koan folder
      - ./.Koan/Data:/app/data
    restart: unless-stopped
