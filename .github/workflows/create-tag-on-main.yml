name: disabled-create-tag-on-main

# Superseded by .github/workflows/release-on-main.yml (ARCH-0082).
# Previously: NBGV-based tag creator on push to main. Removed because the new
# two-tier versioning system computes versions from build/versions.props rather
# than git history, and creates release/v<kernel> tags directly from the
# release-on-main workflow.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See release-on-main.yml + ARCH-0082."
