# ARCH-0103: AODB adapter conformance — the family-base realization contracts

**Status**: Accepted (Enterprise Architect, 2026-06-25). The fleet mandate is canon; the contracts below are ratified in shape; per-family/per-adapter implementation is phased in §7.
**Date**: 2026-06-25
**Deciders**: Enterprise Architect
**Scope**: Define the adapter-side **realization contracts** for the Access Overlay Definition Block (ARCH-0102), and mandate that **every Koan-shipped data adapter implements all three AODB modes** (Shared, Container, Database). ARCH-0102 establishes *what the framework composes and pushes down*; this decision establishes *how an adapter realizes it*, and converges the multi-generational per-adapter divergence onto **storage-model family bases** so each mode is implemented once per family, not bespoke-or-skipped per adapter.
**Related**: **ARCH-0102** (the AODB — this is its adapter-realization companion) · **ARCH-0094** (Adapter Forge / the Conformance Gate — the capability-token-co-defined-with-its-check model used here) · **ARCH-0084** (the capability model — `IDescribesCapabilities`, `DataCaps`) · **ARCH-0079** (integration tests as canon — every adapter ships a real-boot conformance spec) · **DATA-0105/0106** (managed-field stamp / read-filter contributor — the Shared-mode sources) · **[koan-design-principles]** (conformity-by-design; fail-closed; fewer-but-more-meaningful-parts) · **[contributor-pipelines-never-bespoke]**.

---

## 1. The mandate (canon)

**All Koan-shipped data adapters MUST implement all three AODB modes.** The realization bar is **native-or-emulated, no exceptions**: an adapter realizes each mode by its backend's native mechanism where one exists, and by framework-uniform emulation where it does not. There is no honest-fail-closed exemption — "the adapter doesn't do this mode" is not a shippable state. The 15 shipped data adapters in scope:

- **Relational**: Sqlite, Postgres, SqlServer
- **Document/KV**: Mongo, Couchbase, Redis, Json, InMemory
- **Vector**: Weaviate, Qdrant, Milvus, SqliteVec, InMemoryVector, ElasticSearch, OpenSearch

This supersedes the capability-gated "no capability lies" reading (which permitted an adapter to decline a mode and fail closed). Capability tokens remain — but now as a *conformance ledger* (every adapter declares all three, and the conformance kit proves each), not as an opt-out.

**No mode deferral (binding).** "This backend can't really do mode X, so defer it" is rejected for every adapter — the realization is *native-or-emulated*, and the three modes the fleet audit flagged as awkward are all feasibly realizable (and were re-confirmed in the 2026-06-25 feasibility evaluation): **InMemory Shared mode** via an object-sidecar (`(entity, managedValues)`) read by the in-memory filter evaluator; **Couchbase Database mode** via a per-source cluster provider; **vector Container mode** via class/collection-per-particle name-mangling as the universal floor, with native multi-tenancy (Weaviate tenants / Qdrant collections / Milvus partitions) as a per-adapter enrichment behind the same contract. A feasibility evaluation that recommends deferring a mode is a planning aid, not a license — the mandate holds.

**No technical debt carried.** The rebuild harvests what is reusable and rebuilds the rest; it does not preserve cruft. The audit named the debt to discard (raw-POCO serialization paths that skip managed fields; un-wired capability claims; the Mongo legacy-config migration shims; per-adapter copy-paste of health/telemetry/registration) — none of it survives into the family bases.

## 2. The debt this clears (the 2026-06-25 fleet audit)

The modes are realized **three different ways or not at all**, the classic multi-generational accretion:

1. **Two divergent factory contracts** — `IDataAdapterFactory.Create(sp, source)` vs `IVectorAdapterFactory.Create(sp)` (no `source`). The vector family **structurally cannot route**, and `VectorService` never consults `EntityContext.Source`/`DatabaseRouteRegistry` — so a Database-mode tenant axis routes the row write to the tenant DB while the embedding silently lands in the shared store (**split-brain isolation**).
2. **Three write-stamp mechanisms** — the read-side fold is generic in `RepositoryFacade`, but the Shared-mode *write*-stamp diverges: relational via a `System.Text.Json`/Newtonsoft contract resolver, Mongo via explicit BSON injection, and **Json/Couchbase/Redis/InMemory inject nothing** (they serialize the raw POCO; the managed field is lost). Those four also don't declare `RowScoped`, so today they *fail closed* — safe, but non-isolating, which the mandate forbids.
3. **Container mode declared-but-never-wired across all 7 vector adapters** (native multi-tenancy unused).
4. **Overlay naming applied only to Weaviate** — Qdrant/Milvus/ElasticSearch/OpenSearch may silently drop `__`-prefixed metadata fields (the un-propagated Phase-0 bug; a live silent-isolation-breach risk).
5. **No `ContainerScoped`/`DatabaseScoped` capability tokens and no conformance kit** — "compliant" is unmeasurable.

## 3. The framing — three layers, three contracts

The AODB already names one element kind per layer of the data path. Each becomes one realization contract with a clean *framework-computes / adapter-realizes* seam:

| AODB element | Mode | Layer | Framework provides | Adapter realizes |
|---|---|---|---|---|
| **Moniker** | Database | **connection** (factory) | the routed `source` key | a repo bound to that source's physical store |
| **Particle** | Container | **container** (table/collection/index) | the ambient name particles | a distinct physical container per particle-set |
| **FieldFilter** | Shared | **record** (per-row) | managed-field *values* + the read *filter* | persist the values as first-class **filterable** fields |

## 4. The contracts

### 4.1 Moniker → source-routed creation (one factory contract)

A **marker base** unifies the two factory interfaces at the discovery/naming/routing surface; the `Create` signatures align to `(sp, source)`. The marker lives in the foundation assembly (`Koan.Data.Abstractions`, no downstream deps); the two `Create` surfaces stay specialized because their return types (`IDataRepository` vs `IVectorSearchRepository`) live in different assemblies — a single generic interface returning both would cycle. The two `Create` methods differ *only* by their inherent return type; the contract (discovery, naming, source-routing) is one.

```csharp
// Koan.Data.Abstractions — the foundation marker (no cycle: nothing downstream is referenced).
public interface IAdapterFactory : INamingProvider
{
    bool CanHandle(string provider);
}

// Koan.Data.Abstractions
public interface IDataAdapterFactory : IAdapterFactory
{
    IDataRepository<TEntity, TKey> Create<TEntity, TKey>(IServiceProvider sp, string source = "Default")
        where TEntity : class, IEntity<TKey> where TKey : notnull;
}

// Koan.Data.Vector.Abstractions — gains the source parameter (the asymmetry fix).
public interface IVectorAdapterFactory : IAdapterFactory
{
    IVectorSearchRepository<TEntity, TKey> Create<TEntity, TKey>(IServiceProvider sp, string source = "Default")
        where TEntity : class, IEntity<TKey> where TKey : notnull;
}
```

**The routing decision is one primitive**, `RoutedSource.Resolve<TEntity>()` (`Koan.Data.Core.Routing`): an explicit `EntityContext.Source` wins, else a Database-mode `[DataAxis]` route (`DatabaseRouteRegistry`), else null. **Both** `AdapterResolver` (record) and `VectorService` (vector) resolve through it, so a Database-mode axis routes both planes to the same source — the split-brain is closed by construction. Per-source physical resolution stays uniform (`AdapterConnectionResolver.ResolveConnectionString`), reused by vector. **`ResolveFactory<TFactory>`** (a shared `[ProviderPriority]` + `CanHandle` ranking helper) replaces the duplicated ranking in `AdapterResolver`/`VectorService`. One intended behavior consequence: the **vector** factory pick is now `[ProviderPriority]`-ranked in the no-match / blank-desired fallback (it was DI-registration order). The record plane already ranked by priority, so this is the vector plane *converging onto the shared rule*, not a regression — the only observable difference is in a deliberate multi-vector-adapter app whose desired provider matches no vector factory, where the highest-priority (not the first-registered) adapter is now chosen; a regression spec pins it.

### 4.2 Particle → container resolution

One contract — *given the ambient particles, return the physical container* — with two sanctioned realizations the family base selects:

- **Name-mangling** (the existing `StorageNameGenerator` → `INamingProvider.ResolveStorage`): the particle becomes a name segment. Works for every backend; the universal floor for Container mode.
- **Native container** (Mongo collection · Couchbase scope · Weaviate/Qdrant/Milvus native multi-tenancy / collection): the family base, when the backend declares it, routes to the native container instead of name-mangling.

The overlay-naming rule (`OverlayNamingRule`/`IOverlayNamingAware`, ARCH-0102 Phase 0) rides this contract: a backend that reserves identifier characters declares its rename once, applied at write/read/index from the one path. Container compliance for an adapter = *its containers are physically distinct per particle-set, and `__`-names survive its naming (or are renamed injectively)*.

### 4.3 FieldFilter → managed-record persistence

The read half is already uniform (filter translation). The write half is the contract: **managed fields are persisted as top-level, filterable fields, and a cross-scope write is guarded.** This converges to **one mechanism per serialization family**, not per adapter:

- **JSON-text family** (relational JSON column · Redis value · Json file): the **shared managed-field JSON injector** (lifted from relational's `ManagedFieldContractResolver`, which is already serializer-level and storage-agnostic) reads `ManagedFieldWriteScope` and injects the ambient managed values into the serialized JSON. Redis/Json route their serialization through it and gain the write-stamp; their existing in-memory read-filter then matches it (the read was always applied — it had nothing to match).
- **BSON family** (Mongo): a BSON serialization hook (Mongo already injects explicitly; formalize it as the family's injector).
- **Object-graph family** (InMemory): a sidecar — the store holds `(entity, managedValues)` so the in-memory filter evaluator reads managed values without mutating the POCO.

The **cross-scope write guard** (no tenant-A takeover of a tenant-B row) is the family base's: relational does it via the `ManagedUpsert` conflict `WHERE`; the KV/document bases do it via read-existing-managed-then-conditional-write (or a native CAS).

## 5. The convergence — storage-model family bases

The three contracts are implemented **once per family**; concrete adapters supply only backend primitives (the relational pattern, already proven):

| Family base | Adapters | Shared logic (the base) | Backend primitives (the seam) |
|---|---|---|---|
| **`RelationalStore`** (exists: `Koan.Data.Relational`) | Sqlite · Postgres · SqlServer | schema orchestrator · filter→SQL translator · managed-field JSON injector · naming · managed-upsert skeleton | `ILinqSqlDialect` · `IRelationalDdlExecutor` · `IRelationalStoreFeatures` |
| **`DocumentStore`** (new) | Mongo · Couchbase | managed-field injector (BSON/JSON) · filter→native-query translator · naming · container resolution (native collection/scope) · write guard | a `IDocumentDialect` (doc-path expr, array ops) · a doc DDL/collection executor · connect-per-source |
| **`KeyValueStore`** (new) | Redis · Json · InMemory | managed-field injector (shared JSON / object sidecar) · in-memory filter application · naming (keyspace/file/store per particle) · write guard | get/set/scan primitives · connect-per-source (or in-proc emulation) |
| **`ScopedVectorRepository`** (exists, extend) | the 7 vector adapters | already centralizes Shared (metadata-filter + overlay rename); **extend to drive Container (native MT / name) + Moniker (per-source)** | the per-adapter vector repo (write-point, search, filter-translate) |

**Net:** 15 adapters re-deriving (or skipping) 3 modes → **4 family realizations** of 3 contracts + thin backend primitives. This is the "fewer but more meaningful parts" payoff: the modes have **one** home per family, not N divergent ones.

### 5.1 The helper-module layer (above the family bases)

The family bases themselves stay thin by composing a layer of **storage-agnostic helper modules** — the cross-cutting primitives every base reuses, so a mechanism is written once for the whole fleet, not once per family:

- **`ManagedFieldJsonInjector`** — the Shared-mode write-stamp for every JSON-serializing store. Lifted from relational's `ManagedFieldContractResolver` (already serializer-level); shared by `RelationalStore` (JSON column) and `KeyValueStore` (Redis/Json values). Routing Redis/Json serialization through it persists the managed field, and their existing in-memory read-filter then matches it — the four "no write-stamp" adapters are fixed by *reuse*, not new bespoke code.
- **`FilterAstWalker`** — the shared `Filter`-AST traversal (`AllOf`/`AnyOf`/`Not`/`FieldFilter` dispatch + `FieldPathResolver` + `FilterValueConverter`); each backend dialect renders only the leaves. This is why filter sharing is *partial but real*: the walk is shared, the render (SQL / MQL / N1QL / GraphQL / REST-JSON / native-string) is per-dialect (~70 reusable LOC/adapter, honestly not the whole translator).
- **`HttpVectorStore`** — the HTTP+JSON vector lifecycle (ensure-collection / upsert / KNN-search / export / delete), generalized from the proven `SearchEngineVectorRepository` (ES + OpenSearch already share it). Absorbs the Weaviate/Qdrant/Milvus boilerplate behind an `IVectorDialect` seam.
- **Already shared, reused as-is** — `StorageNameGenerator` (Container naming), `AdapterConnectionResolver` (per-source physical resolution), `InMemoryFilterEvaluator`/`DictionaryFilterEvaluator` (the convergence oracle), `ScopedVectorRepository` (the vector Shared-mode decorator), `RoutedSource` (the one routing decision).

### 5.2 Feasibility evaluation (2026-06-25) — the validated payoff

A quantified fan-out evaluation across the four families confirmed the direction is both desirable and feasible; the current adapters carry **40–90% removable weight**:

| Family | Current ~LOC | Achievable | Per-adapter floor | Lever |
|---|---|---|---|---|
| Relational (3) | ~6,940 | base grows + ~300–700/adapter | ~250–350 (dialect+DDL+features) | the base under-absorbs today — harvest ~600 LOC of copy-paste |
| Document (2) | ~6,260 | ~1,645 (−74%) | ~320 / ~400 | new `DocumentStore` + `IDocumentDialect` |
| Key-value (3) | ~2,435 | ~1,050 (−45%) | **20–70 each** | new `KeyValueStore`; near-total collapse |
| Vector (7) | ~8,000 | ~5,500 (−31%) | HTTP trio −57…73% | `HttpVectorStore` (extend SearchEngine) |

Fleet-wide ≈ **−45% adapter code**, the debt cleared, compliance reduced to a green conformance-kit cell. **Proven pattern**: `Koan.Data.Relational` and `SearchEngineVectorRepository` already are family bases with dialect seams. **Real risks kept** (not waved away): Couchbase's 3-level `bucket.scope.collection` vs Mongo's 2-level (the container-resolution seam returns an opaque handle to absorb it); vector native-MT API divergence (name-mangling is the floor); InMemory must stay the byte-faithful convergence oracle; the new bases must match the relational base's allocation discipline (gated by byte-identity off-proofs).

## 6. Conformance — the executable definition of "compliant"

- **Capability tokens** (co-defined with their check, ARCH-0094 — a token cannot exist without its conformance module, so over-claim fails green): `DataCaps.Isolation.RowScoped` (exists) + new **`ContainerScoped`** + **`DatabaseScoped`**. Every adapter declares all three; the conformance kit proves each.
- **`AodbConformanceSpecsBase<TFactory>`** — one reusable real-`AddKoan()` spec base (the `ManagedFieldNoLeak` / `AdapterSurfaceSpecsBase<TFactory>` + `IAdapterTestFactory` pattern), run by **every** adapter surface, proving: Shared isolation (write-stamp lands + read-filter isolates + cross-scope write rejected) · Container isolation (distinct physical container per particle + `__`-name survival/rename) · Database isolation (per-source physical routing + fail-closed on unconfigured). The vector overlay-naming silent-drop is a RED cell here until fixed.

## 7. Phased implementation (TDD, break-and-rebuild authorized)

Each phase: real-`AddKoan()` conformance specs first (RED), then the rebuild, then green + a byte-identity off-proof + impl-diff review.

- **P1 — the Moniker contract.** `IAdapterFactory` marker + `IVectorAdapterFactory.Create(sp, source)` + `RoutedSource` + `ResolveFactory<TFactory>`; `AdapterResolver` and `VectorService` converge onto both. Gate: Docker-free per-source proof on InMemoryVector + SqliteVec; data-core 273 off-proof byte-identical.
- **P2 — `KeyValueStore` base.** Rebuild Redis/Json/InMemory onto it; Shared write-stamp via the shared JSON injector / sidecar + write guard; Container via keyspace/file/store-per-particle; Database via per-source connection/file/store. Gate: conformance kit green on all three (InMemory + Json Docker-free; Redis via Testcontainers).
- **P3 — `DocumentStore` base.** Rebuild Mongo + Couchbase onto it (Mongo largely conforms — formalize; Couchbase gains write-stamp + per-source). Gate: conformance kit green (Testcontainers). **Mongo is the golden reference, rebuilt from scratch against a documented ideal-state catalogue — harvesting the dense-correct encoders intact, leaving the structural debt behind. The catalogue (responsibilities × mechanism × must-preserve invariant × Koan canon × base/dialect × Couchbase-delta, the 18-invariant harvest register, and the settled base-home/seam decisions) is [ARCH-0103-document-store-catalogue.md](ARCH-0103-document-store-catalogue.md). It also pulls the §6 conformance tokens (`ContainerScoped`/`DatabaseScoped`) forward as Mongo's first green cell.**
- **P4 — vector.** Extend `ScopedVectorRepository` to drive Container (native MT) + Moniker (per-source); **verify+fix the overlay-naming silent-drop** on Qdrant/Milvus/ElasticSearch/OpenSearch. Gate: conformance kit green per adapter (Testcontainers; InMemoryVector/SqliteVec Docker-free).
  - **Pinned P4 follow-ons (from P1, surfaced by the P1 routing-core adversarial review).** P1 wired the Moniker contract through every vector factory but realized per-source physical placement only on InMemoryVector + SqliteVec. These land here, uniformly across all 7 vector adapters rather than as per-adapter spot-fixes:
    1. **Fail-closed on an unconfigured routed source, on BOTH planes.** P1's SqliteVec per-source resolution delegates to `AdapterConnectionResolver`, whose Default-fallback (Priority 4 `Koan:Data:SqliteVec:ConnectionString`, Priority 5 recurse-to-`Default`) *silently* reuses the Default store for a routed source that resolves no connection string. This is a **pre-existing property of the shared resolver** that the SQLite *data* adapter exhibits identically — so it is not a new vector/record split-brain. The honest nuance: a source entirely absent from the registry IS record-fail-closed (`AdapterResolver` Priority 1.5 → `ExternalOnly` throw) but vector-silent (the resolver's Priority 1 `GetSource`→null then falls through); a source present with an `Adapter` but no `ConnectionString` passes Priority 1.5 (which validates `Adapter` only) and BOTH planes then silently share. P4's conformance kit installs the "Database isolation: fail-closed on unconfigured" cell (§6) for both planes — validate the resolved connection string, not just the adapter.
    2. **The five deferring adapters (Weaviate/Qdrant/Milvus/ElasticSearch/OpenSearch) accept `source` and ignore it.** Reachable silent co-mingle: a Database-mode axis over a server-backed vector entity routes to a non-Default source the adapter does not yet isolate. P4 realizes per-source for them; the cleanest P1→P4 stopgap-IF-needed is a shared fail-closed guard (refuse a non-Default source rather than silently ignore) — one helper, not five copies.
    3. **SqliteVec is a family-base refactor candidate.** It re-rolls SQLite connection lifecycle (vs the data connector's `SqliteConnectionFactory`) and identifier sanitization (vs `StorageNameGenerator`/the announced `StorageNamingCapability`); folding it onto the shared durable-in-proc + naming machinery removes that duplication and is where the fail-closed posture above is cleanest to install.
    4. **`VectorService` caches per-source repos forever and is not `IDisposable`.** Distinct routed sources accumulate `ScopedVectorRepository`/inner-repo (and, for SqliteVec, a live `SqliteConnection`) entries with no eviction. ARCH-0102 §6 scopes connection-pool lifecycle / LRU across many tenant keyspaces OUT to a sibling plane; tracked here so P4/P6 closes it (make `VectorService` dispose `IDisposable` cached repos on teardown at minimum).
- **P5 — the conformance ledger. SHIPPED** (`dev`, commits `c7d060af`..`35e910b8`). `DataCaps.Isolation.ContainerScoped` + `DatabaseScoped` tokens declared at the family bases (`DocumentStore`/`KeyValueStore`) + the relational trio; the reusable `AodbConformanceSpecsBase<TFixture>` (4 co-defined cells — Declares / Shared / Container / Database) wired into all SEVEN conformant adapter surfaces (Mongo · InMemory/Json/Redis · Sqlite/Postgres/SqlServer = 28 conformance facts); each adapter's boot report notes its declared modes; the SURFACES ledger row added. Couchbase is excluded (not yet folded onto `DocumentStore` — declares no isolation token, fails closed). 5-lens adversarial review GO_WITH_FIXES, 0 must-fix (1 should-fix = a SURFACES honesty correction, applied). Detail + status in §9.7.

Relational (P0, already conformant) is the reference; its specs seed `AodbConformanceSpecsBase`.

## 8. Consequences

- **Positive**: one realization per mode per family; the routing split-brain closed by construction; the four non-isolating record adapters become isolating; the latent vector overlay leaks closed; "compliant" becomes a green CI cell, not a claim. Adding a new adapter = implement one family's backend primitives, inherit all three modes.
- **Negative / risk**: a real rebuild of five document/KV adapters + the vector decorator onto new bases (mitigated by the proven relational template + per-phase conformance gates + byte-identity off-proofs). The marker-base factory is a partial structural merge (two `Create` surfaces remain) — the assembly boundary makes a single generic interface a cycle; the contract is unified where it matters (discovery, naming, source-routing).
- **Rejected**: (a) capability-gated opt-out — the mandate forbids it; (b) a single generic `IAdapterFactory<TRepo>` returning both repo types — assembly cycle; (c) the "address+document projection / dumb-store" radical convergence — relational needs materialized columns for pushdown, so it cannot be a dumb store; the family-base level is the right altitude.

## 9. P2 `KeyValueStore` — the realization detail (the build map)

The P2 enforcement model, derived empirically from the read-path investigation (2026-06-25). **The framework already owns most of the read path generically** — the rebuild adds only the four adapter-side concerns the relational reference proves.

### 9.1 Framework-vs-adapter responsibility (what the rebuild does NOT have to re-implement)

| Concern | Owner | Mechanism (where) |
|---|---|---|
| AND the managed `__`-predicate into every read | **Framework** | `RepositoryFacade.Query/Count` fold `Filter.Eq("__scope",v)` into `query.Filter` before the adapter sees it |
| get-by-id IDOR | **Framework** | `RepositoryFacade.Get` *lowers* a scoped key-read to `Query(id==X AND __scope==v)` — the adapter only ever gets the predicate via `Query`; raw `Get(id)` is called **only when unscoped** |
| scoped `RemoveAll` | **Framework** | facade loads the scoped rows then deletes them by id (never a bulk unscoped truncate) |
| fail-closed when non-isolating | **Framework** | `RepositoryFacade.InspectScopeAdapter` (at construction): an entity in an active managed scope on an adapter that does not declare the field's `RequiredCapability` (`RowScoped`) — or that is not `IQueryRepository` — throws `"*does not announce*"` |
| **declare `RowScoped`** | **Adapter** | `IDescribesCapabilities.Describe` adds `DataCaps.Isolation.RowScoped` — the single switch that stops the fail-closed gate and lets the predicate flow |
| **write-stamp** the managed values | **Adapter/base** | read `ManagedFieldWriteScope.Effective`; InMemory → object **sidecar**; Json/Redis → the shared **`ManagedFieldJsonInjector`** lifted from relational's `ManagedFieldContractResolver` (injects `"__scope":v` into the serialized JSON) |
| **cross-scope write guard** | **Adapter/base** | on upsert, if an existing record's stamped managed values ≠ `ManagedFieldWriteScope.Current`, throw `InvalidOperationException("…cross-scope write…")` (relational does it via the `ON CONFLICT … WHERE json_extract(...) = @p`; KV does read-existing-then-compare) |
| **evaluate** the managed predicate | **base** | `InMemoryFilterEvaluator` **refuses** managed fields by design (`ResolvedField.GetValue` throws "cannot be evaluated in memory"), so the base needs a **hybrid evaluator** |

### 9.2 The `KeyValueStore<TEntity,TKey>` family base

One abstract base (`Koan.Data.Core` or a new `Koan.Data.KeyValue` assembly both KV connectors reference) implementing `IDataRepository`/`IQueryRepository`/`IDescribesCapabilities`/`IInstructionExecutor`, owning the three contracts; the backends supply only primitives. Internal envelope: `KvRecord<TEntity>(TEntity Entity, IReadOnlyDictionary<string,object?>? Managed)`.

- **Shared (FieldFilter → managed-record persistence).** *Write:* capture `ManagedFieldWriteScope.Effective`; guard cross-scope vs `…Current`; persist the envelope. *Read:* a **hybrid evaluator** walks the `Filter` AST and dispatches each leaf — a `FieldFilter` whose `FieldPathResolver.Resolve(...).IsManaged` is true is evaluated by **`DictionaryFilterEvaluator`** over the record's `Managed` dict; every other leaf (`FieldFilter`/`ClrFilter`) by **`InMemoryFilterEvaluator`** over the `Entity` (a single-leaf compile, so both convergence oracles are reused, none duplicated). Declares `RowScoped`.
- **Container (Particle → container).** Already realized via the ambient partition → distinct physical store (`AdapterNaming.GetOrCompute`/`GetOrCreateStore(partition)`/keyspace); P2 verifies + adds the conformance cell. Name-mangling is the universal floor; the `__`-name survives because the managed keys live in the value/sidecar, not the container identifier.
- **Database (Moniker → source-routed).** The per-source physical store the source ignores today: InMemory keys its store dict by `(source,type,partition)` (mirrors the P1 InMemoryVector fix); Json a per-source directory; Redis a per-source connection / DB-index — resolved via the shared `AdapterConnectionResolver` where a connection string applies.

### 9.3 Backend primitives (the thin per-adapter seam)

Each KV adapter supplies: connect/resolve-store-per-`(source,particle)`; `read-one(id)`, `scan-all`, `write(id, envelope)`, `remove(id)`, `clear`; and the envelope's serialize/deserialize (object identity for InMemory; `ManagedFieldJsonInjector`-configured Newtonsoft for Json/Redis). Everything else (the three contracts, capability declaration, batch, instructions) lives in the base.

### 9.4 Conformance kit (the P2 gate)

`AodbConformanceSpecsBase` proves all three modes through a real `AddKoan()` boot: **Shared** delegates to the existing `ManagedFieldNoLeak.AssertNoLeakAsync()` oracle (read isolation + get-by-id IDOR + cross-scope write-reject + scoped RemoveAll), **Container** asserts a distinct physical store per partition + `__`-name survival, **Database** asserts per-source physical routing (the `MultiDatabaseRoutingSpec` shape). Run per KV surface: InMemory + Json Docker-free, Redis via Testcontainers. (P5 generalizes the base across every adapter surface + adds the `ContainerScoped`/`DatabaseScoped` tokens + boot-report surfacing.)

### 9.5 Sequencing (each = RED conformance → rebuild → GREEN + byte-identity off-proof + impl-diff review + commit)

InMemory (sidecar; the Docker-free convergence oracle) → Json (JSON injector; Docker-free) → Redis (JSON injector + per-source connection; Testcontainers). The base lands with InMemory and is proven before Json/Redis fold on. Byte-identity off-proofs: data-core 274 + tenancy 104 stay green at every step (the `IsEmpty` short-circuits keep off-axis byte-identical).

### 9.6 Status

- **P2 COMPLETE — all three KV adapters folded onto the family base** (`dev`, unpushed). The base (`KeyValueStore<TEntity,TKey>` + `KvRecord` + the hybrid `KvFilterEvaluator`) lives in `Koan.Data.Core/KeyValue/`; the shared write-stamp `ManagedFieldJsonInjector` (lifted from relational's `ManagedFieldContractResolver`) lives in `Koan.Data.Core/Managed/` and is used by BOTH the relational trio AND the KV JSON-text family. The bulk write/remove fast path rides two virtual seams — `WriteManyAsync`/`RemoveManyAsync` (guard+stamp per row, then ONE physical write; Json persists its file once, Redis MSETs / one DEL).
  - **InMemory** (`262b222c` + `861c821f`): a thin subclass over `InMemoryDataStore` (`KvRecord` envelopes keyed by `(source, type, partition)`). Shared = `InMemoryManagedFieldNoLeakSpec`; Container = `InMemoryPartitionSpec`; Database = `InMemoryDatabaseRoutingSpec` (2-source host, the data-plane twin of the P1 InMemoryVector proof). InMemory suite **36**.
  - **Json** (`f2da7059`): a thin subclass; per-record envelope serialized through `ManagedFieldJsonInjector` (entity + `__`-keys), per-source directory (Database), per-partition file (Container), a per-file `SemaphoreSlim` serializing the write-through snapshots. Shared = `JsonManagedFieldNoLeakSpec`; Container = `JsonPartitionSpec`; Database = `JsonDatabaseRoutingSpec`. Json connector **10** + Web Json **49** (Docker-free).
  - **Redis** (`72bc562a`): a thin subclass; the `__`-keys ride the stored JSON value via the same injector; Container = the partition-suffixed keyspace; Database = a per-source Redis logical database index (`GetDatabase(index)` on the shared connection, resolved by the factory via `AdapterConnectionResolver.GetSourceSetting(..., "Database")`); native TTL + UNLINK preserved. Shared = `RedisManagedFieldNoLeakSpec`; Container = Web AdapterSurface; Database = `RedisDatabaseRoutingSpec`. Redis connector **6** + Web Redis **49** (Testcontainers).
- **The non-isolating fixture (necessary side-effect):** with every real KV adapter now isolating (JSON included), the fail-closed safety-net tests can no longer use a real adapter as the "non-isolating" example. A deliberately non-conformant `NonIsolatingFakeAdapter` (declares `Filter.Full` but NOT `RowScoped`; `[ProviderPriority(int.MinValue)]` so it is never the default election; registered explicitly by the runtime fixtures) is the shared linked counter-example; the tenancy + data-core fail-closed specs boot `adapter:"fake-noniso"`.
- **Holistic adversarial review** (3 lenses — isolation soundness, off-path byte-identity, concurrency/native-correctness, `accef29a`): 0 CRITICAL. The one fix taken = the bulk seams above. Adjudicated-and-dismissed (re-derived against relational + git): batch-delete IDOR + direct `RemoveAll` are pre-existing facade-is-the-gate (FC-2); the `Capabilities` `??=` race is benign (deterministic `Describe`); **off-scope re-write erasing the managed sidecar MATCHES relational** (its contract resolver drops the key off-scope too — fixing only KV would break cross-adapter convergence). Documented follow-on: the Redis Database-mode footgun (two sources both defaulting to logical db 0 silently share — same shape as a missing connection string; a boot-time cross-source collision check is the durable fix).
- Gates GREEN at every fold: data-core **274** + tenancy **104** byte-identical (the `IsEmpty` short-circuits keep off-axis identical), full-solution build clean.

### 9.7 Status — P3/P4/P5 (the document family + the conformance ledger)

- **P3 COMPLETE — golden Mongo `DocumentStore`** (`dev`). The document family base (`Koan.Data.Core/Document/`) + the from-scratch golden `MongoDocumentStore` rebuilt against [the catalogue](ARCH-0103-document-store-catalogue.md); all 18 harvest invariants preserved; all three AODB modes proven.
- **P4 COMPLETE — golden-Mongo cleanup** (`dev`, `b878851a` + `d0ea898e`): the `KoanModule` registrar merge (`MongoModule` + one once-guarded `MongoDriverConfiguration`) and the connection-string consolidation (one `MongoConnectionString` helper + the non-Default `"auto"` quirk fix). Fleet residual recorded: the sibling relational/SqliteVec factories share the `"auto"` quirk → hoist the normalization into the shared `AdapterConnectionResolver`.
- **P5 COMPLETE — the conformance ledger** (`dev`, `c7d060af`..`35e910b8`).
  - **Tokens**: `DataCaps.Isolation.ContainerScoped` + `DatabaseScoped` declared at `DocumentStore.Describe` + `KeyValueStore.Describe` + the relational trio's `Describe`. Purely additive — no behavioral path reads the two new tokens (the routing-time fail-closed gate still keys only on `RowScoped`); the conformance kit's `Declares` cell is their consumer (the accepted ARCH-0094 enforcement boundary).
  - **The kit**: `AodbConformanceSpecsBase<TFixture>` (`Koan.Data.AdapterSurface.TestKit`) — four co-defined cells (`Declares_all_three` / `Shared` via the `ManagedFieldNoLeak` oracle / `Container` per-partition isolation + concurrent no-leak / `Database` per-source routing + fail-closed). The shared discoverable `ConformanceShardAxis` + `ConformanceShardedDoc`/`ConformancePartitionDoc` live in the TestKit, which is referenced ONLY by the eight adapter connector test projects — so the discovered Database axis never reaches data-core/tenancy and their off-proofs stay byte-identical; the axis is inert without an ambient shard.
  - **The seven green cells**: Mongo (document); InMemory/Json/Redis (KV); Sqlite/Postgres/SqlServer (relational) — 28 conformance facts. The Postgres/SqlServer Database cells provision a fresh physical database per routed source (`CREATE DATABASE`, unique per-run names; not dropped — reclaimed by the container, harmless in a bring-your-own-server lane).
  - **Boot surfacing + ledger**: each of the seven adapters notes its declared modes in its boot report; the SURFACES row records the surface + guard.
  - **Gates**: 28 conformance facts green; all eight adapter suites green with the shared axis (incl. Couchbase 9/9, correctly excluded — declares no isolation token); data-core **274** + tenancy **104** byte-identical; bootstrap boot-report **38/38**; full-solution build clean. 5-lens adversarial review (Workflow): GO_WITH_FIXES, 0 must-fix / 1 should-fix (the SURFACES Couchbase-capability honesty correction, applied).
  - **Documented follow-ons**: (a) ✅ **DONE — Couchbase folded onto `DocumentStore` (2026-06-26, P4)**: see §9.8; (b) the vector fleet's Container/Database realization + the overlay-naming silent-drop fix (the ADR's separate vector phase); (c) optionally derive the boot note from the declared caps rather than a static string (drift-proofing — a `note`).

### 9.8 Status — P4 Couchbase fold (the document family complete)

- **COMPLETE (`dev`, 2026-06-26).** `CouchbaseRepository` (internal sealed, ~1000 LOC, on no family base, **declared ZERO isolation tokens** so it failed closed on managed-scoped routing) deleted → **`CouchbaseDocumentStore<TEntity,TKey> : DocumentStore<TEntity,TKey>`**, the last document-family adapter to fold (Mongo is the golden reference). Method = catalogue-first then TDD against the existing oracle, harvesting the dense-correct pieces INTACT and leaving the structural debt behind (mirrors the golden-Mongo P3 method). See [the document-store catalogue §8 P4](ARCH-0103-document-store-catalogue.md).
- **Harvested intact**: the `CouchbaseN1qlFilterTranslator` (enum-as-int / GUID-N / null semantics — FilterConvergence-guarded), the GUID-N key + storage encoding, the CAS conditional-replace, the transaction batch, the raw-N1QL escape hatch (`IRawQueryRepository` → `Query.String`), the primary-index online-wait, RequestPlus read consistency.
- **The three AODB modes on Couchbase's 3-level keyspace**: **Shared** = the framework-managed discriminator injected into the document JSON (the shared `ManagedFieldJsonInjector`) + a CAS conflict guard; **Container** = a distinct native **scope** per ambient partition (`CouchbaseClusterProvider`, unchanged); **Database** = a distinct native **bucket** per routed source (a per-source `CouchbaseClusterProvider` pooled by connection|bucket in `CouchbaseAdapterFactory`, `IAsyncDisposable`/`IDisposable`). The base declares `RowScoped`+`ContainerScoped`+`DatabaseScoped`; the dialect adds `Query.String`/`BulkUpsert`/`BulkDelete`/`AtomicBatch`/`ConditionalReplace` (no `FastRemove`/`TtlIndex` — Couchbase has neither).
- **Fold-surfaced Couchbase-local fixes**: a `FormatCollectionName` charset sanitizer (the framework name resolver replaces only `'.'`, leaving a nested-type `'+'` the collection manager rejects — e.g. `ManagedFieldNoLeak+ScopedDoc`) mirroring `FormatScope`; the managed write-stamp + guard normalize each value through the SAME `CouchbaseN1qlFilterTranslator.NormalizeValue` the read filter uses (so Guid/enum/DateTime managed fields round-trip; strings unchanged); the CAS loop trips a clear framework contention error on exhaustion (never an unconditional fallback — a doc can transition scope, so the write stays guard-checked, fail-closed).
- **Gates GREEN**: Couchbase connector 13/13 (9 prior + 4 new AODB cells) + Web AdapterSurface Couchbase 49/49 + data-core 274 + tenancy 104 byte-identical + full-solution build clean.
- **4-lens adversarial review (Workflow)**: 6 confirmed — **HIGH** (CAS loop spuriously fails sustained same-scope concurrent writes) + **MEDIUM** (Guid/enum/DateTime managed-field write/read encoding mismatch) both **fixed pre-commit**; 2 LOW (per-source provider lifetime + duplicate-for-Default) = Mongo parity, deferred to the planned shared `AdapterConnectionResolver`/`StaticOptionsMonitor` hoist (§L); the rest dismissed on re-derivation (the "unconditional final write" fix the reviewer floated is itself unsafe — documented).
- **Residual / next**: the shared `AdapterConnectionResolver` hoist (the `"auto"` quirk + the per-source-pool lifetime + duplicate-for-Default, fleet-wide for Mongo/Couchbase/relational together); the vector fleet's Container/Database + overlay-naming fix.

### 9.9 Status — the vector fleet (Container declared-already / Database source-fold)

- **A 7-adapter fleet map revised the audit.** **Container** is ALREADY realized fleet-wide: every vector adapter (Weaviate · Qdrant · Milvus · SqliteVec · InMemoryVector · ElasticSearch · OpenSearch) folds the ambient partition into the collection/index name via `StorageNameGenerator` (`EncodePartitionInName` defaults true) — partition-fold is Docker-proven on Qdrant (`QdrantPartitionSpecs`). The **overlay-naming "live breach"** the audit flagged is **code-safe** on Qdrant/Milvus/ES/OpenSearch: they store `__koan_tenant` as a NESTED JSON key (`metadata.__koan_tenant`) their stores accept faithfully; only Weaviate's GraphQL reserves a leading `__`, and it already declares `IOverlayNamingAware("koan_")`. (Empirical Docker round-trip is the remaining confirmation.)
- **Database was the genuine gap** — `VectorService` routes the source (P1) but the 5 HTTP adapters accepted-and-ignored it (only InMemoryVector + SqliteVec realized it). **CLOSED (`e196c97c`)** with the SAME name-mangling floor as Container: fold the routed source into the collection name as a **leading particle** (`source{sep}base#partition`), one shared change in `StorageNameGenerator` (source-aware `Resolve` + `Compose` particle + Source cache-key dimension; record plane unaffected — it routes a source to a distinct physical store via the factory, not the name) + `VectorAdapterNaming` (threads the same `RoutedSource`). Off ⇒ byte-identical. **Adversarial review caught + fixed a CRITICAL**: the source particle skipped the injectivity guard the partition/container-axis particles get (a case-folding adapter would collapse `"Sales"`/`"sales"` to one collection) — now fail-closed, co-defined by `VectorSourceFoldInjectivitySpec`. Gates: Axes.Integration 18/18, Tenancy 104/104, data-core 274/274 byte-identical, full build clean.
- **Qdrant production isolation spec — DONE (`8c0f0ad4`); the overlay security flag is now TEST-VERIFIED.** `QdrantVectorIsolationSpec` boots a real `AddKoan()` host (tenancy + a Database-mode `[QdrantSharded]` axis) against a Testcontainers Qdrant, green 3/3: **Shared** — `__koan_tenant` round-trips faithfully through the Qdrant payload (`metadata.__koan_tenant`) and isolates a kNN even when the other tenant's point is nearer; **Container** — distinct partition → distinct collection; **Database** — source-fold → distinct collection per shard. The audit's flagged overlay "live isolation-breach risk" is **empirically closed** (was code-derived-safe). Full Qdrant project 38/40 (0 failed); full build clean.
- **The production spec surfaced + fixed a real pre-existing bug** (what production-adapter specs are for): a full `AddKoan()` boot's `QdrantOptionsConfigurator` binds an absent `CollectionName` key to `""` (not null), and the repo's `_options.CollectionName ?? GetOrCompute` treated `""` as a pinned empty name → `/collections/` → HTTP 404, which would also collapse every entity/partition/source onto one empty-named collection. Fix: resolve via `string.IsNullOrWhiteSpace` (null AND `""`). The surface harness never hit it (bypasses the configurator). **Milvus carried the identical bug** (review-found, HIGH) — fixed the same way (Docker-verify pending). Weaviate + ES/OpenSearch checked, clean.
- **Remaining (Docker-heavy, explicit follow-on)**: the HTTP-fleet production isolation specs for Weaviate/ES/OpenSearch/Milvus (mirror the Qdrant spec) — **DONE, see §9.10**; declaring the `DataCaps.Isolation.*` tokens on the vector repos (needs a vector conformance kit + inner-vs-decorator token placement). The `CollectionName`/`IndexName` option footgun (pinning a real name bypasses partition+source folding) is documented; **warn-on-pin DONE — see §9.14**.

### 9.10 Status — the HTTP-fleet production isolation specs (Weaviate · ES · OpenSearch · Milvus)

- **DONE — all four mirror `QdrantVectorIsolationSpec`** (a real `AddKoan()` host + tenancy + a local Database-mode `[…Sharded]` axis against the adapter's Testcontainers backend; endpoint pinned via config `DisableAutoDetection` + `PostConfigure`). Each green 3/3: **Shared** (`__koan_tenant` overlay round-trips faithfully + isolates a kNN even when the other tenant's point is nearer), **Container** (distinct partition → distinct physical collection/index/class), **Database** (source-fold → distinct physical collection/index/class per shard). The audit's overlay "live isolation-breach risk" is now empirically closed on the WHOLE HTTP fleet (Qdrant §9.9 + these four). Gates: OpenSearch full project 28 passed/4 expected-skip/0 fail; **Weaviate full project 33/33** (matrix + the Phase-0 overlay spec byte-identical + isolation 3/3); ElasticSearch isolation 3/3; Milvus isolation 3/3. Milvus's run also **Docker-confirms** last session's empty-`CollectionName` fix (`8c0f0ad4`, previously Docker-unverified).
- **The Weaviate production spec surfaced + fixed TWO real latent Weaviate naming bugs** (what production specs are for; both adapter-local in `WeaviateVectorRepository.SanitizeClassName`, byte-identical for already-legal top-level names — oracle-anchored by the 33/33 Weaviate run):
  1. **Nested-type `+`** — `.NET` surfaces a `+` in `Type.FullName` for a nested entity type; the framework name resolver folds only `.`, so a nested entity reached Weaviate with a `+` in its class name → 422 *'not a valid class name'*. Fold any non-`[_0-9A-Za-z]` char to `_` (the same gap Couchbase fixed adapter-locally; a bare char fold, not Couchbase's lossy-collision hash — out of scope for the canonical entity set).
  2. **Source-fold case vs Weaviate's first-letter auto-capitalization (the serious one)** — Weaviate auto-capitalizes the first letter of a class name on creation and GraphQL is case-sensitive. The Database-mode source fold prepends the routed source as a **lowercase** leading particle (`alpha_…`), so the class was stored as `Alpha_…` but a read computed `alpha_…` → queried a non-existent class → **silently returned empty** (a cross-source isolation read would falsely look empty). Upper-case the first char to match what Weaviate stores. **CAVEAT** (documented in-code): because Weaviate first-char-folds, two Database-mode sources differing only in first-char case collapse to one class — the source-fold injectivity guard can't see this fold (the Weaviate naming policy is case-preserving), so Weaviate Database-mode sources must be first-char-canonical (conventionally lowercase).
- **Test-side notes**: Weaviate builds its HNSW index asynchronously with no synchronous-refresh knob (unlike ES/OS `RefreshMode="true"` or Milvus `ConsistencyLevel="Strong"`), so its spec polls the entity's own-scope search until indexed (`WaitIndexed`) BEFORE asserting isolation — both vectors are confirmed indexed first, which closes the masking window (a real leak still fails the exact-set `Equal`). Referencing `Koan.Tenancy` from the Weaviate test project made `TenancyRuntime` (an `IStorageGuard`) discoverable by the Phase-0 overlay fixture's hand-rolled SP; it now registers a Development `IHostEnvironment` (dev-open posture, inert with no ambient tenant) so the synthetic `__disc` overlay test is unaffected.
- **4-lens adversarial review (Workflow, ultracode): GO / 0 must-fix** (3 LOW = comment-honesty on `SanitizeClassName`, applied; the masking-window and exact-set-leak concerns were re-derived as genuinely closed).

### 9.11 Status — vector `DataCaps.Isolation.*` token declarations + the vector conformance kit

- **DONE.** The vector plane now DECLARES its AODB isolation tokens, co-defined with their proofs (ARCH-0094). **Placement = the decorator** — `ScopedVectorRepository.Describe`, the ONE chokepoint wrapping every vector adapter, not the inner: `DataCaps.Isolation.ContainerScoped` + `DatabaseScoped` unconditionally (the shared name-mangling floor realizes both for EVERY vector adapter), and `RowScoped` gated on the inner's `VectorCaps.Filters` (Shared needs metadata filtering; the Shared read-path already fail-closes on its absence). Purely additive — only the conformance kit + boot reports read it; vectors never flow through the record-plane `RepositoryFacade.InspectScopeAdapter`, so routing/fail-closed behavior is unchanged (byte-identical: Axes.Integration 18/18, VectorTenantIsolationSpec 5/5, InMemory full project 33/33).
- **The kit** = **`VectorAodbConformanceSpecsBase`** (`Koan.Data.VectorAdapterSurface.TestKit`), the vector twin of the record-plane `AodbConformanceSpecsBase`: a reusable base (boots a real `AddKoan()` host via an abstract `BootHostAsync`) + a shared discoverable `VectorConformanceShardAxis`/entities. Four co-defined cells: **Declares** (single-adapter-convention assert + Container/Database always declared) · **Shared** (RowScoped co-defined BEHAVIORALLY — declared ⇒ the `__koan_tenant` overlay isolates a kNN; under-claimed ⇒ a scoped read fails closed, the honest path for a pure-KNN store) · **Container** (partition → distinct collection) · **Database** (source-fold → distinct collection). Wired Docker-free on **InMemoryVector** (4/4) as the canonical co-definition; the HTTP fleet's live-Docker behavioral proof stays in each adapter's `*VectorIsolationSpec` (§9.9/9.10).
- **Honesty carve-out (review-surfaced)**: `DataCaps.Isolation.DatabaseScoped`'s "fail-closed on an unconfigured source" clause is the RECORD plane's external-only posture; the vector name-fold floor is LAZY (any ambient source → a distinct collection name, so there is nothing to fail closed on). The token doc + the decorator now state this so token and behavior agree; the vector Database cell proves distinct-source isolation, not the record plane's throw.
- **4-lens adversarial review (Workflow, ultracode): GO / 0 must-fix** — 2 MEDIUM fixed pre-commit (the kit's unconditional RowScoped assert would have spuriously red-failed an honest non-filtering adapter → made capability-aware; the DatabaseScoped doc over-claim → carved out); LOWs (single-adapter assert, comment honesty) applied.
- **Follow-on**: ~~a SqliteVec conformance cell (the natural Docker-free non-filtering twin, exercising the Shared under-claim/fail-close branch)~~ **DONE — see §9.13**; the HTTP adapters adopting the kit (consolidating the Task-1 production specs onto it).

### 9.12 Status — Mongo + Couchbase folded onto the shared `AdapterConnectionResolver.ResolveRoutedConnection`

- **DONE.** The (b) `"auto"`-quirk hoist (`28e18479`) shared the resolver across the relational + SqliteVec factories; this folds the two **document** factories onto it too. `MongoAdapterFactory` + `CouchbaseAdapterFactory` non-Default branches now call the single `AdapterConnectionResolver.ResolveRoutedConnection(config, registry, provider, source, baseOptions.ConnectionString)`. Couchbase's private local helper is **DELETED** (no spec pinned it); Mongo's `MongoConnectionString.ResolveRoutedConnection` (the pure 2-arg helper) is **KEPT byte-identical** because `MongoConnectionStringSpec` pins it (constraint: do not churn that spec) — it is now a test-pinned pure helper, while the rest of `MongoConnectionString` (Build / MergeOverrides / ApplyParameters / ExtractHost) stays in production use.
- **Byte-identical on the routed path.** Couchbase's old local logic IS the shared logic (the exact De Morgan dual) for ALL inputs. Mongo's old helper diverged in exactly one corner — the source resolves to the `"auto"`/blank sentinel AND `baseOptions.ConnectionString` is itself blank (old returned the blank default; shared returns the sentinel) — which is **structurally unreachable**: `baseOptions.ConnectionString` is the configurator-normalized Default, which `MongoOptionsConfigurator` always resolves to a concrete `mongodb://…` (every discovery failure/disabled path falls back to a built localhost connection), never blank/`"auto"` post-boot. Both factories also early-return for the Default source before the call, so the shared method's Default branch is never reached by them. (The shared form is arguably the more correct of the two old helpers — it returns the unresolved sentinel rather than substituting a blank.)
- **Oracle (green):** `MongoConnectionStringSpec` 34/34 (helper + spec untouched, byte-identical); `MongoDatabaseRoutingSpec` 2/2 (live Mongo per-source routing); `CouchbaseAodbConformanceSpec` 4/4 (live Couchbase per-source bucket routing); both connectors build clean. **2-lens adversarial review (Workflow, ultracode): GO / 0 must-fix** — LOWs (the unreachable corner-case divergence; the test-pinned Mongo helper now production-orphaned) are all no-change.
- **Residual (unchanged):** the per-source-pool lifetime + duplicate-provider-for-Default (the Mongo/Couchbase review LOWs) remain a separate follow-on.

### 9.13 Status — the SqliteVec vector conformance cell (the Docker-free non-filtering twin)

- **DONE.** `SqliteVecVectorAodbConformanceSpec : VectorAodbConformanceSpecsBase` (`Koan.Data.VectorAdapterSurface.SqliteVec.Tests`) wires the kit (§9.11) onto sqlite-vec, the in-process durable vector floor — Docker-free, 4/4 green: **Declares** (single-adapter convention + Container/Database tokens) · **Shared** · **Container** · **Database**. It closes the §9.11 follow-on.
- **It is the FIRST cell to drive the Shared cell's under-claim / fail-close branch** (the reason it was the natural next twin). sqlite-vec is a pure-KNN store — `SqliteVecVectorRepository.Describe` announces `Knn`/`BulkUpsert`/`BulkDelete`/`AtomicBatch`/`ScoreNormalization`/`DynamicCollections` but **no `VectorCaps.Filters`** — so `ScopedVectorRepository` honestly does NOT declare `RowScoped`, and a scoped read FAILS CLOSED (`NotSupportedException`) rather than leak a neighbouring tenant's vector. Every other vector adapter with an isolation spec (InMemoryVector, Weaviate, Qdrant, Milvus, ES, OpenSearch) declares `Filters` and takes the *isolate* branch — so SqliteVec and InMemoryVector now **co-define `RowScoped` from both sides** (declared ⇒ isolates / under-claimed ⇒ fails closed). The non-vacuity of all four cells was traced to RED counterfactuals in the adversarial review.
- **Container / Database realized by mechanism, not name-fold.** Container = a distinct `vec0` virtual table per ambient partition inside one SQLite database (the partition folds into the table name via the production `INamingProvider.ResolveStorage` path). Database = a distinct SQLite database per routed source, resolved at the factory through the shared `AdapterConnectionResolver.ResolveRoutedConnection` (the same call path the §9.12 fold uses). The fixture uses `Data Source=:memory:` per source — unshared per connection, held one-per-repo for its lifetime — so isolation is genuine with no files/cleanup, and the routing-collapse counterfactual fails RED. (The isolating mechanism here is connection-identity per routed source, not connection-string distinctness; the resolver's string-distinctness / `"auto"` collapse is proved separately by §9.12.)
- **3-lens adversarial review (Workflow, ultracode): GO_WITH_FIXES, 0 code defects** — both must-fixes mechanical: added `AssemblyAttributes.cs` (`[assembly: CollectionBehavior(DisableTestParallelization = true)]` — the cell was the only VectorAdapterSurface project missing the fleet-idiom guard against an `AppHost.Current` / `DatabaseRouteRegistry` race once a second spec class lands) + this durable record. Bundled a NOTE fix: a stale `<see cref>` in `VectorAodbConformanceSpecsBase` (`Declares_all_three_isolation_modes` → `Declares_realized_isolation_modes`, the post-co-definition method name). Gates: SqliteVec 4/4, InMemoryVector 33/33 (shared kit base unchanged behaviorally).
- **Follow-on (unchanged):** the HTTP adapters adopting the kit (consolidating the Task-1 production `*VectorIsolationSpec`s onto `VectorAodbConformanceSpecsBase`).

### 9.14 Status — the CollectionName/IndexName pin footgun (warn-on-pin)

- **DONE.** A vector adapter option that pins a STATIC collection/index name — `CollectionName` (Qdrant/Milvus) or `IndexName` (ElasticSearch/OpenSearch) — is used verbatim, **bypassing the name-fold** (`VectorAdapterNaming.GetOrCompute` → `StorageNameGenerator.Resolve`). So every isolation discriminator that the name composes is DEFEATED for that entity — they all collapse onto the one pinned name. `StorageNameGenerator` folds **three**: (1) the ambient **partition** particle (Container), (2) the Database-mode routed **source** particle (Database — explicit `EntityContext.Source` or an axis-derived route), and (3) **container-name particles** from a Container-mode `[DataAxis]` (`StorageNameParticleRegistry` — the "a separate container IS the isolation" realization). Tenancy (`RowScoped`) is unaffected — it isolates by a metadata read-filter, not the name. (Weaviate has no static-name option; InMemoryVector/SqliteVec have none — so the three adapters above are the full surface.)
- **Decision = WARN, not fail-close** (the §9.9 follow-on resolved). The pin is a deliberate, documented override; failing closed on it would break legitimate single-tenant uses that pin a name and happen to partition for unrelated reasons. Instead the framework surfaces the footgun: a **one-per-entity-type `ConfigWarning`** ("`vector.name.pinned` / `isolation-defeated`") naming the entity, the option, the pinned value, and which discriminator is active — emitted **only when the pin actually bites** (one of the three name-fold discriminators above is in scope for that op), so single-tenant, non-partitioned apps see nothing. (Adversarial review caught a **HIGH false-negative** here pre-commit: the predicate originally checked only partition + routed source, missing the Container-`[DataAxis]` particle — a real silent defeat with no warning. Now all three are checked, mirroring `StorageNameGenerator.Resolve` exactly.)
- **One shared warner, three thin call sites (conformity-by-design).** The decision lives in **`VectorAdapterNaming`** (the naming authority): a pure public predicate `PinnedNameDefeatsActiveIsolation<TEntity>` + the warn-once `WarnIfPinnedNameDefeatsIsolation<TEntity>`. Each adapter's name resolver calls it from its pinned branch (`QdrantVectorRepository.CollectionName`, `MilvusVectorRepository.CollectionName`, `SearchEngineVectorRepository.IndexName`) — the blank-check + computed path are untouched, so **off ⇒ byte-identical** (only a log is added on the pinned-and-isolated path); the already-warned `ContainsKey` fast path keeps the hot name-resolution path cheap. `IndexPrefix` is NOT a footgun (it decorates the *computed* name, preserving the fold) — only the full `IndexName` pin bypasses it.
- **Lazy (op-time), not boot-time** — the warning fires at the first isolated op that resolves a pinned name, which is exactly when (and before) the pin could leak; a proactive boot-time sweep would need per-entity option + axis enumeration and is out of scope for this hardening.
- **Tests-first (Docker-free):** `VectorNamePinWarningSpec` (`Koan.Tests.Data.Core`) — the predicate (blank / no-discriminator → false; partition / explicit routed source / **Container-axis particle** → true) + the emission via the internal `KoanLog.TestSink` seam (one warning under a partition, dedups on the second call; silent with no discriminator or a blank pin). Plus `VectorNamePinWiringSpec` — the **end-to-end wiring proof**: a real `QdrantVectorRepository` built by its public factory with a stub `IHttpClientFactory` (no Docker) emits the warning from its actual pinned `CollectionName` getter (Milvus's getter is the byte-identical block; SearchEngine calls the same warner — both inspection-verified against this exemplar). 9/9. Gates: InMemoryVector 33/33, SqliteVec 4/4, Axes.Integration 18/18, data-core 283/283 (274 off-proof byte-identical + the 9 new), the three adapter connectors build clean.
- **Known edge (LOW, recorded for honesty):** `SearchEngineVectorRepository.IndexName` short-circuits on `IsNullOrEmpty` while the predicate's blank-check is `IsNullOrWhiteSpace`, so a degenerate whitespace-only `IndexName` is used as the real index yet is NOT warned. Whitespace index names are invalid for ES/OpenSearch anyway (they fail at the HTTP layer), and Qdrant/Milvus use `IsNullOrWhiteSpace` (a whitespace `CollectionName` is treated as "no override", never pinned) — so this is a narrow SearchEngine-local quirk left as-is rather than risk a behavior change to its blank-check.

### 9.15 Status — per-source provider pooling: dedup-for-Default + the lifetime contract (Mongo/Couchbase parity)

- **DONE.** The two LOWs deferred from the Couchbase fold review (§9.8) and the §9.12 fold — now closed for both document-family factories.
- **(a) Dedup-for-Default.** `MongoAdapterFactory` / `CouchbaseAdapterFactory` pool one provider per distinct physical source, with the Default source using the DI-managed provider. Previously a routed source whose resolved placement **coincided** with Default still opened a SECOND provider in the per-source cache (a duplicate `MongoClient` / cluster connection to the same server). Now each factory, after resolving the routed placement, reuses the DI-managed provider (and Default's full options) when it coincides with Default — Mongo compares `(connection, database)`; Couchbase compares `(connection, bucket, username, password, managementUrl)` so a reuse can never cross to a different cluster identity. The common trigger is a source relying on discovery: `AdapterConnectionResolver.ResolveRoutedConnection` collapses its `"auto"`/blank sentinel onto the Default connection, so it coincides and dedups. Coincidence is **exact-Ordinal** (not connection-string canonicalization) — a cosmetically-different-but-physically-identical connection opens a second pool (a benign efficiency miss, never a misroute; the dangerous direction — wrong-reuse — cannot happen because the comparison is the complete physical identity). Off (genuinely distinct source) ⇒ the existing cache path, byte-identical.
- **(a′) Inter-source key widened to full identity (review HIGH — Couchbase).** The Couchbase per-source cache was keyed `connection|bucket` only, so two distinct non-Default sources with the same connection+bucket but DIFFERENT credentials would collide on ONE pooled provider — source B silently authenticating as source A (a cross-source credential leak; pre-existing, surfaced by the §9.15 work in the method it touched). The cache is now keyed by the **value-tuple `(connection, bucket, username, password, managementUrl)`** — the same five fields the Default-dedup compares — so distinct-credential clusters never share a provider (a value-tuple key also removes the delimiter-collision risk a string key would carry). Mongo carries no separate credentials (auth/TLS/replicaSet are URI-encoded in the connection string), so its `connection|database` string key already captures cluster identity — no analogous fix needed.
- **(b) Lifetime contract — eviction is UNNECESSARY here, and a naive evictor would be UNSAFE; a safe one is deferred (not impossible).** Two distinct, accurate claims: **(1) Unnecessary** — the cache is bounded by the deployment's distinct-physical-source count (one client per distinct placement), NOT by entity count, so it does not grow with usage; this is the intended pooling contract. **(2) The naive evictor is unsafe, a safe one is unwired** — the repositories created here are cached upstream per `(entity, key, adapter, source)` and **hold** their provider for that cached lifetime, exposing no liveness/refcount signal, so a *naive* time/size-based LRU would dispose a still-live client. A **refcounted or idle-timeout-with-in-use-guard** evictor WOULD be safe, but it requires cross-cache coordination (the upstream repo cache signalling provider liveness) that is not justified at the current source-count bound. So eviction is a deliberate, recorded trade-off — deferred, not impossible. An unbounded-distinct-sources deployment (per-tenant-database at extreme scale) would want connection multiplexing, a different architecture, out of scope.
- **Tests-first (Docker-free):** `MongoSourceProviderDedupSpec` + `CouchbaseSourceProviderDedupSpec` build a minimal DI host and assert (i) the internal `SourceProviderCount` — Default + a Default-coinciding source ⇒ 0 pooled (both reuse the DI provider); distinct sources ⇒ one pooled each; re-creating for a pooled source ⇒ no growth; Couchbase same-connection-**different-bucket** ⇒ distinct placement; same-connection+bucket-**different-credentials** ⇒ distinct providers (the (a′) proof) — AND (ii) **provider IDENTITY** (the deduped store wraps the SAME DI provider instance; a distinct source wraps its own) so the count can't pass for the wrong reason (a store built on the wrong provider would keep the count at 0). (`InternalsVisibleTo` added to the Couchbase connector for the count hook; Mongo already had it.) No Docker — `MongoClient`/the Couchbase cluster connect lazily and the store ctor is facade-gated.
- **Note (pre-existing, out of scope — Mongo-specific):** Mongo's non-Default `sourceOptions` copy is partial (it omits `NamingStyle`/`Separator`/`CollectionName`), so a genuinely distinct Mongo source uses default naming rather than Default's overrides — a latent naming-consistency item independent of this dedup. Couchbase's `sourceOptions` copies the naming fields, so it does not share the gap. The dedup path sidesteps it on both by using Default's full options for a coinciding source.
- **Gates:** the dedup specs green (Mongo connector 66/66, Couchbase connector 14/14, both incl. the new specs); the live `MongoDatabaseRoutingSpec` / `CouchbaseAodbConformanceSpec` are byte-identical (their routed sources target distinct databases/buckets, never coinciding with Default, so they take the unchanged cache path); `MongoConnectionStringSpec` 34/34 untouched.

### 9.16 Status — the HTTP fleet adopts the conformance kit (the whole vector fleet co-defines through ONE base)

- **DONE.** All five HTTP vector adapters now run the shared `VectorAodbConformanceSpecsBase`, each a thin subclass replacing its bespoke `*VectorIsolationSpec`: `QdrantVectorAodbConformanceSpec` · `WeaviateVectorAodbConformanceSpec` · `ElasticSearchVectorAodbConformanceSpec` · `OpenSearchVectorAodbConformanceSpec` · `MilvusVectorAodbConformanceSpec`. With InMemoryVector (§9.11) + SqliteVec (§9.13) that is **all seven vector adapters co-defining the same four cells (Declares / Shared / Container / Database) through the one kit** — the consolidation the §9.11 follow-on called for. Each cell is **live** (the adapter's Testcontainers backend, a real `AddKoan()` host + tenancy + the discoverable `VectorConformanceShardAxis`), so each adapter's `DataCaps.Isolation.*` token declaration is co-defined against its realization on that adapter (declare-but-not-realize ⇒ red).
- **Each subclass is just `BootHostAsync`** (start the container + settings + `AddKoan()` + a `PostConfigure<TOptions>` endpoint pin) **+ `DisposeBackendAsync`** (stop the container); Weaviate adds **`SettleAsync`**. The kit's no-`[VectorAdapter]` conformance entities route to the adapter under test via the single-adapter-host convention (each test project references exactly one vector connector — asserted by the Declares cell's `HaveCount(1)`), and the kit's `VectorConformanceShardAxis` (Database-mode, discovered from the TestKit) replaces each adapter's bespoke shard axis. Net: five bespoke 3-method specs + their entities + shard axes → five thin subclasses; the behavioral cells + the Declares co-definition live ONCE.
- **Three additive kit-base extensions** (byte-identical for the in-proc cells — InMemoryVector 33/33 + SqliteVec 4/4 unchanged): (1) `DisposeBackendAsync` — a virtual teardown hook called after the host disposes, so a subclass can stop its container (the base owns `IAsyncLifetime`); (2) **`SettleAsync<TEntity>(id, query)`** — the per-save settle hook now carries the just-saved id + embedding so an async-index adapter can poll its own-scope search until that id is queryable (Weaviate's HNSW has no synchronous-refresh knob; ES/OpenSearch use `RefreshMode=true`, Milvus `ConsistencyLevel=Strong`, the in-proc/Qdrant adapters are immediately consistent ⇒ default no-op); (3) the Container/Database cells gained a **by-id physical-separation** assertion (`GetEmbedding` — the other scope's vector is ABSENT from this physical store), **conditional on adapter support**.
- **The migration surfaced a real kit-design gap (caught only by live Docker).** The by-id enrichment initially assumed universal `GetEmbedding` support, but **SearchEngine (ElasticSearch/OpenSearch) and Milvus do not implement by-id retrieval** (`NotSupportedException`) — they failed Container+Database until the assertion was gated behind a `SupportsGetEmbedding` probe. Now Qdrant/Weaviate/InMemoryVector/SqliteVec get the stronger by-id proof; ES/OpenSearch/Milvus prove isolation by the kNN Search alone (which all support). Zero coverage loss vs the bespoke specs (the bespoke Qdrant used by-id; the bespoke ES/OS/Milvus used Search), and no adapter over-asserts.
- **Non-isolation specs preserved untouched** (only the bespoke `*VectorIsolationSpec` files were deleted): the Weaviate matrix + `WeaviateOverlayIsolationSpec` (the ARCH-0102 `__`→`koan_` overlay rename proof), and each adapter's `*MatrixSpecs`/`*QuantizationSpecs`.
- **Gates (live Docker):** all five HTTP cells 4/4 — Qdrant · Weaviate (with the WaitIndexed settle) · ElasticSearch · OpenSearch · Milvus (etcd+minio+milvus stack) — plus InMemoryVector 33/33 + SqliteVec 4/4. The full vector fleet's AODB three-mode isolation is now co-defined and live-proven through the single kit.
