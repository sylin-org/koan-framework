# Prompt Stash Progress Ledger

One-stop tracking for the assessment's implementation prompts. Each row maps to a
self-contained card file under [`06/`](06/) (tactical) or [`07/`](07/) (strategic).
Canonical sources: [`../06-prompt-stash.md`](../06-prompt-stash.md) and
[`../07-strategic-prompt-stash.md`](../07-strategic-prompt-stash.md).

**Agents**: open your card file, paste it into a fresh session, and update your row here —
status `in-progress` when you start, `done` / `blocked` / `obsolete` (with a one-line note)
when you finish. Link commits by short SHA. If repo reality contradicts the card, record it
in the **Divergence log** at the bottom.

**Tier** (load-bearing — do not feed frontier cards to small models):
`T1` small model, autonomous · `T2` small model, recipe-driven · `T3` frontier model only.

**Status**: `pending` · `in-progress` · `done` · `blocked` · `obsolete`.

## Run order (it's a gated DAG, not a strict sequence)

```text
FIRST:  B1 (sln truth) · F1 (fail-loud boot — improves every later session's error visibility)
THEN:   A1 A2 A3 · C0 · all cut/park cards (C*) · most E* · D* · G* — any order, B1 first for cuts
LATE:   H-series (docs/DX) after A2 lands · B2/B3 (CI/release) after B1
07:     entire strategic ladder gated on B1 + F1 + A2 first; then P1 → P2 → P3 → P4 → P5,
        with hard gates noted per row (P4.1 ← Facet 3; P4.2 ← 06 S3).
```

## 06 — Tactical (truth restoration, cuts, migrations, DX)

Preamble for these cards: the `[PREAMBLE]` block in `../06-prompt-stash.md`.

> **REORG (2026-06-14).** The cut/park cards were reclassified by
> [08-agyo-reorganization.md](../08-agyo-reorganization.md) (+ agyo-tools `AGYO-0001`) into a
> three-way disposition. **→ agyo (migrate):** C2 (done — `Sylin.Agyo.WebSockets`), C4, C7, C9,
> C10, C13, C17. **→ split:** C5 (delete Recipe.Abstractions / migrate Observability bundle), C8
> (delete ServiceMesh / migrate Translation). **→ confirmed delete:** C1, C3, C6, C11, C14, C19.
> The Status/Notes columns below remain the **Koan-side** truth (cut/reverted/blocked as executed);
> **agyo-side migration status is tracked in agyo-tools `docs/SURFACES.md`**, not duplicated here.
> Consumer-facing migrations (C5/C7/C9/C17) proceed under transition safety. See the Divergence log.

| ID | Tier | Deps | Status | Date | Agent/model | Commits | Notes |
|---|---|---|---|---|---|---|---|
| B1 | T2 | — (first) | done | 2026-06-13 | opus-4.8 | (uncommitted) | sln truth — added 25 buildable test projects (168→195 incl. 2 transitive prod refs); EXCLUDED 13 broken/husk projects beyond the card's 5 (see Divergence log) — build green, `--list-tests` enumerates 2922 tests / 71 assemblies |
| B2 | T2 | B1 | done | 2026-06-18 | opus-4.8 | c8348b85 | **Premise CONFIRMED (not stale this time):** `ci.yml`/`validate-main-pr.yml` are `disabled-*` `workflow_dispatch`-only noops; create-tag/nuget-release/tag-on-main disabled/superseded; only `release-on-main.yml` (build-only) + `surfaces.yml` (ledger lint) active → no PR build/test gate (`surfaces.yml`'s own header says "general build/test CI is disabled by design"). Added `.github/workflows/pr-gate.yml` (on `pull_request`→main/dev, concurrency-cancel, `contents:read`) that runs **`scripts/green-ratchet.ps1 -Configuration Release -Base origin/<pr-base>`** — the canonical local merge gate (build + test `--no-build` + docs-lint + doc-code diff-scoped to the PR base). **Deviation from the card's inline-steps recipe → reused green-ratchet** (it already composes EXACTLY those 4 legs; card step 1 said READ it → intent was reuse; keeps CI==local, no drift). Verified locally (Release): build + docs-lint (0 err / 1331 non-fatal warns) + doc-code (4 marked blocks, diff-scoped to 28 changed docs) GREEN; test-leg `--no-build` mechanics confirmed on an in-sln suite (5/5). Full container matrix is CI-only. Did NOT touch release-on-main.yml (B3). **Note: ubuntu-latest HAS Docker → container suites RUN (not skip) in CI; the first PR run may surface the X-testcontainers-modernize flakiness — that's the gate working, not a B2 defect.** |
| B3 | T3 | B1, B2 | done | 2026-06-18 | opus-4.8 | dbdd8d48 | **3 sub-tasks; sub-task 1 (NBGV rework) was already DONE (re-derived stale).** The cited `testsBuild.json` evidence described the PRE-rework `release-on-main.yml` (Update-Versions.ps1 + build/versions.props); the CURRENT workflow is already NBGV-native (`PublicRelease=true` + per-package version.json), and versions.props/Update-Versions.ps1 no longer exist (`Directory.Build.props:60` names them only in a comment). **Sub-task 3 (test gate):** release-on-main.yml ran ZERO tests → added `dotnet test Koan.sln -c Release --no-build` between build and pack (refuse to publish a failing main). **Sub-task 4 (metadata):** `net9.0`→`net10.0` in the 2 meta-package nuspecs + `sylin-labs`→`sylin-org` in ALL package-facing URLs. **Expanded beyond the card's literal "packaging/*.nuspec":** the same stale URLs live in `Directory.Build.props` (the root that stamps every Sylin.Koan.* package via PackageProjectUrl/RepositoryUrl/PackageReleaseNotes) + 4 PACKED src READMEs (Koan.AI/Core/Data.Core/Messaging.Core) — fixing only the 2 nuspecs would leave ~150 packages wrong. Authors "Sylin Labs" PRESERVED (canonical display name, not a URL). **Verified by PACKING `Sylin.Koan.Core` → its generated nuspec shows `github.com/sylin-org` URLs**; YAML parses; touched XML well-formed. `release-from-dev.ps1` is DEPRECATED (hard `exit 1`) — NOT "the newer path" the card guessed; the workflow is the path (no delegation). |
| A1 | T1 | — | done | 2026-06-13 | opus-4.8 | c3a13de8 | 30 ADRs marked; docs-lint 0 errors. Drift: FLOW banner reworded truthfully (Flow→Canon mid-flight, not removed); DATA-0019 skipped (card mislabels Outbox as Cqrs; no CQRS ADR exists) |
| A2 | T2 | — | done | 2026-06-14 | opus-4.8 | 8bcc0bb6 | ghost APIs (Flow/.Embed/koan-CLI/Describe(BootReport)) + 0.6.3 pins removed across 6 docs; docs-lint clean. Ran parallel w/ H5+H7 |
| A3 | T1 | — | done | 2026-06-13 | opus-4.8 | ad13b65c | 11 tracked litter + 103 *.lscache (card said ~9) + stale Cache/Unit; build green; ran parallel w/ A1; fixed 1 cross-card link (DEC-0053) |
| C0 | T1 | — | obsolete | 2026-06-15 | opus-4.8 | — | **No-op: all 19 target directories already gone** (swept by the agyo reorg + Canon migration + C-series cuts). Verified each path absent (`[ -e ]` = 0/19 present). Nothing to delete; build already green. See Divergence log. |
| C1 | T2 | B1 | done | 2026-06-14 | opus-4.8 | 825e6fd0 | cut Koan.Data.Cqrs + Mongo outbox (zero consumers → JOBS-0005); DATA-0019 superseded; build + bootstrap 24/24 green; ran on dev alongside user's Mcp work |
| C2 | T2 | B1 | done | 2026-06-13 | opus-4.8 | ffef0899 | cut Koan.WebSockets (197→195); 0 src consumers, absent from ledgers/nuspec; build + bootstrap integration 17/17 green. **→ REORG: migrated to agyo** (Sylin.Agyo.WebSockets.0.1.2, build+pack green) — the proven reference pattern for the other migrations |
| C3 | T2 | B1 | done | 2026-06-13 | opus-4.8 | 035dc891 | cut Koan.Web.Json.Strict (195→193); STJ island, 0 consumers, absent from ledgers/nuspec; build + bootstrap integration 17/17 green |
| C4 | T2 | B1 | done | 2026-06-14 | opus-4.8 | 5c2d7498 | attic-tagged + cut GraphQl connector (archived S4 sole consumer; HotChocolate CVE treadmill); WEB-0041/0042 superseded; tag attic/koan-web-graphql; not consumed; build green |
| C5 | T2 | B1 | done | 2026-06-14 | opus-4.8 | d3f97694 | **SPLIT done (reorg).** Was reverted (Recipe.Observability consumed downstream). Now: Koan.Recipe.Abstractions DELETED (superseded by ARCH-0086 KoanModule); Observability bundle migrated → Sylin.Agyo.Observability (re-expressed as a KoanModule, real OTel). Both removed from Koan (consumers re-pointed); build green. |
| C6 | T2 | B1 | done | 2026-06-14 | opus-4.8 | 54576324 | cut Inbox-Redis connector (dead client API, only archived consumer); MESS-0025/0026 already retired; build green, sln 288→284 |
| C7 | T2 | B1 | done | 2026-06-14 | opus-4.8 | d3f97694 | **Migrated (reorg).** Was blocked (consumed). Now: Koan.Secrets.Abstractions/Core + Vault → Sylin.Agyo.Secrets.* (3 projects); consumers re-pointed; removed from Koan. The fail-soft reflection seam TryInvokeSecretsBootstrap STAYS in Koan.Data.Core (no-ops when the assembly is absent). Build green. |
| C8 | T2 | B1 | done | 2026-06-14 | opus-4.8 | f7d8a499 | cut ServiceMesh + .Abstractions + Translation (4 projects) + excised Web.Admin defensive surface; S8 atticked; not consumed downstream; build green, zero refs |
| C9 | T2 | B1 | done | 2026-06-14 | opus-4.8 | d3f97694 | **Migrated (reorg).** Was aborted (consumed downstream — the finding that forced the external-consumer gate). Now: Koan.Tagging → Sylin.Agyo.Tagging (clean lift, doc-drift cleaned); consumers re-pointed; removed from Koan (+ its unit test). Build green. |
| C10 | T2 | B1 | done | 2026-06-14 | opus-4.8 | d5c03ae0 | parked Koan.Rag + Abstractions to attic (zero consumers, recoverable); deleted Rag bootstrap spec (F1 specs intact); not consumed downstream; build green |
| C11 | T2 | B1 | done | 2026-06-15 | opus-4.8 | 2abbe1dd | **Partial by STOP discipline (card premise stale).** Cut the genuinely-inert surface: `src/Koan.AI/Pipelines/**` (5 files — TextPipeline/ImagePipeline/IAiPipelineStage/PipelineContext/StorageResult, zero refs outside the folder) + the dead internal-duplicate `Koan.Data.AI/Workers/MediaAnalysisEmbeddingBridge` (`ComposeEmbeddingText`, 0 callers). Kept the public `MediaAnalysisEmbeddingBridge` (`ComposeText`, tested). **EXCLUDED `PipelineAiExtensions.cs` (card's own STOP fired):** `.Tokenize` is the LIVE stream DSL over Koan.Core `PipelineBuilder<T>` (distinct system), consumed by S5.Recs/SeedService.cs + documented in ai-vector-howto §7. Annotated AI-0020. Build 0 err; AI Unit 151/151; docs-lint 0. |
| C13 | T2 | B1 | done | 2026-06-14 | opus-4.8 | 3f3c1f68 | cut PGVector (didn't compile) + orphan test + dead IVectorFilterTranslator/VectorEmbeddingAttribute; unwound B1's transitive sln-add; tagged attic/pgvector; not consumed; build green, sln 279→277 |
| C14 | T2 | B1 | done | 2026-06-14 | opus-4.8 | 99c1ef70 | removed legacy ResilientStorageDecorator + Resilient flag (kept StorageFallbackMode + default/Replicated path untouched); external audit clean (downstream uses Koan.Storage, not Resilient); build green. Done directly by orchestrator (agent rate-limited) |
| C17 | T2 | B1 | done | 2026-06-14 | opus-4.8 | (scheduling agent) | **Migrated + fully implemented (reorg).** Was blocked (consumed). Now: Koan.Scheduling → Sylin.Agyo.Scheduling (and the ~60% vaporware FINISHED in agyo: cron/locks/windows/health). Koan-side: S5.Recs S5BootstrapTask → Koan.Jobs @boot job; dropped Koan.Web's Scheduling ref + the dead /.well-known/scheduling endpoint; cleaned KoanServiceActions/Events.Scheduling in Core; removed the project. Build green. (Out-of-sln KoanContext still refs it — separate X-KoanContext.) |
| C19 | T2 | B1 | done | 2026-06-15 | opus-4.8 (agent+review) | 36d8bab1 | deleted the `[Obsolete("Removed in MEDIA-0008")]` IMediaOutputCache family (5 types) + MediaController `_legacyCache` shim (kept the 6b storage write-through + bufferedOutput) + registrar opt-in block + MediaWebOptions.OutputCache + the obsolete-shim test (CaptureReplayCache); deleted ProfileMedia/ColdProfileMedia (only consumer = archived non-sln S6.SocialCreator). **2 ripples the card under-specified, both entailed:** MediaWebOptions.OutputCache property + the shim's own test. **1 deviation (root fix, see Divergence):** Koan.Media.Web reached Koan.Web *transitively* through Core, so cutting Core's ref required a direct ref on Web. Docs swept (no ghost-API). Verified independently: build 0 err; Media 567/567; docs-lint 0; grep 0 residual. |
| C20a | T2 | B1 | done | 2026-06-15 | opus-4.8 (agent+review) | dfa5c9a7 | Merged the 6-file `Koan.Web.Connector.Swagger` into `src/Koan.Web.OpenApi/Swagger/` (OpenApi's only consumer — 1:1). **Public surface preserved** (`AddKoanSwagger`/`UseKoanSwagger`/`KoanWebSwaggerOptions` + namespaces + module Id) so the 6 samples calling them compile unchanged. Added `KoanOpenApiOptions.EnableUi` (`Koan:OpenApi:EnableUi`) as the canonical UI gate, wired in BOTH enablement sites as an override checked first → falls through to the existing logic untouched (**legacy `Koan:Web:Swagger:Enabled` + default-true-in-Dev fully preserved** — user-approved design). Added Swashbuckle PackageRef; removed the project + empty "Swagger" sln folder; repointed all 12 consumer csprojs. **Gate logic reviewed line-by-line (no Swagger test net).** Full clean build 0/0; Web suites green. **Test-net follow-up CLOSED 2026-06-17 (`a876fa8d`):** new `Koan.Web.OpenApi.Tests` WAF suite (in `Koan.sln` → CI) pins the `Koan:OpenApi:EnableUi` gate, 5 specs, mutation-verified — see Divergence log. |
| C20b | T2 | B1 | done | 2026-06-14 | opus-4.8 | 31898d9e | merged Koan.Admin (dead console-UI split, EnableConsoleUi never implemented) into Koan.Web.Admin (24 files, namespaces folded); not consumed downstream; build green, sln 182→181 |
| C20c | T2 | B1 | done | 2026-06-15 | opus-4.8 (agent+review) | ea927c38 | Folded the 20-file `Koan.Web.Transformers` into `src/Koan.Web/Transformers/` (namespaces + module Id preserved) — dissolving the circular dep (Transformers→Web) that forced the 3 `Type.GetType` shims; all 3 de-reflected to direct refs (shims 1/2 use now-same-assembly internals; shim 3 uses the public `ITransformerActivationPredicate`/`ITransformerRegistry` over the existing OpenApi→Web ref). **Opt-in preserved automatically** — the output filter no-ops unless a controller implements `ITransformerActivationPredicate`. Removed from sln; test repointed to Koan.Web. Verified: full clean build 0/0; Transformers 13/13 + Extensions 13/13. (Sort/Admin = pre-existing B1 husks, not in sln.) |
| D1 | T3 | — | pending | | | | break the kernel inversion (Core → Orchestration.Abstractions) |
| D2 | T2 | — | done | 2026-06-15 | opus-4.8 | 61c7eaa9 (+ deletions in 6174b79b — see Divergence) | **Partial by mandate** ("delete only the *verified-dead* surface"). DONE: item 1 (dead markers IServiceAdapter/IKoanService/IDevServiceDescriptor + Koan0049A diagnostic) + item 5 (tombstone IVT). MOOT: item 4 (PGVector/KoanContext already gone). **NOT dead → excluded** (architect-DECIDED: retained + in-code annotated `// ARCH-0077`, deferred to the ARCH-0077 migration): item 2 (legacy attrs still read by CLI ProjectDependencyAnalyzer + ComposeExporter), item 3 (SelfOrchestration is Development-default-live), item 6 (Cli.Core is a published periphery package, not all-internal). Build green; Bootstrap 23/23. |
| E1-postgres | T2 | B1 | done | 2026-06-15 | opus-4.8 (wave) | f1f810e7 | folded PostgresRegistration → registrar; carried the unique IDataProviderConnectionFactory→PostgresConnectionFactory (auto path was missing it). Fixture repointed to AddKoan(). Suite 8/8 (Testcontainers). |
| E1-mongo | T2 | B1 | done | 2026-06-15 | opus-4.8 (wave) | f1f810e7 | pure-duplication delete (registrar already a strict superset — git diff registrar empty). Fixture already used AddKoan(). Suite 20/20 (Testcontainers). |
| E1-sqlite | T2 | B1 | done | 2026-06-15 | opus-4.8 (wave) | f1f810e7 | folded SqliteRegistration → registrar; carried the unique IDataProviderConnectionFactory→SqliteConnectionFactory (DATA-0053; auto path was missing it). DataCoreRuntimeFixture repointed to AddKoan(). Suite 3/3 + Data.Core 130/130. |
| E1-sqlserver | T2 | B1 | done | 2026-06-15 | opus-4.8 (wave) | f1f810e7 | folded SqlServerRegistration → registrar; carried 2 unique lines (SqlServerToRelationalBridgeConfigurator [relocated to own file] + IDataProviderConnectionFactory→SqlServerConnectionFactory). Fixture repointed to AddKoan(). Suite 18/18 (Testcontainers). |
| E1-couchbase | T2 | B1 | done | 2026-06-15 | opus-4.8 (wave) | f1f810e7 | folded CouchbaseRegistration → registrar; carried 2 unique lines (IAsyncAdapterInitializer + IAdapterReadiness → CouchbaseClusterProvider). Fixture already used AddKoan(). Suite 7/8 — the 1 fail is a PRE-EXISTING Docker.DotNet/Testcontainers version mismatch (→ X-couchbase-testcontainers), NOT this change; registration verified by build + Bootstrap + the 7 passing. |
| E1-json | T2 | B1 | done | 2026-06-15 | opus-4.8 (wave) | f1f810e7 | pure-duplication delete (registrar a strict superset); also deleted the JsonRepo DI-bypass factory (0 framework consumers, grep-verified). Both fixtures repointed to AddKoan(). Suite 7/7. |
| E2 | T2 | — (soft: C7) | done | 2026-06-15 | opus-4.8 (wave) | 1d0d4c04 | **Architect chose "author the adapter now" → ARCH-0087.** RabbitMq brought onto the V2 model: new RabbitMqServiceDescriptor ([KoanService] Messaging, amqp, rabbitmq/localhost:5672) + RabbitMqDiscoveryAdapter (real AMQP health check; legacy RABBITMQ_URL/Koan_RABBITMQ_URL preserved). Both V1 sites migrated (runtime via IServiceDiscoveryCoordinator; DI-less Describe via direct adapter ctor; RequireHealthValidation=false for V1 parity / no boot stall — orchestrator remediation of the reviewer's MEDIUM). V1 retired (resolver + 2 connection-resolvers + ForRabbitMQ, grep-zero-ref); OrchestrationAwareServiceDiscoveryV2 → OrchestrationAwareServiceDiscovery. Build green; Bootstrap 23/23. |
| E3 | T2 | — | done | 2026-06-15 | opus-4.8 (wave) | 9c231982 | **Architect chose "keep both, rename for intent"** — the unify-on-one premise was REJECTED: the static coalescer and the DI gate are distinct primitives. The misnamed DI ISingleflightRegistry/SingleflightRegistry → IKeyedLeaseGate/KeyedLeaseGate (ns Koan.Core.Singleflight → Koan.Core.Concurrency; AddKoanSingleflight → AddKoanKeyedLeaseGate); pure rename, logic byte-identical, no consumer migrated. Static Koan.Core.Infrastructure.Singleflight coalescer kept + doc-cross-referenced. Zero-caller relational shim deleted. Living docs (CLAUDE.md, koan-cache-module.md) synced. Build green; Cache (Sqlite+Redis) + Sqlite-relational suites green. |
| E4 | T2 | — | done | 2026-06-15 | opus-4.8 (wave) | 75e79506 | deleted the never-registered root Koan.Core HealthAggregator + HealthAggregatorOptions duplicate (+ the collision full-qualification comment + the dead async RunContributor twin); kept IHealthContributor (public seam) + IHealthRegistry/HealthRegistry/HealthContributorsBridge. Build green; Bootstrap 23/23. |
| E5 | T3 | — | done | 2026-06-17 | opus-4.8 | f71cdbe4 | auth flow engine swap **DONE** (OIDC-501 fixed + PKCE + ARCH-0079 spec). chunk 1 (`1540fe05`) SAML/merge; chunk 2 (`244681c2`) contributor-retire; **WEB-0071** (`4d021ea9`) decision; **chunks 3–5 (`f71cdbe4`)** the swap: OAuth2/OIDC routed to the maintained `OAuthHandler`/`OpenIdConnectHandler` via dynamic scheme seeding (`AuthSchemeSeeder` in `KoanWebAuthStartupFilter` — manual `*PostConfigureOptions`→`IOptionsMonitorCache`→`AddScheme`), parity hooks (`UserInfoMapper` role/perm mapping + `IExternalIdentityStore.Link`) in the handler events, fail-closed on missing sub (drops the old `SHA256("")` collision), correlation/nonce cookie Path=root + Lax-for-http-dev. Test provider extended into a mini OIDC IdP (discovery + JWKS + ES256 id_token + nonce; userinfo returns sub). New real-Kestrel e2e drives OAuth2 **and** OIDC round-trips (real id_token validation + PKCE + nonce), role-mapping mutation-verified. Full-sln 0 err; e2e(2)+dispatcher(18)+bootstrap-auth(4)+Security.Trust(12) green. Architecture chosen via a research+adversarial-decision workflow (Option A — see Divergence log). |
| E6 | T3 | — | done | 2026-06-17 | opus-4.8 | 6780772c | ES/OS shared core consolidation **DONE** (DATA-0103). Extracted the byte-identical skeleton into `SearchEngineVectorRepository<TEntity,TKey>` in the existing `Koan.Data.SearchEngine`, behind a 3-member `ISearchEngineDialect` seam (`BuildSearchRequestBody`/`BuildIndexBody`/`MapSimilarityToken`); both thin packages kept; `ExportAll`/`GetCount`/`IndexStats` implemented once → **OS capability gap closed**. 5 drift bugs fixed (OS factory `elastic` cross-claim; dead ES `DefaultTopK`/`MaxTopK`; OS `sourceKey` provenance back-port; colliding `search` discovery alias dropped from both; ApiKey branch kept-with-note). Supersedes DATA-0097 §6 (its "REST/transport wiring differs" rationale is empirically false). Build 0 err; **BOTH** VectorAdapterSurface Testcontainers suites green on independent re-run (ES 25/4skip, OS 25/4skip — identical; OS now runs the 3 new ExportAll/Stats specs). Extraction delegated to a focused agent; reviewed (dialects byte-faithful) + independently re-verified. |
| E7 | T2 | — | done | 2026-06-15 | opus-4.8 (wave) | 0983d31e | deleted dead surface (IMessageBusSelector / IQueuedMessage / the SendToQueueAsync reflection fallback / zero-registration MessagingTransformers; kept MessagingInterceptors); replaced the per-send reflection with a cached typed-delegate dispatch. **Found + fixed a latent bug**: the old reflection targeted `GetMethod("Send")` which NO proxy exposes (only `SendAsync<T>`), so the non-queued send path always threw — rewrite targets the real SendAsync (the broken path was never test-exercised). Build green; Cache.Coherence.Messaging 14/14; Jobs.Transport 1/1; S3.Mq sample builds. |
| E8a | T2 | — | done | 2026-06-15 | opus-4.8 | 5bbec1df | declared `DataCaps.Write.BulkUpsert` + the `IBulkUpsert<TKey>` marker (a no-method marker — DATA-0003/ARCH-0084). `UpsertMany` at `:634` already does `INSERT … ON CONFLICT (Id) DO UPDATE` in a tx → equivalent to Sqlite (:39/:48) / SqlServer (:35/:44). Spec flipped `BeFalse`→`BeTrue`. **Adversarial check clean:** no other surface asserts Postgres caps; Web AdapterSurface already exercised `/bulk` (default `SupportsBulkUpsert=>true`). Build green; Postgres suite 8/8 (caps + FilterConvergence, Testcontainers). |
| E8b | T3 | — | done | 2026-06-17 | opus-4.8 | f6a6e695 | Couchbase native CAS via `IConditionalWriteRepository` (Get→capture CAS→guard on current→CAS-guarded Replace→CasMismatch/NotFound→false); declares `Write.ConditionalReplace` → JOBS-0005 §20.3 contention-free claiming on Couchbase. **FastRemove DECLINED** (architect): RemoveAll already server-side N1QL DELETE; the drop+recreate-collection analog pays a multi-second CREATE PRIMARY INDEX warmup (slower common-case) + brief index unavailability → no path beats DELETE; declaring it = dishonest cap (ARCH-0084). ADR DATA-0102. **Self-review caught a key-order bug** (GetKey before PrepareEntityForStorage → a dashes-format GUID id computes the wrong key → false; reordered to match Upsert). ARCH-0079 spec (real Couchbase: guard-holds applies + stale-guard no-op, 1/1, mutation-checked guard-drop→fail); full sln 0 err; docs-lint 0. (`CasMismatchException` lives in `Couchbase.Core.Exceptions`, not `.KeyValue`.) |
| E8c | T3 | — | done | 2026-06-17 | opus-4.8 (agent+review) | f108606b | **Premise re-derived STALE — "retract FilterSupport.Full" REJECTED.** `FilterSupport` is operator-correctness, not pushdown efficiency (ARCH-0084): the InMemory reference adapter declares `Full` identically, and the coordinator residual is the entity-path in-memory floor (`Data.cs:99-101`), so `None` would be behaviorally identical + inconsistent + model-contradicting (the "hard error on residual" is the VECTOR path only). Surfaced the correction to the architect mid-card → re-chose keep-Full + native TTL (ADR DATA-0101). Impl: declared `DataCaps.Retention.TtlIndex` + honor single-property `[Index(Ttl)]` via one atomic `SET PX` (past→delete, null→persistent) in a shared `WriteWithTtlAsync` (Upsert + UpsertMany). Adversarial review caught a real non-atomic SET+EXPIREAT crash-leak in the first cut → fixed; rejected its "subtract past-expiry from count" as Mongo-divergent. 3/3 (caps + single + batch TTL), mutation-verified ×2; full sln 0 err; docs-lint 0 err. Corrected stale "Full dishonest" framing in SURFACES/cartography/recommendations. |
| E9 | T2 | — | done | 2026-06-15 | opus-4.8 | 2a2efc09 | `With(...)` summary said "Replaces any previous context (does not merge)" but `:140-146` does `arg ?? prev?.X` (inherit-unless-overridden). Rewrote the XML doc precisely (incl. mutual-exclusion enforced on the **effective** post-inheritance values — adapter-while-source-inherited throws). Production change XML-comment-only. Added `WithContextInheritanceSpec` (6/6) — no prior pin existed (grep: only `Transaction` inheritance was pinned). |
| F1 | T2 | — (early) | done | 2026-06-14 | opus-4.8 | 5781b18f, aa981cbf | fail-loud boot (KoanBootException + MODULES-FAILED block + KOAN_BOOT_LENIENT); ARCH-0079 specs. **Gate caught a coverage gap** (manifest-invoker + render untested) → remediated (+5 specs + minimal test seam); BOTH mutation checks pass (specs fail when behavior reverted). Bootstrap 24/24 |
| F2-sqlite | T2 | F1 | done | 2026-06-15 | opus-4.8 (wave) | 02a6f514, 15bfa32d | burned down all 23 silent catches in SqliteRepository.cs (narrow+log degradable; log-error correctness DROP/DDL). **Orchestrator remediated 3 review findings:** MEDIUM secret leak (cs now `Redaction.DeIdentify` — pinned by mutation-checked test `15bfa32d`), 2 over-narrowed wait-barriers broadened (`ObjectDisposedException` was escaping). Build + Sqlite suites + Bootstrap 23/23 green |
| F2-data-core | T2 | F1 | done | 2026-06-15 | opus-4.8 (wave) | 430a05e8 | 3 malformed-setting fallthroughs (AdapterConnectionResolver) → narrow + `ConfigWarning`. Reviewer SOUND. Data.Core suite 130/130. Warning-capture test deferred to X-f2-failure-coverage (routes through the process-global static KoanLogScope) |
| F2-web | T2 | F1 | done | 2026-06-15 | opus-4.8 (wave) | 91651138 | WellKnownController degradable diagnostics broadened+logged (**orchestrator fixed an over-narrow regression** that failed the whole `/aggregates` endpoint); KoanWebStartupFilter documented no-throw guard; 5 typed fallbacks kept; 3 static-context sites stopped-ambiguous (EntityController:343 → X-entitycontroller-query-parse). Web suites 64+13 green |
| F2-storage | T2 | F1 | done | 2026-06-15 | opus-4.8 (wave) | 44eea2b6 | stream-capability probes narrowed to NotSupportedException; cache→durable + Dispose persists now LogDebug. No correctness swallow. Dedicated Storage suites are pre-existing husks (NU1101/stale-API CS) — covered by full build + Bootstrap boundary; failure-path tests → X-f2-failure-coverage |
| F2-mcp | T2 | F1 | **done** | 2026-06-20 | opus-4.8 | `5154d137` | swallow burn-down: Koan.Mcp — unblocked once AN6 MCP work completed (boundary lifted). Empirical eval re-derived the sites (~33 catches/16 files; ~24 already correct). **9 remediated**: error-hiding → fail-loud (ResponseTranslator catch→null ⇒ rethrow JsonException-with-context mirroring RequestTranslator; McpCustomToolInvoker bare `catch{}` → narrowed to conversion exceptions; JintCodeExecutor broad validation catch → LogDebug); best-effort broad → narrowed (McpSession/McpSessionManager CTS `catch{}` → `catch (ObjectDisposedException)`; StdioTransport ×4 shutdown disposes → LogDebug-observable). **Dedup eval: NO shared helper** — same anti-pattern at distinct seams, not duplicate logic (a JsonException-translation helper [5 sites] / CTS helper [2 sites] = over-abstraction vs. inline narrowing + would edit tested RequestTranslator). **0 untyped catch-all remain**. Conformance 72/72 + codemode 27/27 + Streamable 18/18 hold; error paths defensive/hard-to-reach (test deferral per F2-data-core/storage precedent). |
| G1 | T3 | — | done | 2026-06-17 | opus-4.8 (agent+review) | 8ddb076f | Extracted OTel wiring → new opt-in leaf **Koan.Observability** (ADR ARCH-0088) + Reference=Intent registrar. Core premise CONFIRMED but **card's "move ObservabilityOptions" CORRECTED** (it's type-referenced by `AppRuntime` in Core → would cycle; stays in Core; only the SDK wiring + 5 packages moved). Architect chose **full opt-in** → also stripped Koan.Web's 3 DEAD OTel refs. **Extraction EXPOSED 4 latent free-riders** on `Microsoft.AspNetCore.App`/`Microsoft.Extensions.Http` (leaked transitively via OTel `Instrumentation.AspNetCore`/`.Http` through Core): Data.AI+Cache→`HealthChecks` pkg, Data.Backup→`AspNetCore.App` FrameworkRef, LMStudio→`Extensions.Http` — each given its MINIMAL-correct dep (not framework-on-all). ARCH-0079 spec (`Koan.Observability.Tests` boots real `AddKoan()`→TracerProvider wired by registrar, **mutation-checked**). Adversarial review caught a real double-pipeline trap (registrar + explicit call stack `WithTracing`) → added idempotency marker guard. Full sln 0 err; docs-lint 0; SURFACES row added. |
| G2 | T1/T2 | — | done | 2026-06-15 | opus-4.8 (wave) | 966ba5f5, 0b890f9e | **Done (STOP discipline corrected the card mid-flight).** Safe subset (`966ba5f5`): deleted EntityTransferExtensions (grep-zero-ref), trimmed KoanServiceActions/Events to referenced constants (removed zero-ref Outbox/Canon/Translation; kept Health/Messaging), rewrote Koan.Core.Adapters README/TECHNICAL to existing types (ghost BaseKoanAdapter system removed). **FluentApi cut (`0b890f9e`, architect-approved relocate-then-cut):** relocated the live IServiceRegistry/ServiceLocator to BackgroundServices/ServiceRegistry.Contracts.cs (pure file move — ns was already Koan.Core.BackgroundServices) then deleted the dead unfinished fluent DSL (KoanServices.Do/On/Query + ServiceBuilder + IService*Builder + fluent-only DTOs), −449 LOC. **JsonPathMapper kept** — real test consumer + DATA-0063 reference (card's "zero external consumers" premise was wrong). Build green; Core.Tests 179/179 + Core.Unit 43/43. |
| H1 | T3 | A1, A2 | done | 2026-06-18 | opus-4.8 | caa98610 | **The onboarding fix (§8.4).** Created `templates/` — a content-only template pack `Sylin.Koan.Templates` (PackageType=Template, ISOLATED from root build props to dodge NU5017) shipping **koan-web** (4-line Program.cs + `Entity<Todo>` + `EntityController<Todo>` + Sqlite, REST auto-mapped) + **koan-console** (`StartKoan()` + Entity CRUD over Sqlite). Both reference the meta-packages (`Sylin.Koan.App`/`Sylin.Koan`, architect-chosen concept budget); READMEs print the boot-report-as-demo + the Newtonsoft camelCase/ignore-nulls defaults (§6.1). **Wired into release-on-main.yml** (architect chose "wire meta+templates into release"): a new step packs the 2 meta-package nuspecs (versioned from the Sylin.Koan.Core leaf; dep range `[ver, 0.nextMinor.0)` per compat-ranges) + the template pack → existing push publishes them. **Closed a PRE-EXISTING gap** (nothing published the Sylin.Koan/Sylin.Koan.App meta-packages — release packed only src/*.csproj) + fixed the meta-nuspecs' broken icon (icon.png→resources/image/0_2.jpg) + removed orphaned `scripts/pack-templates.ps1`. **Verified:** template pack packs(0 err)+installs+instantiates both+`sourceName` rename; both meta-packages pack w/ correct deps+icon; a generated koan-web BUILDS clean against live src (ProjectRefs, 0 err). The published-meta restore runs on first release. Zero src/tests edits. |
| H2 | T2 | A2 | done | 2026-06-15 | opus-4.8 | 2019a37b, b7a82698 | **ARCHITECTURE CORRECTED: compile-everything → OPT-IN.** Scout proved ~95% of the "777 failures" were HARNESS artifacts: the validator compiled EVERY block as a standalone program, so fragments (undefined placeholder locals, prose-defined types, multi-pillar montages) failed for being INCOMPLETE, not wrong. I band-aided symptoms for several iterations (rich usings → top-level detection → per-doc type-context → poison guard) — an unwinnable arms race the user halted ("you're fighting symptoms, evaluate the architecture"). **Inverted to opt-in** (`b7a82698`): a block compiles only when marked `<!-- validate -->` (a complete self-contained example that MUST compile); everything else is prose-grade by default. Kills the arms race, makes the gate honest + high-signal (the marked set is the contract that must never go stale), makes the full sweep CHEAP (a handful, not ~777). Kept the Phase-1 wrapping (hoist usings + rich set + top-level/declaration/fragment classification + Model/Web.Controllers/Capabilities usings) for the marked examples. Seeded the onboarding surface: README ×2 + quickstart ×2. **Full sweep: 78 docs, 4 marked, 0 fail.** Ratchet Leg C already wired (green-ratchet); doc updated. Broadening coverage = authors mark more complete examples over time (incremental, safe — no longer an arms race). |
| H3 | T2 | — | done | 2026-06-14 | opus-4.8 | ff4f363b | archived 15 stale dirs (142 renames, history kept) + repointed 22 links; docs-lint Errors:0. Kept epic-assessment/+examples/ (active) → 13 top dirs not 11 |
| H4 | T3→T1 | — | done | 2026-06-18 | opus-4.8 | f6402d1f, 0730b48a | **ALL 8 pillar map cards delivered.** `docs/reference/cards/{data,web,cache,jobs,vector,ai-data,mcp,auth}.md` — one-screen maps (what-it-does · the one canonical pattern · ≤5 attributes · the escape hatch · the sample). `data.md` (`f6402d1f`) = the architect-approved frontier that set the format; the other 7 (`0730b48a`) authored as T2 template work — **web by hand; the 6 (cache/jobs/vector/ai-data/mcp/auth) via a guarded Ultracode research→author→adversarial-review workflow (12 agents).** Every type/attribute/method grep-confirmed in CURRENT code; dead/experimental surface deliberately omitted (e.g. vector's VectorWorkflow/IVectorFilterTranslator). Orchestrator was the FINAL reviewer (read all 7, re-grepped identifiers, ran the authoritative docs-lint). Boundary held: jobs + mcp sourced read-only — ZERO `src/Koan.Jobs`/`src/Koan.Mcp` edits. Side-fix: extended `docs-lint.ps1`'s domain taxonomy with the real `jobs`+`mcp` pillars it was missing (honest fix vs mislabeling). docs-lint 0 err across all 8. |
| H5 | T2 | — | done | 2026-06-14 | opus-4.8 | 6b3894ec | glossary (19 terms, each type-anchored); caught 'set'→'partition' drift + BootReport-not-a-type; fixed pre-existing index.md broken link |
| H6 | T2 | H2 | done | 2026-06-18 | opus-4.8 | b5c41b13 | Normalized non-canonical synonyms presented AS the canonical entity verb (principles.md §5: Save/Remove/Query) across 7 docs files (21 line-replacements): `EntityType.Where(predicate)` top-level entity query → `.Query(...)` (authentication-setup ×2, performance ×5, s5-recs ×6, data/index ×1); `entity.Delete()`→`.Remove()` + `Entity<T>.Upsert/Delete` prose→`Save/Remove` (_canonical-samples, framework-utilities, data/index, data/cache). **Conservative + judgment-scoped:** LEFT all LINQ-on-collections (`allUsers.Where`, `.Take`, `.Select`), real APIs (UpsertMany, UpsertAsync, IBatchSet.Delete, repository.Delete), HTTP-verb endpoint descriptions, query-builder sub-clauses (`q.Where` inside `Query()`), and the streaming Pipeline DSL. The 4 skills + 2 sample READMEs already used canon (0 changes). **Ran as a guarded per-file normalize→adversarial-review workflow (29 files); ~19 review agents hit API rate-limiting → orchestrator did the over-reach review DIRECTLY and reverted the 1 over-reach (canon Pipeline DSL `Where`→`Query`: `Query` materializes a list, can't stream-`.Pipeline()`).** Verified: docs-lint 0 err; snippet-lint 4/4 marked blocks compile. |
| H7 | T2 | A2 | done | 2026-06-14 | opus-4.8 | f28792f9 | /llms.txt (111 lines), verbatim snippets from overview.md; README pointer; ran parallel w/ A2+H5 |
| H8 | T2 | H2 | done | 2026-06-18 | opus-4.8 (+ 3-agent panel) | 3317a31b | **koan-jobs skill authored** (modeled on koan-caching): `IKoanJob<T>` + static `Execute`, the `.Job`/`.Jobs` accessors, the ledger-is-the-queue capability ladder, the 6 attributes, §17 write-safety (mutate-the-given-entity / FIFO-per-work-item), `JobContext` steering verbs, §19 window-don't-multiply conveyor, anti-patterns. Registered in CLAUDE.md's pattern-recognition table + skills README (Tier 2 row + automatic-trigger note). **Sub-task 3 premise PARTIALLY STALE** — re-derived empirically: every package id in the skills is ALREADY correct (`Koan.Data.Connector.{Json,Postgres,Mongo}` resolve to real projects under `src/Connectors/Data/`; the card's `Koan.AI.Ollama→Connector.Ollama` rename is real but appears in NO skill — ai-integration's `"Type":"Ollama"` is a config value). Only genuine staleness = **3 `0.6.3`/date pins** (bootstrap `<PackageReference>` ×2 + README version footer) → fixed (pins dropped; footer → v0.17.x NBGV-versioned). **Verified TWICE**: every identifier grep-confirmed at `src/Koan.Jobs` file:line by the orchestrator, then independently re-verified by a 3-agent adversarial panel (types/attrs · accessors/verbs/options · structure/fidelity — ALL clean, 0 problems, the 4 See-also link targets confirmed on disk). docs-lint 0 err; 0 `<!-- validate -->` markers added (snippet-lint scope unchanged, satisfied by construction). **Boundary held**: `src/Koan.Jobs` read-only, ZERO edits in Jobs trees. |
| H9 | T2 | F1 | done | 2026-06-15 | opus-4.8 | 00f265dd | **Item 1 STALE — already shipped in F1** (MODULES-FAILED block + `ModuleFailure` channel render at `KoanConsoleBlocks:65-76`, pinned by `ModulesFailedRenderSpec`; left unchanged). **Item 2:** moved the raw `ASSEMBLIES\|loaded=` line + `assembly-scan` JSON behind `KOAN_VERBOSE_ASSEMBLIES` (`EmitAssemblySummary` early-returns when off; the per-assembly DETAIL listing was already gated). **Item 3:** fixed the Health-line race — the boot report renders BEFORE `StartupProbeService`'s background task seeds the snapshot, so empty→`probes pending (registered=N)` / `probes=0 (none registered)` instead of a fabricated `overall=Unknown`/`overall=Healthy` (AppRuntime threads `IHealthRegistry.All.Count`). +2 specs (`BootHealthLineRenderSpec`, `AssemblySummaryVerboseGateSpec`). Bootstrap 29/29; full Koan.sln build 0 err; S1.Web verified (clean default stdout; diagnostics restored under the env var). |
| H10 | T3 | H2, H4 | in-progress (Ph0-4+6 done; Ph5 deferred) | 2026-06-18 | opus-4.8 | cdf890d2 + 13 (32369e56→5448f6a1) | **DX/AX skill-set overhaul PROGRAM** — spawned by the 13-skill source-accuracy audit (all 13 graded `refresh`; systemic ghost-API drift + the rot also lives in the feeding guides — see Divergence log). Card: [06/H10-skills-overhaul.md](06/H10-skills-overhaul.md). Realign to **card-anchored skills** (the H4 cards are the API truth; skills carry trigger + canonical-pattern + anti-patterns + see-also and LEAN on the card, not re-state it) + a permanent `<!-- validate -->` compile-gate over `.claude/skills/**` + a `skills-lint` (dir==name, frontmatter, link-resolve). Roster **14→19**: 8 pillar skills 1:1 with cards + 4 foundation (quickstart/entity-first/bootstrap/debugging) + 3 lean data facets (data-modeling[absorbs relationships]/multi-provider/performance) + 5 new-pillar (storage/messaging/media/orchestration/observability, each with a **new H4-style card**); retire relationships→data-modeling & vector-migration→koan-vector; rename api-building→koan-web, ai-integration→koan-ai; +koan-auth (card existed, no skill). **Architect decisions (AskUserQuestion 2026-06-18):** card-aligned+lean facets · **full AI surface now** (accept ARCH-0089 churn) · **expand coverage** (new cards+skills for uncovered pillars). **6 phases** (0 arch+gate → 1 guide-truth → 2 new cards → 3 overhaul skills → 4 new skills → 5 mcp[DEFERRED, concurrent-Mcp boundary] → 6 AX finish). Per-unit acceptance = the koan-jobs standard (grep-verified + compile-gated + adversarial panel + orchestrator-final). jobs ✅ already meets the bar (the reference). **EXECUTION (2026-06-18, 13 commits `32369e56`→`5448f6a1`): Phases 0–4 + 6 DONE.** Ph0 gate (`32369e56`: DX-0048 ADR + dir==name renames [loader-confirmed bug] + `skills-lint` + skill-validate-gate + green-ratchet Leg D). Ph1 6 guides, 33 ghost fixes (`339cc7e0`,`f2da0f64`). Ph2 5 new cards (`d34e54c5`). Ph3 11 existing skills overhauled in 3 batches (`33f34988`,`85a19946`,`bffb1471`). Ph4 6 new skills (`51d6b7fa`). Ph6 retire relationships + flip skills-lint `-Strict` + CLAUDE.md router (`5448f6a1`). **Final roster = 19 skills + 13 cards; EVERY canonical pattern CI-compiled (validate sweep 22/22; skills-lint -Strict 0/0).** The compile-gate caught 3 real defects mid-flight: quickstart CS8803, performance `WithSort(lambda)` misuse, and a genuine **FRAMEWORK bug** — `CacheableAttribute`'s `init`-only TTL setters make its OWN documented `[Cacheable(…, AllowStaleForSeconds=N)]` uncompilable (CS0617) → filed [[X-cacheable-attribute-init]]. **Ph5 `koan-mcp` DEFERRED** (concurrent-Mcp boundary; mcp.md card ready). |
| S3 | T3 | — | done | 2026-06-17 | opus-4.8 | 481c500b | **Card premise STALE → reframed as AI pillar DISSOLUTION (ADR ARCH-0089), not an in-Koan fold.** Two-round deep eval (full surface map + cross-repo migration dossier reading Koan/Agyo/zen-garden) found the "vertical" is real+tested now (S18.Prism + AI test suites; Review isn't even in the card), so "19→~8" is reached by MIGRATION not cutting (AGYO-0001: "not core, yet valuable → sibling repo"). Decision: Koan keeps **8** entity-AI core projects (Contracts/Contracts.Shared/AI/Prompt/Data.AI/Web/Ollama/LMStudio); agentic/RAG/quality (Orchestration+Agents→**fold Agyo.Rag**, Eval, Review)→**Agyo**; ML substrate (Compute **cut-in-favor-of-ZG**, Models **port** w/ ModelEntry ORM decouple, HuggingFace)→**Zen Garden**; archive ZenGarden-connector + Training (vaporware); S18.Prism→Agyo. **Zero core→vertical reverse-deps = clean extract.** Deliverable = the card's ADR + sequenced transition-safe plan; execution is a follow-on 3-repo program (C#→C# Agyo lift + **C#→Rust** ZG port) → card **X-ai-dissolution-migration**. |
| S4 | T3 | E5 | done | 2026-06-17 | opus-4.8 | 17f90b77 | auth + data surface trim **DONE** (ADR **ARCH-0090**, 5 decisions via a 4-reader empirical surface-map; 2 of 4 card premises were stale). Implementations landed: **auth-connector shrink** (`b6fb0c19` — Discord/Google/Microsoft bodies → `AuthProviderDefaults` helper, packages kept), **vector-workflow cut** (`55135939` — 8 files deleted, never-activated speculative subsystem). **Decisions w/ no code change:** Auth.Roles keep-separate (preserve Auth's data-agnostic layering); Vector/VectorData keep (premise STALE — facade vs engine, frozen reflection targets). **Remaining execution:** Data.Direct fold-hard (move ~513 LOC impl → Core, register by default, delete project) — feasibility-confirmed (Core has all deps; register at `ServiceCollectionExtensions.cs:55`), deferred to a focused pass → card **X-data-direct-fold**. |

## 07 — Strategic capability builds (maturity ladder)

Preamble for these cards: the `[SESSION-PREAMBLE]` block in `../07-strategic-prompt-stash.md`.
**Whole ladder gated on 06 B1 + F1 + A2 landing first.**

| ID | Tier | Deps | Status | Date | Agent/model | Commits | Notes |
|---|---|---|---|---|---|---|---|
| P1.1 | T3 | 06 B1 | done | 2026-06-20 | opus-4.8 | ffbaf44b | **Composition lockfile.** Build emits checked-in `koan.lock.json` (app+modules @ **major.minor**) via `build/Sylin.Koan.Core.targets` (NuGet auto-imports `build/<PackageId>.targets`); host writes resolved twin `obj/koan.lock.resolved.json` (+elections/configKeys/entities) + boot line `composition: <n> modules · lockfile ok\|DRIFT`. Pillars enrich via discovered `IKoanCompositionContributor` (Data ships `data:default`). Gates: `compare-koan-lock.ps1` = green-ratchet leg E + boot line. **Deltas:** build file = app+modules only (caps/elections runtime, ARCH-0084); versions major.minor (ARCH-0085 tier) to kill NBGV-height churn; caps omitted/entities best-effort; elections via seam (Core can't ref Data). 13 unit + 1 ARCH-0079 integration; dogfood S0; docs `composition-lockfile.md`+README+overview+CLAUDE.md. Fixed pre-existing P3.2 leftover (CustomTools.Tests Invoke sig). **NOTE pre-existing NU1903** (SQLitePCLRaw 2.1.11) blocks audit-on restore of Sqlite projects — separate concern. |
| P1.2 | T3 | 06 F1, P1.1 | done | 2026-06-19 | opus-4.8 | (see AN-table P1.2) | **Done — duplicate of the AN-table P1.2 row** (MCP introspection resources / O6): `resources/list`+`resources/read` on both transports + `IMcpResourceProvider` seam + `koan://entities` catalog, per-grant projected. Tracked there. |
| P2.1 | T3 | 06 B1 (· H1) | done | 2026-06-20 | opus-4.8 | d4eaebcb | **Conformance kit (full break-and-rebuild — user chose it; the card's 1-project premise was STALE: ARCH-0091 had split the harness into 3 + a fenced xUnit-v2 consumer).** `EntityConformanceSpecs<T>` (one `NewValid()` method) → trait-gated batteries via real `AddKoan()` (ARCH-0079): RoundTrip · QueryPushdown-vs-`InMemoryFilterEvaluator`-oracle (REUSED from packable Koan.Data.Abstractions) · Paging · PartitionIsolation · Cacheable\|Embedding (attr-gated); container entities self-skip. **Moved** Koan.Testing.Hosting + .Containers → `src/` packable (`Sylin.Koan.Testing.Hosting`/`.Containers`) + **new `src/Koan.Testing`** (kit); **retired the shim** + repointed fenced `Koan.Jobs.TestKit` → Hosting; repointed ~18 suites + sln. **Dogfood both green no-Docker:** S1.Web User (sqlite) + S5.Recs UserDoc (forced inmemory). Meta: 10 pass/3 skip incl. a teeth proof. **Fixed a framework bug** the S5.Recs Media dogfood exposed: Koan.Data.AI embedding/media-analysis hook registration missed `BindingFlags.FlattenHierarchy` → every 1-arg-`Entity<T>` `[Embedding]`/`[MediaAnalysis]` entity threw `KoanBootException` at boot (regression spec added). **Deltas:** dropped `new()` constraint (NewValid → supports `required` members); kit lean (Testcontainers ships separately); docs domain `engineering`. Docs: testing-your-app.md + README + H1 note. Full sln build green (`-p:NuGetAudit=false` for the pre-existing NU1903). |
| P3.1 | T3 | P1.2 | done | 2026-06-19 | opus-4.8 | `4b8bae17`→`d41f0221` | **Done outside the card process as ADR SEC-0005 (ARCH-0092 Phase 4).** `AgentGrant` (server-side revocable, materialized on the token-denied path, fresh-per-request revocation) + `AgentAction`/`[Audit]` (7 mutation sites, reads never audited) + the Door/`[Door]` (discloses a denied verb's gate `needs`; role-gated stays a silent Wall). All-suites green; 3 mutations w/ teeth. |
| P3.2 | T3 | P3.1 | **done** | 2026-06-20 | opus-4.8 | `7ef0e90c` · `1fb723bf` | agent-operable runtime — ops verbs as governed tools. New **`Koan.Mcp.Operations`** pkg (sibling of Explorer; refs Jobs+Cache, keeps Koan.Mcp lean): `koan.jobs.{trigger,cancel,status}` + `koan.cache.{flush,flushAll}` as `[McpTool]` verbs on `Toolset` subclasses (reuses ARCH-0092 §H — no parallel registration). Governance reuses SEC-0005 directly: `@ops:{jobs,cache}` AgentGrant gate (EXACT-resource match — a `*` grant does NOT confer ops) → fail-loud naming the grant; `confirm:true` dry-run for destructive (cancel/flushAll); `AgentAction` audit per mutation. **2 GENERAL core touchpoints**: (1) thread `ClaimsPrincipal` into custom-tool invocation as a new injectable param source (any custom verb can request the caller); (2) `[McpOperationalToolset(key)]` + generic `McpServerOptions.Operations` dict + a GLOBAL config-gate at the **handler** (`ListTools`+call chokepoint, both STDIO+dispatcher) — disabled toolset absent from `tools/list` (grant is a separate in-verb gate, so enabled-but-ungranted stays discoverable). Boot report `mcp.ops: jobs,cache · grants required · audited`. **Tests**: new `Koan.Mcp.Operations.IntegrationTests` (real AddKoan host via MCP TestKit) 5/5 — off⇒absent · no-grant⇒fail-loud · grant⇒runs-vs-real-ledger+audits · flushAll-no-confirm⇒dry-run; **2 mutations w/ teeth** (grant gate, config gate); Conformance 72/72 holds. **Card deltas**: string-keyed verbs wrap IJobCoordinator/ICacheClient (anchors named the generic accessors); flushAll enumerates ICachePolicyRegistry tags (no global cache-clear); STDIO anonymous = no ops. **DOGFOOD (S14.AdapterBench) SKIPPED** — S14 doesn't use MCP; bolting ops onto an adapter benchmark = a poor/unmeaningful demonstration (the ARCH-0079 suite IS the real dogfood: real host+ledger+grant+audit); a dedicated MCP-ops showcase is a future item. **DOCS**: `mcp-governed-access.md` doesn't exist → extended `mcp-agent-native-howto.md` instead (Rung 10 ops section + ladder, dedup over a parallel doc) + koan-mcp skill note. docs-lint 0err, skills-lint 0/0. |
| P4.1 | T3 | Facet 3 (HARD GATE), P2.1 | pending | | | | multi-tenancy primitive |
| P4.2 | T3 | 06 S3, P2.1 | pending | | | | app-level AI evals |
| P5.1 | T3 | 06 B2 | **reframed** | 2026-06-21 | opus-4.8 | (design) | **Card premise REFRAMED in design review (not yet built).** The original "strip the app to fit an AOT binary" fused two independent axes: **sovereignty** (whose hardware / does data leave it — a self-hosted Docker host is already sovereign + full-featured; P5.2 proved Koan self-orchestrates Postgres/Weaviate/Ollama on one box) vs **footprint** (cloud→k8s→one Docker host→one native binary). "Scales-down" is the footprint axis; the only net-new rung is the **floor** (single exe, NO container runtime — appliance/edge/air-gapped). KEY INSIGHT: "single binary" must NOT mean fewer capabilities — it means **every capability satisfied by an in-PROCESS resource** instead of a server. So P5.1 becomes "**complete the in-proc tier of the capability ladder**" — build adapters over embedded resources (the seams already exist: ARCH-0084 tokens, the vector-repo + AI-embedding-generator interfaces). Two gaps: **embedded vector** (all current connectors are servers) + **in-proc embeddings** (only Ollama). First step = the deep-research card below. |
| P5.1-research | T3 | — | **done** | 2026-06-20 | opus-4.8 (24-agent Ultracode) | _(this commit)_ | **Decision brief delivered**: [`docs/assessment/evidence/inproc-adapter-survey.md`](../assessment/evidence/inproc-adapter-survey.md). 24-agent workflow (parallel repo grounding [seams/tier/AOT] + ecosystem survey [vector/embeddings/data/messaging] → adversarial guilty-until-verified pass on every AOT/license/maintenance/RID claim → draft→critic→polish). **Verified gaps**: vector + embeddings (+ a 3rd CORRECTION — messaging is a GAP, and the boot report's `InMemoryProvider.Available=true` is a confirmed self-report bug: only `RabbitMqProvider` implements `IMessagingProvider`, no backing class). **AOT verdict**: GardenCoop's actual floor (sqlite+Entity+Newtonsoft+Web) is PROVEN AOT-publishable (win-x64); a managed-only floor has NO hard blocker (Newtonsoft warns-not-blocks, rooted via `NativeAotRoots.xml`); native deps (ONNX-RT/sqlite-vec) AOT-**UNVERIFIED**. **Postures resolved**: (1) two-rung ladder — pure-managed floor (default) + native-allowed full-single-binary (opt-in, self-elects by Reference=Intent); (2) sequence bruteforce→onnx→sqlitevec→channels. **Spawned 7 impl cards** (see "In-proc adapter cards" subsection). Adversarial pass bit: USearch ships NO linux-arm64 (unpacked the real nupkg), sqlite-vec bundles a stale `vec0` w/ a known DELETE bug, ORT "AOT-broken" cites were iOS-Mono not desktop-ILC. |
| P5.2 | T3 | P1.2, P3.1, 06 H1 | done | 2026-06-20 | opus-4.8 | 014e158d | **Wedge demo — real 7-beat agent transcript** (`docs/case-studies/agent-wedge-demo/`), captured LIVE vs framework source (.NET 10 + Docker + Ollama). Built "Recs" anime app one entity → agent-operable MCP server: (1-2) EntityController REST (GUID v7) + boot `Composition` line + checked-in koan.lock.json (P1.1); (3) +Postgres connector + 1 config line → **Koan self-orchestrated a postgres container**, resolved-twin election `data:default→postgres` + verified row prove the zero-code swap; (4) `[Cacheable]` L1; (5) `IKoanJob<T>` durable import (RoutingJobLedger upgraded by Postgres); (6) `[Embedding]`+Ollama(all-minilm)+Weaviate → semantic search (Spy×Family/Vinland Saga ranked #1); (7) `[McpEntity]`+Streamable-HTTP → agent initialize → read `koan://entities` → mutate `anime.upsert` (same entity REST serves). Honest notes kept: Koan fail-loud on a Weaviate port conflict (fixed w/ explicit endpoint); `[Embedding] Async=true` routes through the vector-writing worker; open write gate is ungoverned (SEC-0005 grants/audit govern restricted writes). Prereqs verified (H1/P1.2/P3.1). No framework code changed; docs-lint 0 err. README linked under three-beats. |

### 07 — In-proc adapter cards (spawned by P5.1-research)

Follow-on impl cards from the [in-proc adapter survey](../assessment/evidence/inproc-adapter-survey.md) decision brief (P5.1-research). **The brief is the canonical spec** — §5 (ranked adapter set + adapter-shape sketches against the real seam), §6 (two-rung floor posture), §7 (build sequence), §8 (these cards), §9 (risks). Build order: bruteforce → onnx → sqlitevec → channels (aot-substrate underpins; S2 proves). Standing constraints carry: ARCH-0079 integration tests + mutation-check, no stopgaps, capability tokens declared honestly (fail-loud on what's not real).

| ID | Tier | Deps | Status | Date | Agent/model | Commits | Notes |
|---|---|---|---|---|---|---|---|
| X-inproc-vector-bruteforce | T3 | P5.1-research | **DONE** | 2026-06-21 | opus-4.8 | 34d82f2d | ✅ Shipped as `Koan.Data.Vector.Connector.InMemory` (promoted the proven in-memory reference into a Reference=Intent connector over `TensorPrimitives`; **deduped** the test oracle onto it). Matrix 29/29 + ARCH-0079 bootstrap 1/1 + mutation-verified. **Do first — the correctness oracle** every other vector adapter validates against. `IVectorAdapterFactory` + `IVectorSearchRepository<T,K>` over `TensorPrimitives.CosineSimilarity` (per-partition `ConcurrentDictionary<TKey,float[]>`, optional sqlite/json persist via `Entity<T>`); honestly declares `Knn`/`BulkUpsert`/`BulkDelete`/`ScoreNormalization`/`DynamicCollections` + `Filters(FilterSupport.Uniform)` post-scan, fail-loud on `Hybrid`/`NativeContinuation`/`StreamingResults`/`AtomicBatch`/`MultiVectorPerEntity`. Pure-managed, AOT-trivial (CONFIRMED), all floor RIDs; O(N)/query honest ≤~10⁵ not millions. Brief §5(1)/§8. |
| X-inproc-embeddings-onnx | T3 | X-inproc-vector-bruteforce | **DONE** | 2026-06-21 | opus-4.8 | 0760bc86 | ✅ Shipped as `Koan.AI.Connector.Onnx` (FastBertTokenizer + ONNX Runtime, mean-pool+L2). **Real inference verified** vs the committed all-MiniLM model (cats~felines closer than cats~quarterly-report, 384-dim). Side-loadable model; relative-path resolve added. **2 follow-up gaps found** (router source-registration; `[Embedding]` EmbeddingState DDL) — see X-onnx-ai-source-registration / X-embedding-state-ddl. `IEmbedAdapter.Embed(AiEmbeddingsRequest)` via `IAiAdapterContributor`→`IAiAdapterRegistry.Add` (no factory/`[ProviderPriority]` — mirrors `OllamaAdapterContributor`) over `Microsoft.ML.OnnxRuntime` + all-MiniLM-L6-v2 (Apache-2.0, 384-dim); **lead with `FastBertTokenizer`** (AOT-clean), NOT `Microsoft.ML.Tokenizers.BertTokenizer` (type inventory unconfirmed). Declares `AiCapability.Embed` only; report `Model`+`Dimension` truthfully for `VectorModelGuard`. Closes the embed→store loop via `EmbeddingWorker`. **First native dep — AOT UNVERIFIED (leaning broken); spike before asserting.** Brief §5(2)/§8/§9. |
| X-inproc-vector-sqlitevec | T3 | X-inproc-vector-bruteforce | **DONE** | 2026-06-21 | opus-4.8 | 3f5df9c4 | ✅ Shipped as `Koan.Data.Vector.Connector.SqliteVec` over **vec0 v0.1.9** (the DELETE-fixed release) for win-x64/linux-x64/linux-arm64, **embedded as assembly resources + self-extracted at load** (single-file/AOT-friendly, no NuGet RID plumbing) + `sqlite3_vec_init` entry point. Caps Knn/Bulk/AtomicBatch/ScoreNorm/DynamicCollections; CanHandle("sqlite") auto-pairs. ARCH-0079 native round-trip 1/1 + mutation-verified. sqlite-vec over the SAME `SqliteConnection` as the shipped data adapter (one `.db` = rows + `vec_*` virtual table); adds `AtomicBatch`(real SQL txn) + `MultiVectorPerEntity`; `Filters`=equality-only; NOT `Hybrid`. **Two corrections MUST land:** (a) ship a newer native `vec0` — the bundled May-2025 build predates the v0.1.8/v0.1.9 DELETE fixes and the adapter DELETEs; (b) self-resolve `runtimes/{rid}/native/vec0` before `LoadExtension` (SDK `PackageReference` copies nothing, `LoadExtension` bypasses RID resolution). AOT `LoadExtension` confidence UNCERTAIN — gate on spike. Recall validated vs the bruteforce oracle. Brief §5(3)/§8/§9. |
| X-inproc-messaging-channels | T3 | — | **DONE** | 2026-06-21 | opus-4.8 | ad9ab9e6 | ✅ Shipped as `Koan.Messaging.Connector.InMemory` (Channels bus, fan-out, pause-buffers). **Fixed the confirmed boot-report lie** (Core's `InMemoryProvider.Available=true` with no backing class). ARCH-0079 2/2 + mutation-verified. `IMessagingProvider` at Priority=10 over `System.Threading.Channels` (+ pump task; honor the `IMessageConsumer` Pause/Resume/IsActive contract); non-durable by design (durable leans on the Jobs sqlite ledger). Fills the reserved-but-empty Priority=10 slot AND **fixes a confirmed self-report bug** — `Koan.Messaging.Core/Initialization/KoanAutoRegistrar.cs:33-37` hardcodes `InMemoryProvider.Available=true` while the only `IMessagingProvider` impl is `RabbitMqProvider` (re-verified at source). Substrate AOT-clean; the shared-pipeline reflection liability is owned by X-aot-substrate. Brief §5(4)/§8. |
| X-aot-substrate | T3 | — | **DONE (single-file) — NativeAOT remains (→ S2)** | 2026-06-21 | opus-4.8 | 8531bef6 | ✅ **The published single-file exe now RUNS the full embedded stack.** Root cause: a Reference=Intent connector (`<ProjectReference>`, never symbol-used) is dropped from the app's compiled metadata (the boot reference-fixpoint can't see it) AND leaves no loose `Koan.*.dll` when bundled → `No IDataAdapterFactory instances registered`. The `.deps.json` names it but `DependencyContext.Default` is **null under single-file** (empty `Assembly.Location` — proven with a probe), so that's not a path. FIX: the Koan build targets embed a **`koan.modules.manifest`** resource in the app assembly (same `@(ReferencePath)` Koan-filter as the lockfile) — embedded resources survive bundling — and `AppBootstrapper.LoadIntentModulesFromManifest` reads it + `Assembly.Load`s each by name (no-op for loaded). The loose-DLL scan stays as the fallback for ProjectReference consumers without the targets (test projects) — **load-bearing, kept, NOT removed** (the honest outcome: an additive meaningful prong, not a removal). Content-path (b) does NOT arise without `IncludeAllContentForSelfExtract` (`AppContext.BaseDirectory` = exe dir = where `models/` is). Live-verified: **21 modules** discovered (was 13), ONNX embeds from the bundled model, search ranks correctly, REST serves. Guard: `EmbeddedModuleManifestSpec` (read/parse/load contract) + Bootstrap 36/36. **NativeAOT (no `Assembly.Load`, no `.deps.json`) is a DIFFERENT mechanism (generated `[ModuleInitializer]` + trim-roots) — remains the S2 spike.** _(orig:)_ **The GardenCoop-embedded single-file publish CONFIRMED this is real even for single-file JIT (not just AOT):** the reflective boot scan finds no loose `Koan.*.dll` when bundled → adapter resolution fails (worked around by `IncludeAllContentForSelfExtract`); and content-path resolution (`AppContext.BaseDirectory`) diverges from the extraction dir. Underpins BOTH floor rungs. Characterize/harden the most AOT-hostile seam on the floor — the reflective boot scan (`AppBootstrapper.cs:83`: `AppDomain.GetAssemblies` + referenced-assembly fixpoint + `Koan.*.dll` dir scan; tolerated under AOT via force-root + lenient-skip) — plus a curated `NativeAotRoots.xml` baseline beyond GardenCoop and a documented "what to root when you add a module" rule. **Also owns the messaging-pipeline AOT liability** (`MakeGenericMethod` + Expression trees + reflection-STJ in `MessagingExtensions`/`IMessageBuffer`) — the §9 hazard with no other owner. SQLite-leg notes: `--self-contained true` + trim roots are hard publish requirements; deprecated `SQLitePCLRaw.lib.e_sqlite3` ID → `SourceGear.sqlite3` (cosmetic). Brief §3/§8/§9. |
| S2.Sovereign-proof | T3 | X-inproc-vector-bruteforce, X-inproc-embeddings-onnx, X-inproc-vector-sqlitevec | **DONE (win-x64 + linux-x64; arm64 deferred)** | 2026-06-21 | opus-4.8 | a9b98a5b·fdaea45e·71c09d74 | ✅ **NativeAOT PROVEN on win-x64 AND linux-x64 (ARCH-0093).** The single native binary (42 MB win exe / 40 MB ELF linux, both stripped) runs the WHOLE embedded stack end-to-end — ONNX embed → sqlite-vec k-NN → SQLite/Newtonsoft → MVC JSON, semantic search correct with **byte-identical scores on both RIDs** (`'sweet red fruit'`→Heirloom Tomatoes 0.5237), no container/servers. linux-x64 verified on a **real Debian 13 box** (provisioned clang+SDK+icu, `git archive HEAD`→scp→`dotnet publish -r linux-x64 -p:KoanAot=true`→run→`curl` HTTP 200) — proves the framework changes are RID/OS-agnostic (MSVC link vs clang, different libc/loader). How-to: `docs/guides/nativeaot-howto.md`. 4 AOT blockers found+fixed at framework level: (1) discovery via generated `koan.trimroots.xml` (`preserve=all` per Koan module, same `@(ReferencePath)` filter as lockfile+manifest) — ILC keeps Reference=Intent connectors + their `[ModuleInitializer]`s + reflective registrar `.ctor`s (else `MissingMethodException`); `[ModuleInitializer]`s DO fire under AOT for kept assemblies; (2) `PublishAot` behind `-p:KoanAot=true` local gate (a global prop trips netstandard2.0 generators → NETSDK1207) + Windows `-p:IlcUseEnvironmentalTools=true` under vcvars64 (findvcvarsall/vswhere corrupts linker path); (3) **Dapper emits IL → hand-rolled `Koan.Data.Relational.Ado` (SqlParameters+AdoCommands, AOT-clean) for SQLite + thin `Koan.Data.Relational.Dapper` shim for non-AOT adapters** (the user's call); (4) `System.Text.Json` reflection-disabled under AOT → sqlite-vec JSON → Newtonsoft (canon); `(dynamic)ddl` DLR → direct call. Verified: SQLite 3/3 (full filter corpus)+Data.Core 158/158+Jobs-SQLite 76/76+Bootstrap 38/38+full-sln green+AOT live e2e. **Deferred:** linux-arm64 (RID-agnostic framework; only the cross-ILC toolchain remains) → `X-aot-linux-arm64`; template-less `[Embedding]` STJ (custom resolver) + EmbeddingMigrator STJ → `X-aot-embedding-stj`. _(prior:)_ **Sample `samples/guides/g1c2.GardenCoopEmbedded` built + LIVE-verified** (`dotnet run`): one process, no container/servers — SQLite data + sqlite-vec vectors (shared `.db`) + in-process ONNX embeddings + Channels bus; semantic search ranks correctly ('ripe red tomato'→Heirloom Tomatoes 0.73, 'sweet berries'→Wild Blueberries 0.63). Self-contained single-file publish → ONE `GardenCoopEmbedded.exe` + `models/` + native libs beside it (model copy fixed via `CopyToPublishDirectory`, `cf0bff3d`). **The published single-file exe now RUNS the full stack (win-x64)** — fixed by **X-aot-substrate** (`8531bef6`): the embedded `koan.modules.manifest` makes boot discover the Reference=Intent connectors that single-file bundling hides; 21 modules discovered, ONNX embeds from the bundled model, search ranks correctly, REST serves. Content-path (b) didn't arise without `IncludeAllContentForSelfExtract` (`AppContext.BaseDirectory` = exe dir = where `models/` is). **REMAINING = the NativeAOT measured spike (target linux-arm64):** a DIFFERENT mechanism (no `Assembly.Load`/`.deps.json` under AOT — needs generated `[ModuleInitializer]`s + trim-roots) AND native-dep (ONNX-RT/sqlite-vec) AOT verification. **The embedded vision itself is PROVEN via `dotnet run`** (loose assemblies): semantic search works end-to-end in one process. The empirical proof of the net-new floor rung AND the spike that converts Rung A from "inferred AOT-clean" to "measured." A sample that AOT-publishes **Rung B** (sqlite + sqlite-vec + ONNX embeddings + Channels messaging) and round-trips embed→store→search in a single binary, no container. **Target RID = `linux-arm64`** (the edge/appliance story the rung is justified by — GardenCoop only proved win-x64, which would NOT validate the sovereign claim). Brief §6/§8/§9. |
| X-inproc-data-duckdb | T3 | P5.1-research | **deferred (re-scoped)** | 2026-06-21 | opus-4.8 | — | **Empirical re-scope:** a DuckDB *data* adapter is NOT thin — riding `Koan.Data.Relational` still means a full repository implementing ~10 interfaces + a `DuckDbDialect` (`ILinqSqlDialect`) + schema orchestration + connection handling (the `SqliteRepository` is the template — hundreds of LOC). And the survey rated its CRUD value ≈ sqlite (its real edge is analytics, which a basic adapter doesn't even showcase). Recommended as a **focused follow-up** rather than rushed; not in the sample (decided). `DuckDB.NET.Data` is ADO.NET so the mirror is feasible, just heavy. |
| X-onnx-ai-source-registration | T3 | X-inproc-embeddings-onnx | **DONE** | 2026-06-21 | opus-4.8 | 65180052 | ✅ `OnnxAdapterContributor` now publishes the adapter as an `AiSourceDefinition` (Provider = adapter.Id, advertises `Embedding`) so `Client.Embed` + the `[Embedding]` worker route to it. **Architect refinement (user):** aligned ONNX identity with the Ollama convention — provider-level `Id="onnx"` (was `onnx:{model}`), the embedded model is the source's *default model* (a usage-time concern), not baked into identity. ARCH-0079: `OnnxEmbeddingsPillarBootstrapSpec` asserts source registration + `Client.Embed` end-to-end (mutation-verified); sample reverted `Embedder`→`Client.Embed`, live-verified. _(orig:)_ The ONNX adapter registers in `IAiAdapterRegistry` (so direct `IEmbedAdapter.Embed` works + is tested) but NOT as an AI **source** — so `Koan.AI.Client.Embed` / the `AiCategoryRouter` (`ResolveSource`) can't route to it, and the `[Embedding]` worker can't use it. Make the `OnnxAdapterContributor` register an `AiSourceDefinition` (mirror `OllamaAdapterContributor`'s `IAiSourceRegistry.RegisterSource`) so the local embedder is first-class through the facade + worker. **Files:** `src/Connectors/AI/Onnx/Initialization/OnnxAdapterContributor.cs`; mirror `src/Connectors/AI/Ollama/Initialization/OllamaAdapterContributor.cs` (`AiSourceDefinition` build ~L184); router `src/Koan.AI/Pipeline/AiCategoryRouter.cs:106` (`ResolveSource`). Then the sample reverts `Embedder`→`Client.Embed`. |
| X-embedding-state-ddl | T3 | — | **DONE (→ DATA-0104 canon)** | 2026-06-21 | opus-4.8 | 379433f0 + 78f48cf1 | ✅ Reframed from a point-fix into framework canon via a user architecture co-design + an 11-target repo-wide audit (read→adversarial-verify→synthesize). **ADR DATA-0104:** one recursive rule at the single chokepoint `StorageNameResolver.Resolve` — `Resolve(G<a1..aN>) = join("--", ai=>Resolve(ai)) + "-" + ApplyCase(SimpleName(G))` (`-`=spine, `--`=branch, entity=leftmost anchor, arity-stripped, cased per adapter). Fixes 9 adapters at once (audit found Postgres/Redis/Json **silently collapsed every `EmbeddingState<T>` into one store** → cross-entity corruption; Couchbase create-failure; SQLite/SqlServer/Mongo version-fragile). Re-pointed 2 bypasses (Redis/Json `AppHost==null` fallback), closed Couchbase `MaxIdentifierBytes` gap. Guards: resolver grammar matrix (all 3 styles) + capability-honesty oracle + ARCH-0079 SQLite round-trip (mutation-verified). **`78f48cf1`** = the re-entrancy bug the naming fix UNBLOCKED: the sync `[Embedding]` AfterUpsert hook called `VectorData.SaveWithVector` (re-Saves entity → re-fires hook → StackOverflow); fixed to vector-only `VectorData.Save` + a mutation-verified regression spec (restoring SaveWithVector crashes the host with STATUS_STACK_OVERFLOW). **Ergonomic auto-flow now works** — `g1c2.GardenCoopEmbedded` Seed collapsed to `item.Save()`, live-verified (0 overflows, `Produce-EmbeddingState` table clean, search correct). _(orig:)_ `[Embedding(Async=true)]` queues through `EmbeddingWorker`, whose generic tracker `EmbeddingState<TEntity>` gets a mangled storage name (`EmbeddingState\`1[[Ns.Entity, …]]`) that the relational schema orchestrator / SQLite DDL path can't create (`CreateTableWithColumns` fails). Generic-entity storage-name resolution needs a clean name (e.g. `EmbeddingState_Entity`) for relational adapters. Blocks the ergonomic `[Embedding]` auto-flow on SQLite (works on schemaless stores). **Files:** `src/Koan.Data.AI/Workers/EmbeddingWorker.cs` (stores `EmbeddingState<T>`); fails at `src/Connectors/Data/Sqlite/SqliteRepository.cs:1230` (`CreateTableWithColumns`) / `src/Koan.Data.Relational/Orchestration/RelationalSchemaOrchestrator.cs:143`; fix = clean generic name in the `Koan.Data.Core` storage-name resolver. Repro: re-add `[Embedding(Async=true)]` to the sample's `Produce`. |
| X-generic-typeid-breaking | T3 | X-embedding-state-ddl | pending (**BREAKING — needs compat**) | | | | **Carved out of DATA-0104.** The same closed-generic `Type.FullName`/`Name` mangling lives in two **persisted/on-the-wire** identifier sites the storage-naming chokepoint does NOT reach: `JobRecord.WorkType` (`src/Koan.Jobs/JobTypeBinding.cs:204`, `WorkType = clr.FullName!` — the durable ledger ordering/coalesce key) and the messaging routing key (`src/Koan.Messaging.Core/MessagingExtensions.cs:52` `GetConcreteTypeName`, `t.FullName ?? t.Name` — in-flight wire). Changing their derivation **orphans existing ledger rows / breaks in-flight messages** → needs a migration / compat shim (dual-read old+new, or a version gate). Do NOT fold into a naming PR. |
| X-generic-typeid-ephemeral | T3 | — | **DONE (cache-key sites; raw-SQL sub-note remains)** | 2026-06-21 | opus-4.8 | cd700299 | ✅ All 5 cache-key sites now use a new shared `CacheKey.EntityTypeName(Type)` helper: non-generic → `Type.Name` (unchanged, no churn), closed generic → arity-stripped name **+ recursive type-arg tokens** (`Track<Todo>` vs `Track<Product>` no longer both collapse to `Track\`1` → silent cross-type cache collision). **Premise correction:** the card said "reuse the DATA-0104 `SimpleName`/strip-arity helper" — but strip-arity ALONE still collides (proven by mutation: strip-only fails the new `For_closed_generics_…_do_not_collide` guard); the **type args** are what disambiguate, so the helper appends them (it does NOT cross into `StorageNameResolver`, keeping the cache/data layer boundary). Sites: `CacheKey.For`, `EntityCacheExtensions.BuildEntityKey`/`Flush`/`FlushAll`-tag, `CachedRepository._entityName`. 4 new specs in `CacheKeyForSpec` + mutation-verified; Cache.Abstractions 59/59. **Remains (trivial edge):** `RewriteEntityToken` raw-SQL token rewrite still no-ops for a generic entity referenced by CLR name (`\b` vs backtick) — raw-SQL + generic-entity-by-name is a near-impossible combination, left as a note. |
| X-sqlite-fallback-create-generic | T3 | — | **DONE (both warnings — one DLR cause, mutation-proven)** | 2026-06-21 | opus-4.8 | fdaea45e | ✅ **Root-caused: BOTH fallback-create warnings were the ONE `(dynamic)ddl` DLR failure under NativeAOT, not two bugs.** Captured the AOT stack (instrument the catch → publish → run): `Value cannot be null (Parameter 'key')` (for `Produce`) is `Microsoft.CSharp.RuntimeBinder.ExpressionTreeCallRewriter.VisitSAVE` → `Dictionary.TryInsert` throwing on the binder's OWN internal null key — the DLR's expression-tree rewriter can't function under AOT; `'object' does not contain a definition for CreateTableWithColumns` (the generic tracker) is the sibling `RuntimeBinderException`. `((dynamic)ddl).CreateTableWithColumns(...)` was gratuitous (`ddl` is statically `SqliteDdlExecutor`) → direct typed call (3 sites, `fdaea45e`). **Mutation-proven:** the current AOT binary runs fallback-create with ZERO warnings; re-adding `(dynamic)` brings the exact null-key stack back. The earlier "null-key residual (separate)" note was a misread of the run3 (pre-fix) log — same cause, already fixed. **Lesson: a DLR `dynamic` call under AOT can surface as an opaque internal `ArgumentNullException("key")` (the binder's cache), not just `RuntimeBinderException` — `grep "(dynamic)"` is the real AOT tripwire.** |
| X-aot-linux-arm64 | T3 | S2.Sovereign-proof | pending (packaging) | | | | **The appliance/edge sovereign RID.** ARCH-0093 proved the framework substrate is RID/OS-agnostic on win-x64 AND **linux-x64** (real Debian 13 box). arm64 is the SAME recipe (`-r linux-arm64`, `-p:KoanAot=true`) on an **arm64 host** — the sqlite-vec `vec0.linux-arm64.so` is already embedded and ONNX-RT ships arm64 runtime packs. Cross-compiling from x64 needs the aarch64 cross toolchain (`clang` + an arm64 sysroot) or a `buildx`/QEMU arm64 builder — a packaging exercise, not a framework one. Prereqs on an arm64 Debian/Ubuntu host: `clang zlib1g-dev binutils libicu-dev` + the .NET 10 SDK; then `git archive HEAD`→transfer→publish→run→`curl` the search endpoint (see `docs/guides/nativeaot-howto.md`). |
| X-aot-embedding-stj | T3 | S2.Sovereign-proof | **DONE** | 2026-06-21 | opus-4.8 | 5973cb2a | ✅ **Premise corrected + fixed.** The STJ path was NOT "template-less" — it's `EmbeddingPolicy.FullJson` ONLY (`BuildEmbeddingText`: `Template`→`RenderTemplate`, `Properties`/`AllStrings`→`BuildFromProperties` are reflection-over-names + string ops, already AOT-clean). `SerializeToJson` (FullJson) used `System.Text.Json` with a custom `TypeInfoResolver` (property exclusion) → STJ reflection is AOT-disabled AND diverged from the entity's persisted `(Id,Json)` Newtonsoft form. **Fix:** ported `SerializeToJson` + the exclusion resolver (now a Newtonsoft `DefaultContractResolver`) and the `EmbeddingMigrator` export to Newtonsoft (canon); the low-level STJ DOM `JsonDocument`/`Utf8JsonWriter` for depth-truncation is AOT-safe and kept. New regression spec `FullJson_exclude_removes_property_via_contract_resolver` (mutation-verified — neutralizing the exclude → test fails). AI Unit 152/152. Explicit `Newtonsoft.Json` ref added to `Koan.Data.AI`. |
| X-relational-dapper-shim-adopt | T3 | S2.Sovereign-proof | pending (optional) | | | | **Optional dedupe.** ARCH-0093 shipped `Koan.Data.Relational.Dapper` (`DapperCommands`, the Dapper-backed twin of the AOT-clean `Koan.Data.Relational.Ado` surface, same `SqlParameters` model) but did NOT migrate the non-AOT relational adapters onto it. Postgres + SQL Server still call Dapper directly (1 cs file each). Adopting the shim would put Dapper behind one seam across the server adapters; non-AOT so not urgent. (Correction — a prior note here claimed Json/Mongo/Redis/Couchbase carry dead `Dapper` PackageReferences; **re-verified empirically as FALSE** — none of those 4 reference Dapper at all. Only Postgres + SqlServer do.) |
| X-inproc-vector-hnsw-managed | T3 | X-inproc-vector-bruteforce | pending (conditional) | | | | Managed-ANN scale rung — HNSW.Net (MIT, net10-only TFM; MessagePack v3 source-gen resolver not auto-composed under AOT) or HnswLite (MIT, 1-contributor; core/SQLite-persistence version split). **GATED on a `PublishAot` persistence spike** before adoption. Build only if the bruteforce O(N) scan becomes the bottleneck and a managed (non-native) ANN is wanted over sqlite-vec. Brief §5(deferred)/§8. |

## 07-AN — Agent-native projection (Stage 9 harvest)

Net-new cards from [09-agent-native-projection.md](../09-agent-native-projection.md) (the grounded
+ adversarially-verified harvest of the agent-native charter), stashed in
[07/AN-cards.md](07/AN-cards.md). The "one projector" reframe: description and enforcement are the
same projection, or they drift. **AN-leak is a confirmed live security finding — triage independent
of the rest.**

| ID | Tier | Deps | Status | Date | Agent/model | Commits | Notes |
|---|---|---|---|---|---|---|---|
| AN-leak | T3 | — (do first) | **done** | 2026-06-19 | opus-4.8 | `0f33b7ef` | **SECURITY (fixed).** New `GovernedRelationshipExpander` (Koan.Web) governs `?with=all` / `with:"all"` expansion: each edge re-queries the related type through ITS OWN WEB-0068 predicates (AND-composed with the FK filter, pushed down — also fixes the `All()`+in-memory N-load). Walled child edges + walled parents omitted (walled-means-silent). Domain `GetChildren`/`GetParents`/`GetRelatives` stay app-authority + unchanged (DECIDED #3). MCP rides the same `IEntityEndpointService` — one fix, every transport. Tests: `RelationshipExpansionVisibilitySpecs` (REST T1/T2/T-parent/T-app-authority) + `RelationshipVisibilityMcpSpec` (MCP path) via the new reusable `Koan.Mcp.TestKit` harness; mutation-verified. ADR amended (WEB-0068 §2026-06-19). |
| AN1 | T2 | — | **done** | 2026-06-19 | opus-4.8 | `e5745b24` | MCP wire-shape conformance: `ToolDescriptor` now emits spec `inputSchema` (camelCase, was `input_schema`) + a dedicated `annotations` object (ToolAnnotations container, title now; AN4 fills hints) + an omit-when-empty `outputSchema` slot for AN-structured; Koan's `metadata` bag kept as non-spec provenance. New `Koan.Mcp.Conformance.Tests` (`WireShapeConformanceSpec`, entity tool + custom `[McpTool]` verb) via the harness. **Unblocks AN4.** |
| AN2 | T3 | — | **done (premise-corrected)** | 2026-06-19 | opus-4.8 | _(this commit)_ | **Card premise was a SEMANTIC MISREAD** — `[McpIgnore]` is MCP-local; REST exposes those fields; hiding them from OpenAPI would make the doc lie. Empirically verified ([McpIgnore] field IS present in OpenAPI). Literal BUILD NOT done (dishonest). REAL drift surfaced: OpenAPI is STJ-reflective (`Microsoft.AspNetCore.OpenApi`) while REST is Newtonsoft (camelCase + StringEnumConverter + `[JsonProperty]` renames) → doc misnames renamed fields (probe: `renamedField`→`original`). Guard: `SchemaProjectionConformanceSpec` (honest-invariant + drift characterization). Real fix = a Newtonsoft-faithful `IOpenApiSchemaTransformer` (public-contract change) → **architect decision, card `X-openapi-newtonsoft-fidelity`**. |
| X-openapi-newtonsoft-fidelity | T3 | AN2 | **done** | 2026-06-19 | opus-4.8 | _(this commit)_ | **Approach changed from the card's per-schema `IOpenApiSchemaTransformer` to fixing the SOURCE globally** — `NewtonsoftSchemaMirror` (Koan.Web.OpenApi) mirrors the Newtonsoft contract onto the System.Text.Json options the generator reads, so the doc is faithful BY CONSTRUCTION. **Empirical finding:** `Microsoft.AspNetCore.OpenApi` reads `Http.Json.JsonOptions` (NOT `Mvc.JsonOptions` — MVC-only config failed the spec), so the mirror lands there. Three drifts closed: (1) **string enums** via `JsonStringEnumConverter` (API-first mandate: an enum is a semantic string, never an opaque int — the int-enum doc was a contract violation, the wire was already string); (2) **`[JsonProperty]` renames** via a `JsonTypeInfo` modifier re-reading the resolver's own output (conservative — explicit renames only, never clobbers a non-Koan `[JsonPropertyName]`); (3) **`[JsonIgnore]` omission** (off the wire+storage → absent from the doc). Side benefit: minimal-API responses (same `Http.Json` options) now share the string-enum contract. Guard `SchemaProjectionConformanceSpec` flipped drift→fidelity (3 teeth + keeps-`[McpIgnore]`); mutation-checked; OpenApi 9/9 + MCP-conformance 65/65 + Web suites green. **Public-OpenAPI-contract change** (renamed fields + string enums now correct) — architect-directed. |
| AN3 | T3 | — | **done (core)** | 2026-06-19 | opus-4.8 | _(this commit)_ | New `McpToolAccessPolicy` (Koan.Mcp.Execution) is the single effective-access authority (auth ∩ required scopes); `HttpSseRpcBridge` refactored onto it (the per-transport copy of HasAccess/HasAccessCustom/UserHasScopes deleted). STDIO local-trust documented (binds the raw handler by design — stdin/stdout = local process owner; WebSocket is a no-op placeholder, so no remote-unfiltered path today) + pinned by `EnforcementConsolidationSpec` (policy matrix + STDIO tripwire). Prior surface was UNTESTED → now covered. **Boundary/deferred:** unifying MCP `RequiredScopes` with REST's SEC-0002 `IAuthorize` per-verb topology is a cross-pillar change → follow-up `X-mcp-rest-authz-unify`. |
| X-mcp-rest-authz-unify | T3 | AN3 | pending (architect) | | | | Declare per-verb auth/scope topology once at the operation/descriptor level so REST (SEC-0002 `IAuthorize`/`RequiredScopes`) and MCP (`McpToolAccessPolicy`) gate identically — no MCP-only authz vocabulary. Cross-pillar (Koan.Web.* + Koan.Mcp). Surfaced by AN3. |
| AN4 | T2 | AN1 | **done** | 2026-06-19 | opus-4.8 | _(this commit)_ | The `annotations` object (AN1) now carries verb-derived hints. Entity verbs derive mechanically from the 12-op kind (`HintsForOperation`): Collection/Query/GetNew/GetById → `readOnlyHint`; Upsert/UpsertMany → `idempotentHint`; Delete* → `destructiveHint`. Custom `[McpTool]` verbs gain NOTHING automatically — opt in via new `[McpReadOnly]`/`[McpDestructive]`/`[McpIdempotent]` markers. Code-mode tools annotated (execute=not-readOnly, validate=readOnly). **A9 (prose half):** the machine hints pair with the human side-effect prose carried by the tool `description` (McpEntity.Description / BuildDescription); richer per-verb prose is a small future enhancement. Tests: `AnnotationDerivationSpec` (Conformance) — entity verb hints + unmarked-vs-`[McpDestructive]` custom verb. |
| AN5 | T3 | AN3 (soft) | done | 2026-06-19 | opus-4.8 | `4b8bae17`→`d41f0221` | **Done as the Door half of SEC-0005 (with P3.1).** `[Door]`/`DoorNeeds` discloses a denied verb's gate `needs` (opt-in, deny-by-default); a role-gated verb stays a silent **Wall** ("admin is a Wall"); STDIO has no doors. The privilege-enumeration oracle is gated by being opt-in per-verb. |
| AN6 | T3 | — | **done** | 2026-06-20 | opus-4.8 | `c5e76ece` · `e96cd46d` · `c06afc0b` · `a25b0a07` · `79c0bfa0` | **ADR AI-0037** (break-and-rebuild: ONE dispatch+session+streaming core, Streamable HTTP primary, legacy `/sse`+`/rpc` → thin opt-in shim). **OAuth ingress half (RFC 9728/8707/PKCE) already DONE via SEC-0006** → net-new = the transport + `MCP-Protocol-Version`. Decisions (user): Streamable default / SSE opt-in-deprecated · **full streaming** (per-POST SSE + GET push + sessions + `Last-Event-ID` resumability) · break-and-rebuild. **Ph1 DONE `c5e76ece`** — extracted `McpRpcDispatcher` (transport-agnostic dispatch core: method switch + SEC-0004 gate/visibility/principal), legacy bridge slimmed to delegate; behavior-preserving (conformance 65/65 + real HTTP/SSE e2e 9/9). **Ph2a DONE `e96cd46d`** — unified session model (`McpSseStream` per-stream event-id cursor `{streamId}.{seq}` + bounded replay; `McpSession` single-GET + per-request streams + retention; `McpSessionManager` mint 32-hex/resolve/terminate/idle-sweep), 7 unit specs, conformance 72/72. **Ph2b DONE `c06afc0b`** — `StreamableHttpTransport` POST/GET/DELETE `{baseRoute}` handler over the dispatcher + session model: POST (Accept→406, single msg, 202-notification, initialize→session-id header, non-init→protocol+session 400 -32000/404 -32001/403-not-owner, SSE-per-request default or `StreamableJsonResponse` opt-in), GET (single push stream→409, `Last-Event-ID` tail replay, eager header-flush `WriteServerPushStream`), DELETE (terminate). `JsonRpcEnvelope.TryParse` collapsed to ONE shared parser; two transports share one route group + auth filter + well-known. New real-Kestrel suite `Koan.Mcp.Streamable.IntegrationTests` (12/12: 10 mechanics + 2 auth same-principal crux; **3 mutations w/ teeth**); conformance 72/72 + Auth.Server 50/50 hold. **Ph3 DONE — full architecture eval + adversarial review + 4 commits** (`d41f2769` ADR eval+revisions · `6282ca76` Ph3-pre golden legacy wire spec · `c5d533af` Ph3a · `a25b0a07` Ph3b). 3-lens adversarial-review workflow (verdict needs-revision) caught 4 real defects → 10 binding ADR revisions. **D-C** `IMcpConsoleRenderer` seam — core SOLELY owns bare `GET {baseRoute}` (negotiates event-stream→stream [inline auth] / html→renderer / 405-stream-off / 404), console mapped OUTSIDE auth group (anonymous face); Explorer drops bare-GET+implements seam. **Default flip (atomic):** `EnableHttpSseTransport`=master→Streamable default (`StreamableHttpEnabled`), legacy=explicit `EnableLegacySseTransport`. **D-A/D-B** legacy → thin shim over unified `McpSession`/`McpSessionManager`/`McpRpcDispatcher`; **DELETED 4 files** (`HttpSseSession`/`Manager`/`HttpSseRpcBridge`/`ServerSentEvent`); frames via `EnqueueRaw` (no id:, mutation-verified), inline dispatch (id-correlated), session dies w/ GET stream, health lifted to `McpSessionManager`. **D-D KEEP STDIO on StreamJsonRpc** (withdrew ADR Phase-4 "STDIO onto core" — justified divergence). Net: session models 2→1, SSE frames 2→1, −3 files. Streamable 18/18, golden legacy 1/1 byte-faithful, conformance 72/72, Auth.Server 50/50. **Ph4 DONE `79c0bfa0`** — doc-surface reconciliation + dedup: ADR→Accepted; ONE HTTP-transport guide (`mcp-http-sse-howto.md` retitled "MCP over HTTP", Streamable-first "Transport at a glance" + legacy demoted to deprecated opt-in, 4 stale config literals fixed); mcp.md card (Streamable default, dropped AddKoanMcp ceremony); koan-mcp skill (Transports note + EnableLegacySseTransport); agent-native card+guide ("HTTP/SSE"→"HTTP"); oauth-server-howto (Streamable demo); AI-0013→Superseded; SURFACES row. Gates: docs-lint 0 err, skills-lint 0/0, doc-code 1/1. **AN6 COMPLETE — all 4 phases.** |
| AN7 | T3 | AN-leak, P1.2 | **done (v1)** | 2026-06-19 | opus-4.8 | _(this commit)_ | Governed edge traversal as DISCOVERY. **Premise re-derived (like AN2):** the card's 3-step build assumed step 3 (a traversal mechanism) was net-new — but the governed traversal ALREADY EXISTS (AN-leak's `GovernedRelationshipExpander` for `with=all`; the per-entity Query tool runs WEB-0068 predicates). A separate per-edge traversal verb/resource would (a) duplicate that governance and (b) re-introduce the catalog explosion the card engineers against. So AN7 v1 = the genuinely net-new half: **step 2, governed edge metadata in `koan://entities`.** Each grant-visible entity gains an `edges` array (`EntityEdgeProjection` reading `IRelationshipMetadata`'s `[Parent]`/child relationships) — `{name, kind: parent\|child, target, via: <fk field>, cardinality: one\|many}`. An edge is a ROUTE, never a verb (no edge becomes a tool). **Per-grant at the catalog level: an edge whose TARGET type is walled is ABSENT** (walled-means-silent — the field name would leak the target's existence; `visibleByType` built from `EntityProjection.Visible`). The edge carries the navigation recipe (target tool + via field); the agent follows it via the already-governed Query/Collection tool, which sizes the result per grant (the works-authored-open / works-reviewed-walled asymmetry is that resolved query's projection, DECIDED). Subsumes AN-leak (its security-critical first slice, `0f33b7ef`). Tests (`Koan.Mcp.TestKit`, entities Author/Article with two same-target edges): `EdgeTraversalSpec` (child+parent edges, recipe, walled-target-absent) + `EdgeNavigabilitySpec` (the recipe resolves through the governed Collection tool). Mutation-checked (grant-ignoring edge filter → walled-absent fails). **Boundary:** a Door-style invitation on a walled edge = AN5/grants. |
| P1.2 | T2 | — | **done (core)** | 2026-06-19 | opus-4.8 | _(this commit)_ | MCP introspection resources (O6) — new `resources/list` + `resources/read` on both transports + a discoverable `IMcpResourceProvider` seam (TryAddEnumerable; AN8's `koan://self` + apps ride it) + the built-in `koan://entities` catalog, **projected per grant** (verbs filtered via `McpToolAccessPolicy`, walled entity omitted; null principal = STDIO local-trust full, remote edge passes a concrete principal). Harness `ListResourcesAsync`/`ReadResourceAsync`; `ResourcesConformanceSpec` (list/read/unknown-uri + per-grant projection). **Note:** MCP `initialize` capabilities advertisement is pre-existing-absent (Koan has no JSON-RPC initialize handler; tools work the same way) → resources callable but not advertised in a handshake; broader protocol handshake is AN6 territory. |
| X-agent-native-ref-card | T2 | AN1-AN9 | **done** | 2026-06-19 | opus-4.8 | _(this commit)_ | **Decision: standalone card** (user-confirmed "write it now"). New `docs/reference/cards/agent-native.md` — dual framing: *what you DECLARE* (`[McpEntity]`, verb-hint attrs `[McpReadOnly]`/`[McpDestructive]`/`[McpIdempotent]`, `IMcpResourceProvider`, `[KoanApp]`, `RequiredScopes`) ↔ *what the AGENT SEES* (spec tools+annotations, `resources/*`, `koan://entities`+`koan://self`, per-grant projection / walled-means-silent, the correlation pin); unbuilt half (grants/Door/device-grant/dry-run/Streamable-HTTP) under a **Frontier** heading (honest API-truth, no vapor). Stale `mcp.md` refreshed (agent-native note + cross-ref, dates bumped). `domain: mcp` (reused, no taxonomy change). green-ratchet doc legs (build/docs-lint/doc-code/skills-lint) all GREEN. |
| AN8 | T3 | P1.2/O6, AN3 | **done (core)** | 2026-06-19 | opus-4.8 | _(this commit)_ | `koan://self` resource (`SelfResourceProvider`, on the P1.2 seam) renders the per-grant projection in TWO faces in one resource: first-person `prose` greeting (app identity from `[KoanApp]`→`KoanEnv.CurrentSnapshot.Application` + visible verbs) + `structured` contract (identity + entities + verbs). Prose never the only form. Per-grant projection shared with the catalog (`EntityProjection.Visible`; walled entity absent — slice of charter T6). `SelfIntroductionSpec`. **Deferred (needs AN5/grants):** the Door "one step further" line + admin-tier semantics. **Addendum A4/A5/A6 (pagination-into-result / etag / depth ladder) not yet wired** — small follow-ups on this resource. |
| AN9 | T2 | — | **done (core)** | 2026-06-19 | opus-4.8 | _(this commit)_ | The pin: `RequestTranslator` accepts a client-owned `correlationId` (opaque, UNTRUSTED — never validated/deduped-for-trust/grant-associated) + mints a GUIDv7 (`StringId.New()`) when absent, threaded into `EntityRequestContext.Items` (`McpCorrelation`); `ResponseTranslator` echoes it in result diagnostics for trajectory stitching. **Continuity ≠ authority pinned** (`CorrelationPinSpec`): the pin gates nothing — `McpToolAccessPolicy` is principal-only (accepting a pin in place of a grant = session fixation). A7/#20 (client-owned, device_code stays server-issued) honored. **Deferred (pairs with P3.1):** threading the pin into a durable audit trail. |
| AN10 | T3 | P3.1, AN3, AN9, WEB-0071 | done | 2026-06-20 | opus-4.8 | SEC-0006 · WEB-0072 (`05c8474f`+`bcd993c6`) | **Delivered via SEC-0006 + WEB-0072 (outside the card process).** The RFC 8628 **device grant** is the SEC-0006 `DeviceEndpoint` (device_code opaque + server-issued, `slow_down`/`interval` enforced; advertised through RFC 8414 `/.well-known/oauth-authorization-server`). Reference=Intent honored (no `[McpAuth]`; clients drive via discovery). The 3-strings invariant holds: device_code (server-issued, SEC-0006) ≠ the AN9 client-owned correlation pin (untrusted, gates nothing). **WEB-0072 P3** added the Development dev-client auto-seed (`koan-dev-explorer`, fail-closed) + the interactive "play the device" console exerciser. **Original-intent nuance:** AN10's framing was MCP-on-ramp-specific (companion spec 09 §12); the device grant is AS-level + transport-agnostic, so it serves the agent on-ramp without MCP-edge-specific wiring. Verify against 09 §12 if a stricter reading is wanted. |
| AN11 | T3 | AN2, AN3 | **done (v1)** | 2026-06-19 | opus-4.8 | _(this commit)_ | dry-run + state-delta + honest validation errors. **A1:** `dry_run:true` is a first-class endpoint concept — `DryRun` on the 7 mutation request DTOs + threaded through every `IEntityEndpointService` mutation path (Upsert/Patch/Delete + UpsertMany/DeleteMany/DeleteByQuery/DeleteAll). The hook/validation pipeline runs (BeforeSave/BeforePatch/BeforeDelete); the adapter write does NOT (terminate-before-write — universally honest for DB rehearsal; AfterSave/AfterDelete not raised). Batch ops skip-and-report a count (`wouldUpsert`/`wouldDelete`/`wouldDeleteAll`); DeleteByQuery reports an EXACT count (it queries first anyway). **A2:** every entity mutation (rehearsal AND real) returns a semantic delta in `meta.diagnostics.delta` — `{operation: create\|update\|delete, dryRun, changes:[{field,from,to}]}` — identical shape for preview and commit (`MutationDeltaProjector`; v1 = payload-touched/changed fields, one keyed pre-read gated by `EntityMutationProbe.WantsDeltaKey` so REST stays cost-free). **Walled-means-silent on the delta channel:** an `[McpIgnore(Output)]` field is ABSENT from the delta (the projector applies `McpFieldPolicy`; wire names, string enums). **A3:** a bad payload returns `meta.diagnostics.didYouMean` from SCHEMA FACTS ONLY (enum members, required fields) via `SchemaValidator` walking the generated input schema — never rows/counts/existence (#6 re-applied to the error channel; the AN-leak existence leak cannot reopen through validation). **A10:** `dry_run` on a custom `[McpTool]` verb does NOT execute it — `McpRpcHandler` returns an honest partial rehearsal (`rehearsable:false`, declared effect hints, "not executed") naming the framework-inspection limit. `dry_run` discoverable in every mutation tool schema (#14). Tests (ARCH-0079, `Koan.Mcp.TestKit`): `DryRunStateDeltaSpec` (T12/T13 — writes-nothing + same-shape + update transition + leak guard), `DidYouMeanSpec` (T14 — enum/required + schema-not-rows), `CustomVerbRehearsalSpec` (A10). Mutation-checked (4 defects → 7 caught). **v1 boundary (honest, not stopgap):** rehearsal is terminate-before-write, not a rolled-back tx (DB-only honest); A10's "build-time warning" for un-inspectable effects = the runtime partial-rehearsal (a Roslyn analyzer is a future enhancement); saga compensation = v3. Addendum A4/A5/A6→AN8, A7→AN9, A8→AN5, A9→AN4 |

## Cards discovered during pilot execution

New cards surfaced while running other cards (not in the original 06/07 stash). Same columns.

| ID | Tier | Deps | Status | Date | Agent/model | Commits | Notes |
|---|---|---|---|---|---|---|---|
| X-KoanContext | T3 | — | handed off | 2026-06-14 | | | **DECIDED + handed to a separate session.** `Koan.Service.KoanContext` (14.8k LOC, out-of-sln, mid-refactor) is a self-hosted Context7 (code/docs intelligence over MCP+Web+Vector). Resolution: rename `Context`→**Librarian** (clearer intent), re-home to agyo-tools as **`Agyo.Service.Librarian`** (agyo's `Agyo.Service.*` runnable-tool tier — PowerToys host apps too, not only libraries), and re-platform onto `Sylin.Agyo.Rag` (its bespoke pipeline duplicates the migrated RAG toolkit). Full version-controlled brief: **`agyo-tools/docs/handoffs/librarian-migration.md`** — executed independently of this prompts effort. Koan-side: `git rm` the husk + close this row once Librarian lands in agyo. |
| X-FluentAssertions | T2 | — | done | 2026-06-15 | opus-4.8 | cb60e404 | Fixed the AwesomeAssertions v6→v7 rot in `S16.PantryPal.Tests` (`HaveCountGreaterOrEqualTo`→`HaveCountGreaterThanOrEqualTo` + `BeGreaterOrEqualTo`×2 + the CS1997 `async DisposeAsync`); net10.0 / AwesomeAssertions 9.4.0, restores clean (CVE pin inherited) → **re-added to Koan.sln** (`--in-root` to dodge a solution-folder name collision with the S16.PantryPal API project). Standalone 11/11; full sln build 0 err. **Audit of the other husks (re-derived empirically):** only `S7.Meridian.Tests` shares the FA rot (1 site) but is BLOCKED beyond FA scope — its `EmbeddingCacheTests` references `IEmbeddingCache`/`CachedEmbedding`/`IDocumentStorage` (grep-zero in src, deleted) → stays excluded (sample-API decay, separate concern). No other test project carries the old API. |
| X-snippet-lint-fix | T2 | — | done | 2026-06-15 | opus-4.8 (wave) | f7648df6 | root cause was a case-insensitive PowerShell collision: locals `$full`/`$files` aliased the typed `[switch]$Full` / `[string[]]$Files` params → a swallowed MetadataError fell through to "no docs to validate" in BOTH modes, so the gate validated nothing. Renamed to `$fullPath`/`$scopedFiles`; both modes run end to end; mutation-verified (a broken snippet → exit 1). **Unblocks H2** — which now sees 777 real doc-snippet failures (its job). |
| X-semantic-pipelines-retire | T2 | — | done | 2026-06-15 | opus-4.8 | e9df0130 | **Archived (not rewritten).** Empirically re-derived: the DSL it teaches (`AllStream`→`Tokenize`→`Branch`→`SaveWithVectors`, the live `PipelineBuilder<T>`) is real, but it duplicates `ai-vector-howto.md §7` (status:current, all-examples-tested); its only unique content was the removed Flow pillar + the deleted `FlowInterceptors`/`FlowEvent` (grep: 0 src files). Rewriting would just clone §7 → `git mv` to `docs/archive/guides/` (history kept) + banner; repointed all 7 inbound links across 6 live docs to ai-vector-howto. Consistent with DX-0041. docs-lint Errors:0, exit 0. |
| X-f2-failure-coverage | T2 | — | done | 2026-06-15 | opus-4.8 | e7033da9, 79a47bc2, 83ce6dc8 | **Data-core slice DONE (the explicitly-deferred item).** Built the reusable `KoanLog.TestSink` seam (internal, at the single `KoanLog.Write` chokepoint — fires before logger dispatch regardless of logger; null in prod = one null-check) + `InternalsVisibleTo(Koan.Tests.Data.Core)`, unblocking the deferred `ConfigWarning` capture (the per-scope-cached static `KoanLogScope` made factory-attach flaky). `AdapterConnectionResolverFailurePathSpec`: 4 behavioral (malformed @ source-definition / config-source / config-adapter → default; well-formed coerces) + 1 observability (exactly one Cnfg Warning, outcome=malformed-value, provider/setting/origin/reason). Mutation-verified (revert warn→swallow fails `ContainSingle`). 5/5; full sln 0 err. **Storage slice DONE (`79a47bc2`):** resurrected the `Koan.Storage.Core.Tests` husk (was net8.0 on the phantom `Koan.TestPipeline`/`Koan.Storage.Core` `*` packages, excluded since B1; its 4 specs were dead TestPipelineFixture illustrations) → real net10 project (ProjectReference Koan.Storage), re-added to sln; `ReplicatedStorageProviderFailurePathSpec` pins the cache-fault→durable degrade contract (3/3, **mutation-verified**: `catch(Exception)`→`catch(IOException)` makes the non-IOException degrade test fail). **Web slice DONE (`83ce6dc8`):** new net10 `Koan.Web.WellKnown.Tests` WebApplicationFactory suite (modeled on the Sort harness — marker `.sln` + content-root assembly attr; `GenerateProgramFile=false` for the SDK.Web+test-SDK entry-point clash). `FaultyAggregate` pinned to a non-existent adapter via `[DataAdapter("does-not-exist")]` makes the endpoint's per-item `GetRepository` throw — the exact fault the degradable catch absorbs; spec asserts 200 (not 500) + FaultyAggregate present-but-degraded (empty tokens) + HealthyAggregate listed. **Mutation-verified** (catch→rethrow makes the endpoint 500 + the test fail). 1/1; added to sln. **Done:** data-core + Storage + Web all have real degradable-path specs; **Sqlite's** correctness path (connection-string redaction) is already pinned in `15bfa32d` — its remaining DDL/DROP/directory edge paths are deliberately left (low-ROI real-fault injection). Pre-existing unrelated flake noted: `VectorConcurrencySpec.Concurrent_reads_and_writes_without_transaction`. |
| X-entitycontroller-query-parse | T2 | — | done | 2026-06-17 | opus-4.8 | ed490c96 | **Architect chose GET-parity** (via AskUserQuestion). The bare `catch {}` in POST `/query` swallowed `JToken.Parse` + the `(int)page`/`size` casts → ran with DEFAULTS at 200; worst case a non-object body **dropped the filter and returned UNFILTERED results**. Fix: a non-object body and a negative page → `BadRequest`; non-coercible page/size are ignored (treated as absent) EXACTLY like the GET-list sibling (which uses `int.TryParse`); new `TryCoerceQueryInt` coerces without throwing; bare catch removed. Empirical wrinkle that shaped the decision: GET-list is itself LENIENT on non-numeric page/size (TryParse→default; 400 only on negative page), so "400 on everything" would make POST *stricter* than GET — GET-parity avoids a new inconsistency. New `QueryParseSpecs` (5, real WebApplicationFactory + in-memory store), **mutation-verified** (neuter each guard → its spec fails). **Fixed 2 pre-existing latent test-infra bugs to land it:** Sort.Tests csproj was missing `GenerateProgramFile=false` (CS7022 top-level-Program vs test-SDK-Main, fatal under TWAE — the fix WellKnown.Tests already had; surfaced when the Koan.Web change forced a full rebuild) + the suite shares a process-global in-memory store, so adding a 2nd test class exposed a cross-class parallelism race → assembly `DisableTestParallelization`. Full sln 0 err; Sort 24/24. |
| X-couchbase-testcontainers | T2 | — | obsolete | 2026-06-15 | opus-4.8 | — | **Premise empirically FALSE — no version mismatch exists.** `dotnet list package --include-transitive`: the Couchbase AND Postgres connector tests resolve IDENTICAL `Docker.DotNet 3.125.15` + `DotNet.Testcontainers 1.7.0-beta.2269` (both via shared `Koan.Testing`; the Data AdapterSurface TestKit pulls no Testcontainers). And `MissingMethodException…CreateClient()` is NOT Couchbase-specific: `PostgresContainerFixture` and `CouchbaseContainerFixture` are the same pattern — both catch it (`IsTestcontainersMissingMethod`) and fall back to a raw `docker run` CLI. The exception is the UNIVERSAL behavior of the dead 1.7.0-beta stack on net10; `…fixture.testcontainers.missingmethod` is a normal precursor WARN, not the failure cause → "align versions" is a verified no-op. The genuine residual (Couchbase 1/8) is cluster-init/networking → existing **"Couchbase Testcontainers networking" backlog** (live-Couchbase debugging). Strategic fix split out as **X-testcontainers-modernize** (T3). |
| X-testcontainers-modernize | T3 | — | done | 2026-06-18 | opus-4.8 | ARCH-0091 · `feat/testcontainers-harness-redesign` (26 commits from `6873ad0f`) | **Scope GREW far beyond the card's "version bump."** The card framed a fixture-only swap (`DotNet.Testcontainers 1.7.0-beta` → `Testcontainers 4.x`). The right fix (ARCH-0091, architect-ratified — `docs/decisions/ARCH-0091-integration-test-harness-redesign.md`) was a FULL harness redesign: retire the bespoke `Koan.Testing` `TestPipeline`/`TestContext` DSL entirely and migrate ALL non-fenced test projects xUnit **v2 → v3** (VSTest-compatible, `OutputType Exe`, `Assert.Skip`, `ValueTask IAsyncLifetime`) onto new `Koan.Testing.Containers` fixtures (`KoanContainerFixture` + `KoanDataSpec<T>`, official Testcontainers 4.x module builders). Done in waves (pilot → 1 data → 2a vector → 2b data → 2c web-e2e → 3a/b/c host+unit → MCP samples), each **real-store-oracle-verified** (`dotnet test` vs the live backing store, never agent-compile-green). Close-out: bespoke types deleted (40 files) with `Koan.Testing` kept ONLY as a Hosting shim so the FENCED xUnit-v2 `Koan.Jobs.TestKit` still resolves `KoanIntegrationHost`; Jobs trees stay v2 (fenced); 4 orphan dead dirs (Context.Unit/DocMind/PantryPal/Web.Admin) deleted; `tests/README` rewritten. Full `Koan.sln` build 0 err. |
| X-data-direct-fold | T3 | S4 | done | 2026-06-17 | opus-4.8 | 67886dea | **Premise STRONGER than framed — `Koan.Data.Direct` was a true orphan (ZERO `ProjectReference`s repo-wide; only `Koan.sln` listed it), so its `KoanDataDirectModule` never loaded and `.Direct()` threw by default everywhere; the 3 connector "Koan.Data.Direct" matches were DATA-0053 provenance COMMENTS, not a compile-time edge.** Moved the 5 impl files (`DirectSession`/`DirectTransaction`/`DirectDataService`/`JsonSettings`/`ConfigurationConstants`) → `src/Koan.Data.Core/Direct/` (namespace `Koan.Data.Core.Direct`, impls now `internal`; flattened `ConfigurationConstants` to dodge an `Infrastructure` clash with Core's own); **registered `IDirectDataService` by DEFAULT** next to `AddSingleton<IDataService>` (`ServiceCollectionExtensions.cs:55`) → `IDataService.Direct(...)` works out-of-box, reworded the now-defensive throw; dropped `DirectRegistration` + `KoanDataDirectModule`; deleted the project from `Koan.sln` + dir. Direct was ENTIRELY untested → shipped its first guard `DirectDataAccessSpec` (2 specs through real `AddKoan()` + SQLite: default-registration + Execute/Scalar/Query round-trip + tx commit/rollback), **mutation-verified** (disabling the registration fails both). External break (architect-accepted): `Sylin.Koan.Data.Direct` id + `AddKoanDataDirect()` gone — migration note in modules-overview/capability-map/cache.md/CLAUDE.md + ARCH-0090 §1 Status. Full sln 0 err; Data.Core 139/140 (the 1 = pre-existing `VectorConcurrencySpec` flake, 7/7 isolated). |
| X-ai-dissolution-migration | T3 | S3 | pending | | | | (From S3 / ARCH-0089.) Execute the AI pillar dissolution — a multi-repo, transition-safe (AGYO-0001 §3) program: **(Agyo, C#→C#)** `Sylin.Agyo.Eval` + `Sylin.Agyo.Review` standalone first, then fold `Orchestration`+`Agents` into `Agyo.Rag` (harmonise `EntityToolGenerator` ↔ `RagRetrievalTools`); **(Zen Garden, C#→Rust)** confirm-then-delete `Koan.AI.Compute` (ZG hardware discovery already covers it), port `Koan.AI.Models` to ZG (decouple the `ModelEntry` `Entity<>` ORM → read-only cache or DTO+REST), HuggingFace follows; **archive** ZenGarden-connector + Training; move `S18.Prism` → Agyo (flagship demo); split the AI test suites by destination (`Eval/Review.Tests`→Agyo, `Models.Tests`→ZG, `Integration/EndToEnd` split; `Prompt/Contracts.Shared/Data.AI` tests + Ollama/LMStudio unit tests stay). Stand up new packages green BEFORE stripping `Koan.sln`; keep Koan publishing until consumers re-point. **Plus the residual in-Koan ARCH-0084 token migration** for the staying core (Mechanism 1 `AiCapability` strings + Mechanism 2 `AiCapabilityConfig`, across Ollama/LMStudio + `AiCategoryRouter`). |
| X-cacheable-attribute-init | T2 | — | done | 2026-06-20 | opus-4.8 | _(this commit)_ | **RESOLVED.** (Discovered during H10 Phase 3 by the skill compile-gate.) `CacheableAttribute` (`src/Koan.Cache.Abstractions/Policies/CacheableAttribute.cs`) exposes `L1TtlSeconds`/`SlidingTtlSeconds`/`AllowStaleForSeconds` as **`init`-only** integer-second setters — which **cannot be attribute named arguments** (CS0617), so the attribute's OWN documented usage `[Cacheable(60, AllowStaleForSeconds = 10)]` (its XML doc lines 19-20) does **not** compile. The per-entity SWR / L1 / Sliding-TTL opt-in is inexpressible today; only the per-call fluent `.AllowStaleFor(...)` works. **Fix:** change these (AND the base `CachePolicyAttribute.L1AbsoluteTtl`/`SlidingTtl`/`AllowStaleFor`) from `init` to `set` (behaviourally identical for attribute usage). An in-flight init→set on the derived props alone cascades to CS8852 on the init-only base, so the full fix spans both types + needs a cache-suite test pass — deliberately NOT rushed inside H10 (koan-caching works around it via `.AllowStaleFor`; src kept clean). **RESOLVED:** the int-second bridges (`L1TtlSeconds`/`SlidingTtlSeconds`/`AllowStaleForSeconds`) gained **computed read-write getters** projecting their canonical `TimeSpan?` base props — CS0617 needs read-WRITE, not write-only, so a naive `init`→`set` (which left them write-only) still failed; the getters are what make them valid named attribute args. ALL base `CachePolicyAttribute` props also `init`→`set` (the SAME latent bug hit every enum/string/bool/array named arg, not just TTL). Proof: `CacheableAttributeSpec` now applies `[Cacheable(60, L1TtlSeconds=10, SlidingTtlSeconds=30, AllowStaleForSeconds=15)]` + `[CachePolicy(…, Tier=, Strategy=, Tags=)]` as ATTRIBUTES (the form that wouldn't compile) and reads them back; abstractions 55/55, topology 50/50; mutation-verified (drop a getter → CS0617 fails the build). |

## Divergence log

When a pre-flight fails or repo reality contradicts a card, record it here:
date · prompt ID · what was found · what was done instead.

| Date | ID | Finding | Action |
|---|---|---|---|
| 2026-06-13 | B1 | The card named only 5 husks to exclude. In reality 38 test projects were missing from the sln, and **13 more break the build** beyond the card's list: (a) 3 net8.0 husks referencing nonexistent `Koan.TestPipeline`/`Koan.*` NuGet packages — `Koan.Storage.Core.Tests`, `Koan.Web.Admin.Tests`; (b) 2 net8.0 sample-test husks that fail restore (`Koan.Samples.DocMind.Tests` refs a *missing* `samples/S13.DocMind/API/...csproj`; `Koan.Samples.PantryPal.Tests` is net8.0 vs net10.0 `Koan.Testing`); (c) 3 MCP/PantryPal projects fail NU1903 (transitive MessagePack 2.5.192 CVE — the 7e8a44f0 pin only covers projects that ProjectReference `Koan.Orchestration.Aspire`, which these don't): `Koan.Mcp.TestHost`, `Koan.Samples.McpCodeMode.Tests`, `S16.PantryPal.Tests`; (d) 4 projects fail **compilation** against current APIs — `Koan.Web.Sort.Tests` (CS7022 dup entrypoint), `Koan.Storage.Connector.Local.Tests` (stale `StorageProfile`/`StorageOptions`/caps), `S7.Meridian.Tests` (stale `IEmbeddingCache`/`CachedEmbedding`/`IDocumentStorage.Delete`), and a **production** project `src/Services/code-intelligence/Koan.Service.KoanContext` (does not compile; was dragged in transitively by `Koan.Tests.Context.Unit`). | Added the 25 buildable live test projects (+2 transitive prod refs `Koan.Cache.Analyzers`, `Koan.Data.Connector.PGVector`). Per STOP condition, did NOT fix-forward any broken project; excluded all 13 breakers (and orphaned transitive pull-ins `samples/S7.Meridian`, `Koan.Service.KoanContext`). Build green; `--list-tests` exit 0. These 13 are husk/rot candidates for the C-series cut cards — they fell out of the sln precisely because they don't build. |
| 2026-06-13 | B1·CVE | (B1 follow-up, per architect decision) `StreamJsonRpc 2.22.23 → MessagePack 2.5.192` is a **second** CVE-2026-48109 root not covered by `7e8a44f0`'s Aspire-only pin: 12 NU1903 warnings fleet-wide + 3 unbuildable MCP test projects. | Pinned MessagePack 2.5.301 in `Koan.Mcp` (the sole direct StreamJsonRpc referencer) → NU1903 = 0; recovered `Koan.Mcp.TestHost` + `Koan.Samples.McpCodeMode.Tests` into the sln (195→197). Commits `b8530a30`, `28bb32ba`. Also fixed the malformed XML comment that broke `dev`'s build (`dcc1477c`). |
| 2026-06-13 | S16.PantryPal.Tests | The CVE pin recovered 2 of B1's 3 MCP breakers, but this one has rot **beyond** the CVE: stale FluentAssertions v6 API + a CS1997 async `DisposeAsync` bug. | Left excluded from the sln; logged as card **X-FluentAssertions**. Not fix-forwarded — separate concern from the CVE. |
| 2026-06-13 | A1 | Card prescribes the banner "Flow pillar removed from the codebase" and names "DATA-0019 (Cqrs)". Reality: Flow is **mid-migration to Canon** (`Koan.Canon.Domain`/`.Web` still in the sln; `Koan.Flow.Core`/`Koan.Canon.Core` dirs exist) — not removed; and `DATA-0019` is the **Outbox** ADR, there is **no CQRS ADR**, while `src/Koan.Data.Cqrs` still exists. | FLOW banners reworded to the truthful "superseded by the Koan.Canon rebuild"; DATA-0019 skipped (both act-conditions unmet). Upstream gap flagged: the CQRS decision has no ADR. |
| 2026-06-13 | A3 | Card estimated "~9" `*.csproj.lscache`; actual **103** (one per project). Deleting `ZERO-CONFIG-ANALYSIS.md` orphaned an inbound link at `docs/decisions/DEC-0053:339` — a **cross-card artifact** (A3 deletes; the link lives in A1's lane). | Deleted all 103 (gitignored). Orchestrator repaired the DEC-0053 link during integration (folded into A3 commit `ad13b65c`). Confirms: concurrent cards can create artifacts neither owns — the integrator resolves them. |
| 2026-06-14 | F1 | First code-adding card under the strengthened test-adequacy gate. The implementation was faithful and its 2 initial ARCH-0079 specs were mutation-proof — but the gate's **coverage critic** found the **manifest-invoker fail-loud path** (the one whose failure "can silently no-op the ENTIRE framework") and the **MODULES-FAILED render** were UNTESTED. A careful agent still missed the most dangerous path. | Remediated: +5 specs (manifest fail-fast/lenient, render +/-, KoanBootException field/message, clean-boot guard) + a minimal null-by-default internal test seam (the source-gen loader swallows everything, so the branch is otherwise unreachable). Orchestrator **independently mutation-checked** every new spec (revert→fail, restore→pass). F1 passes only AFTER remediation. Lesson: comprehensive integration tests are not automatic — the coverage-critic + mutation-check gate is what makes "wrote tests" mean "tested the dangerous paths". |
| 2026-06-14 | wave-2 docs (A2/H5/H7, parallel) | (a) `scripts/validate-code-examples.ps1` is **broken** (SwitchParameter crash, line 219) in all modes — snippet-lint validates nothing. (b) `set` drifted → folded into `partition` (DATA-0077); `BootReport` is no longer a type (rendered provenance). (c) A2's heading rename broke an inbound deep link from out-of-lane `docs/examples/entity-pattern-recipes.md:40` (A2 left a backward-compat anchor shim). (d) `semantic-pipelines.md` is wholesale deleted-Flow prose. | 3 cards ran in parallel cleanly (disjoint lanes); glossary anchors `set`→partition + BootReport→renderer; orchestrator fixed the pre-existing `index.md → reference/index.md` broken link at integration. New cards filed: X-snippet-lint-fix (blocks H2), X-semantic-pipelines-retire. `DataSetContext.With(set)` ghost in comparison.md deferred. |
| 2026-06-14 | C9 + ALL cut cards | **SYSTEMIC blind spot, caught by the operator.** Cut/park prechecks grep only IN-REPO consumers (src/samples/tests/sln), but Koan ships as NuGet packages whose real consumers live in OTHER repos — invisible to that grep. C9's target Koan.Tagging is actively consumed by a downstream repo, so it must NOT be cut/parked. The same in-repo-only precheck passed the 5 already-merged cuts (C1 Cqrs, C2 WebSockets, C3 Json.Strict, C5 Recipe, C6 Inbox-Redis). | C9 aborted (no change; Koan.Tagging intact). **Cut batch HALTED** (C14 not run) pending an external-consumer audit of the 5 merged cuts — any externally-consumed package may need reverting. **New rule: every cut/park precheck must include an external-consumer check (downstream repos / NuGet), not just in-repo grep.** |
| 2026-06-14 | external-consumer audit — RESOLVED | Audited the 5 merged cuts against the downstream consumer repo + Koan samples (consumer repo path kept private per persona separation). Only **Koan.Recipe.Observability (C5)** was actually consumed (PackageReference + packed from source) → reverted (`35318300`). **Koan.Tagging** (C9 target) consumed → not cut. **C1 Cqrs / C2 WebSockets / C6 Inbox-Redis** = zero downstream refs → stay cut. **C3 Web.Json.Strict** = only a doc mention → stays cut (re-add if it was planned). | Net: 1 of 6 reverted; build green. Going-forward gate: every cut/park is preceded by an external-consumer grep of the downstream repo + Koan samples before removal. |
| 2026-06-14 | C-series reorg → agyo-tools | The cut-or-keep binary kept losing capability the external-consumer gate flagged (C5/C7/C9/C17 all "cuts" that turned out consumed). An independent 11-capability re-eval ([08-agyo-reorganization.md](../08-agyo-reorganization.md)) found **0 core-Koan, 9 preservable** → the user founded **agyo-tools** ("PowerToys for Koan"; sibling repo depending on `Sylin.Koan.*` public packages, never the reverse — STACK-0001). | Scaffolded agyo to Koan's repo canon (`AGYO-0001`) + a local Koan package feed; migrated **WebSockets (C2)** end-to-end (`Sylin.Agyo.WebSockets.0.1.2`, build+pack green, layering clean, pushed to agyo `origin/dev`); readjusted the 06 cut cards + added a MIGRATE-TEMPLATE + the migrate-to-agyo ACCEPTANCE addendum. Remaining migrations (C4/C5/C7/C8/C9/C10/C13/C17) tracked in agyo `docs/SURFACES.md`. |
| 2026-06-14 | reorg COMPLETE (all 9 migrated + Koan-side removed) | The full agyo-tools reorganization landed. All 9 capabilities live in agyo as `Sylin.Agyo.*` (13 src projects) with **ARCH-0079 integration tests through real `AddKoan()`** (35 pass / 4 infra-gated skip). The integration tests surfaced + we FIXED real bugs (Rag FlattenHierarchy → `[RagCorpus]` works; GraphQl per-container registration + totalCount cast; Translation null-guard) and **fully implemented** the vaporware (Scheduling cron/locks/windows/health via Cronos; Observability real OpenTelemetry). Decisions: config sections rebranded `Agyo:*`; the compat-ranges MSB4096 fix landed in BOTH repos (`b795161e`); the registry generator attached repo-wide in agyo for boot-order-robust discovery. | **Koan-side removals done (consumers re-pointed):** Tagging/Secrets(×3)/Recipe.Observability removed (`d3f97694`); Recipe.Abstractions deleted (superseded by KoanModule); Scheduling removed with S5.Recs → Koan.Jobs `@boot` job, Koan.Web de-bloated (dropped the hard-ref + dead `/.well-known/scheduling`), `KoanServiceActions/Events.Scheduling` cleaned from Core; attic/Koan.Rag deleted; Translation husk removed. The Secrets reflection seam STAYS in Koan.Data.Core. **Koan.sln build green + Bootstrap integration 23/23.** Open: out-of-sln KoanContext still refs Scheduling (separate X-KoanContext re-home). |
| 2026-06-15 | F2 wave (sqlite/data-core/web/storage) + X-snippet-lint-fix | Ran as a supervised workflow (5 implement + 5 adversarial-review agents). The reviewers caught real issues the implement agents missed: a MEDIUM **secret leak** (SqliteRepository logged the raw connection string — SQLCipher `Password=`/`Key=` — unredacted; a NEW regression introduced by *adding* the warning), an over-narrowed WellKnownController diagnostics catch that broke `/.well-known/aggregates` degradability, and 2 over-narrowed Sqlite wait-barriers letting a mid-poll `ObjectDisposedException` escape. | Orchestrator independently re-derived all four from source and remediated before commit (`Redaction.DeIdentify` on the cs; broadened the diagnostics + barrier catches), re-ran the full gate (build + Sqlite/Data.Core/Web suites + Bootstrap 23/23 + lint), and added a **mutation-checked** redaction test (`15bfa32d`). Two follow-ups filed (X-f2-failure-coverage, X-entitycontroller-query-parse). The KoanContext husk (2 tracked files) was concurrently removed by the **Librarian session** (`4fd1bc7b`) — left untouched, not part of these commits. F2-mcp deferred (Mcp boundary). |
| 2026-06-15 | D1/D2 cartography + D2 execution | 5-agent read-only cartography re-derived both cards vs reality. **D1** (break Core→Orchestration.Abstractions): the inversion is carried by ONE type (`KoanServiceAttribute` + 2 enums), but the prescribed target (ARCH-0077, still *Proposed*; `sequencing.json` correction #3) is to lift orchestration metadata OUT to per-connector Aspire hosting packages on its OWN migration track — NOT relocate into the kernel — so D1 deferred pending an architect call. **D2**: only items 1+5 are genuinely dead; item 4 moot (PGVector C13 + KoanContext gone); items 2/3/6 are NOT dead (item 2 legacy attrs read by 2 unpredicted live consumers — ProjectDependencyAnalyzer + ComposeExporter; item 3 SelfOrchestration auto-activates in Development; item 6 Cli.Core is a 75-public-type published periphery package). | Executed D2 items 1+5 (markers + Koan0049A + tombstone IVT). **Shared-index race:** a concurrent session's commit (`6174b79b` weaviate) swept the staged `git rm` deletions into itself — net dev state is correct + green (deletions reachable at HEAD), provenance split; my edits in `61c7eaa9`. Lesson: stage+commit atomically under concurrent sessions. Items 2/3/6 under review for ARCH-0077 routing. |
| 2026-06-15 | E1×6 (registration consolidation) | Ran as a workflow (6 sequential folds + 6 parallel adversarial reviews); all 6 sound. The folds revealed the **auto-discovery (AddKoan) path was silently INCOMPLETE** vs the manual Add*: Sqlite/Postgres/SqlServer never registered IDataProviderConnectionFactory (DATA-0053 Direct sessions), SqlServer was missing the relational-materialization bridge configurator, Couchbase missing IAsyncAdapterInitializer/IAdapterReadiness — all latent Reference=Intent gaps now FIXED by folding the manual lines into the registrars. Mongo/Json were pure duplication. Two coordination notes: (a) the Sqlite fold over-reached and also removed the Json registration from the SHARED DataCoreRuntimeFixture (benign — final state consistent, Data.Core 130/130); (b) the Couchbase suite fails 1/8 on a PRE-EXISTING Docker.DotNet/Testcontainers version mismatch, orthogonal to E1 (filed X-couchbase-testcontainers). | One atomic wave commit `f1f810e7` (16 files, −164 LOC). Verified: build green; Bootstrap 23/23; Data.Core 130/130; Sqlite 3; Json 7; Postgres 8; SqlServer 18; Mongo 20; Couchbase 7/8 (pre-existing infra). All 6 E1 rows → done. |
| 2026-06-15 | E2/E3/E4/E7 (consolidation, sequential workflow) | Two delicate cards correctly STOPPED at real architect-forks rather than guessing (the STOP discipline working). **E2** — post-C7 only RabbitMq remains on V1, but it has no IServiceDiscoveryAdapter (V2 coordinator → localhost-fallback regression in compose/k8s), one call site is in DI-less `Describe()`, and V2 is public (rename = forbidden public-API change); needs an architect decision. **E3** — the static (coalesce-into-one) vs DI-registry (serialize + 5s timeout) singleflight primitives are genuinely different contracts (proven vs the registry's own spec); migrating the relational hot paths would double-DDL the unguarded EnsureCreated sites + add a timeout failure mode; needs an architect decision. **E7** review surfaced that the old `MessagingExtensions.Send<T>` reflection targeted `GetMethod("Send")` — a method NO proxy exposes (only `SendAsync<T>`) — so the non-queued send path was a LATENT ALWAYS-THROW; the cached-delegate rewrite is a bug-fix, NOT the "identical" the impl claimed (orchestrator re-derived via `git show` + `git grep`; the broken path was never test-exercised). | E4 (`75e79506`) + E7 (`0983d31e`) committed, verified green (Bootstrap 23/23; Cache.Coherence.Messaging 14/14; Jobs.Transport 1/1; S3.Mq builds). E2 + E3 → blocked, escalated to the architect with crisp options. |
| 2026-06-15 | E2 + E3 resolved (architect decisions) | The two escalated forks were decided + implemented (workflow: 2 impls + 2 adversarial reviews; both reviews concerns→remediated). **E2 = "author the adapter now"** → ADR-0087: new RabbitMqServiceDescriptor + RabbitMqDiscoveryAdapter (real AMQP health check), both V1 sites migrated, V1 retired, V2 suffix dropped. Reviewer MEDIUM: the boot-report Describe would do a blocking ~5s AMQP connect because both sites set RequireHealthValidation=true (V1 did none) → orchestrator set RequireHealthValidation=false at both sites (V1 parity, no boot stall; CanConnect stays the live runtime check). **E3 = "keep both, rename for intent"** → unify-premise rejected; the misnamed DI SingleflightRegistry→KeyedLeaseGate (Koan.Core.Concurrency), static coalescer kept + documented, zero-caller shim deleted; pure rename. | E2 (`1d0d4c04`, + ADR-0087) + E3 (`9c231982`, + CLAUDE.md / koan-cache-module.md doc-sync) committed; build green, Bootstrap 23/23, Cache (Sqlite+Redis) + Sqlite-relational suites green. **Low follow-ups (noted, not blocking):** docs/guides/aspire-integration-reference.md still cites the deleted V1 connection-resolver types (ARCH-0077-pending doc); no ARCH-0079 spec yet exercises the new RabbitMqDiscoveryAdapter through real AddKoan (Bootstrap covers its registration); a few historical ADRs (DATA-0057 / DATA-0095 / ARCH-0075) still cite the old Koan.Core.Singleflight namespace or the deleted relational shim. |
| 2026-06-15 | G2 (cut kernel dead strata) — partial | The STOP discipline corrected the card: 2 of its 4 named "expect zero consumers" deletions were NOT zero-consumer. Done (safe subset, `966ba5f5`): EntityTransferExtensions deleted (grep-zero-ref); KoanServiceActions/Events TRIMMED to referenced constants (the card's "trim + report" branch — Outbox/Canon/Translation removed, Health/Messaging kept); Koan.Core.Adapters README/TECHNICAL rewritten to existing types (ghost BaseKoanAdapter type-system removed). | **Deferred (not zero-consumer):** (a) FluentApi/** can't be unit-deleted — FluentApi/ServiceModels.cs co-locates the LIVE IServiceRegistry + ServiceLocator (consumed by KoanBackgroundServiceAutoRegistrar:34, KoanBackgroundServiceOrchestrator:74, ServiceRegistry:8); the dead fluent surface (KoanServices.Do/On/Query + ServiceBuilder) is cuttable only after relocating those 2 infra types out of the FluentApi folder — architect decision pending. (b) JsonPathMapper has a real test consumer (JsonUtilities.Canonicalization.Spec) + a DATA-0063 reference → kept (the card's "zero external consumers" premise was wrong). Build green; Core.Tests 179/179, Core.Unit 43/43. |
| 2026-06-15 | C20c · env | Mid-card, an unrelated Rust compiler filled the **F: drive to 100% (6.7 MB free)**, which (a) blocked the full-sln build with `IOException: not enough space` (zero compile errors) and (b) **corrupted obj ref-assemblies** for projects building when it hit — surfacing afterwards as `CS0009 Invalid metadata section span` / `CS0234 namespace does not exist` in ElasticSearch + Swagger (NOT C20c-caused; C20c only touched Web/Transformers). | User freed the space (drive back to 398 GB free). Cleared the corrupt artifacts via `dotnet build-server shutdown` + `-t:Rebuild` + a fresh restore-build → **full clean Koan.sln build 0 errors / 0 warnings**, confirming both C20c and that the corruption was purely disk-incident debris. Lesson: after a disk-full event, do a full Rebuild before trusting incremental build errors. |
| 2026-06-15 | C11 | The card's premise — "delete `PipelineAiExtensions.cs` IF zero Tokenize consumers" — is **STALE**. Re-derived: `.Tokenize` is the LIVE stream-pipeline DSL (extension over `Koan.Core` `PipelineBuilder<T>`), a *different system* from the inert `src/Koan.AI/Pipelines/**` class surface the card conflates it with. Real consumer: `samples/S5.Recs/Services/SeedService.cs` (in-sln); also documented in `ai-vector-howto.md §7` (status:current, all-examples-tested) — the same guide [[X-semantic-pipelines-retire]] repointed to. | Did the **safe subset** (matching the G2 pattern): deleted the verified-inert `Pipelines/**` (5 files) + the zero-caller internal bridge duplicate; **kept `PipelineAiExtensions.cs`** (the card's own "if consumers exist, STOP" fired). The full-sln build green is itself the proof — S5.Recs would not compile if `.Tokenize` were gone. Lesson restated: cut-card "dead surface" premises conflate distinct same-named systems; re-derive which type lives where before cutting. |
| 2026-06-15 | C19 | The card's step-8 precheck ("ZERO Koan.Web/AspNetCore usages in Koan.Media.Core") held for Core's *own* code, but missed a **transitive** consumer: `Koan.Media.Web` (AddKoanControllersFrom / Koan.Web.Extensions) reached `Koan.Web` *through* `Koan.Media.Core`. Cutting Core's ref broke Web's build (CS0234). Also: 2 ripples the card didn't name but the cut entails — the `MediaWebOptions.OutputCache` property (type of a deleted options class) and the `CaptureReplayCache : IMediaOutputCache` test (a test *of* the deleted shim). And 3 live docs advertised the deleted public surface. | Root fix, not revert: declared `Koan.Web` directly on `Koan.Media.Web.csproj` (the real consumer owns its dependency); Core no longer carries the unused ref (card intent preserved). Removed the entailed property + obsolete-shim test. Swept the 3 docs (+ MEDIA-0004 ADR status note) so no ghost-API reference remains. Executed by a delegated agent, then orchestrator-reviewed (diff + independent build/Media/docs-lint re-run). |
| 2026-06-15 | C0 | Card lists 19 tombstone directories to delete (src/Koan.Data.Lucene, Koan.Cache.Adapter.Memory, Koan.Jobs.Core, Koan.Flow.Core, Koan.Context, Koan.Canon.Core, Connectors/Canon, the Canon/Vector/AI orphan test trees, and 8 stale samples). Re-derived empirically: **all 19 are already absent** — removed earlier by the agyo reorganization, the Canon rebuild, and the C-series cuts. The card's premise is fully stale. | Marked C0 `obsolete` (no files to remove). Build was already green (E8a sln build); no action taken beyond the ledger. Confirms the standing lesson: re-derive cut-card premises before acting. |
| 2026-06-15 | H2 (architecture) | The card's whole approach — "compile every doc snippet, fix the ones that fail" — rests on a category error: doc snippets are NOT standalone programs (~95% are fragments by nature). The compile-everything / `validate:skip`-the-exceptions (opt-OUT) design made the COMMON case the exception you must annotate and forced the harness to model every way docs differ from code. I band-aided symptoms for several iterations (CS0103 undefined locals → rich usings; builder snippets → top-level detection; sibling types → per-doc type-context, which cascaded CS8803→CS0246 and needed a poison guard) — an arms race the user correctly halted: *"you're fighting symptoms, evaluate the architecture."* | **Inverted to OPT-IN** (`b7a82698`): default prose-grade; compile only author-marked `<!-- validate -->` complete examples. Eliminates the arms race, makes the gate honest (the marked set is the contract), makes the sweep cheap. Kept the Phase-1 wrapping for the marked examples; seeded README + quickstart (4 blocks green across 78 docs). **Lesson: when a gate's failures are dominated by an artifact of the harness's MODEL rather than the thing under test, fix the model — don't chase the symptoms. A docs-code gate should validate what's ASSERTED complete, not police teaching fragments.** |
| 2026-06-15 | X-f2-failure-coverage | The card's hard part (the F2-data-core `ConfigWarning`) was deferred precisely because the static `KoanLogScope` caches its per-scope `ILogger` on first host boot, so a unit test's attached factory never takes effect (verified by reading `KoanLog.KoanLogScope.ResolveLogger`). Also: `Koan.Core` did not grant IVT to `Koan.Tests.Data.Core`, and `ConfigurationConstants` is internal. | Built the proper seam (not a flaky capture, not a stopgap): an internal `KoanLog.TestSink` at the single `Write` chokepoint that mirrors every emission regardless of logger/factory state — reusable for ALL future KoanLog-emission tests — plus the IVT grant; used literal config keys to avoid the internal `ConfigurationConstants`. Scoped the card to the named/deferred data-core slice (done + mutation-verified); Sqlite/Web/Storage failure-path tests remain (each needs provider-specific fault injection — the seam is now in place for the log assertions). While verifying, isolated a PRE-EXISTING flaky concurrency test (`VectorConcurrencySpec`, fails 1-2/run independent of this change — proven by running the suite with my spec excluded). |
| 2026-06-15 | X-couchbase-testcontainers | The card (echoing the E1-couchbase finding) claimed a "Docker.DotNet/Testcontainers version mismatch SPECIFIC to the Couchbase test project." Re-derived empirically — false on both counts. (1) Versions: `dotnet list package --include-transitive` shows the Couchbase + Postgres connector tests resolve the SAME `Docker.DotNet 3.125.15` + `DotNet.Testcontainers 1.7.0-beta.2269` (shared via Koan.Testing) — nothing to align. (2) Specificity: the `MissingMethodException…CreateClient()` is identical in `PostgresContainerFixture` and `CouchbaseContainerFixture` — both deliberately catch it + fall back to `docker run` CLI; it's the universal failure mode of the ancient 1.7.0-beta stack on net10, a WARN logged before the (working) fallback. The E1 observer mistook this WARN for the cause of the 1 failing Couchbase test, which is really cluster-init/networking. | No code change (a version bump would be a verified no-op). Marked `obsolete`; re-pointed the real residual to the existing "Couchbase Testcontainers networking" backlog (live-Couchbase debugging) and filed **X-testcontainers-modernize** (T3) for the fleet-wide migration off the dead 1.7.0-beta stack → Testcontainers 4.x (removing the MissingMethodException + CLI-fallback machinery). Lesson: a "version mismatch" attribution is cheap to falsify (`dotnet list package`) — do it before bumping. |
| 2026-06-15 | X-FluentAssertions | Re-derived the husk scope empirically: the FA v6 API survives in exactly 2 excluded projects — `S16.PantryPal.Tests` (3 sites) + `S7.Meridian.Tests` (1 site). The card's "net8.0 S16.PantryPal.Tests" framing was stale: the project is now net10.0 on AwesomeAssertions 9.4.0 and its only blockers were the 3 FA renames + 1 CS1997. `S7.Meridian.Tests` has the FA rename too BUT its `EmbeddingCacheTests` references deleted APIs (`IEmbeddingCache`/`CachedEmbedding`/`IDocumentStorage`, grep-zero in src) — rot beyond this card. | Fixed + re-added S16.PantryPal.Tests (11/11 green); left S7.Meridian.Tests excluded + UNTOUCHED (an unverifiable partial FA edit to a project that still can't compile is worse than a clear note) — flagged for a future sample-modernization card. Tooling gotcha: `dotnet sln add` defaulted to a path-mirroring solution folder named "S16.PantryPal" that collided with the same-named API project (MSB5004) → `--in-root` is the fix. |
| 2026-06-15 | H9 | The card's 3 items were re-derived empirically: **item 1 (render a MODULES-FAILED block from the F1 failure channel) was ALREADY DONE** — F1 shipped the `ModuleFailure` record, `RegistrySummarySnapshot.ModuleFailures`, `RecordFailure()`, and the render at `KoanConsoleBlocks:65-76` (pinned by `ModulesFailedRenderSpec` + the lenient-boot integration specs). The card's line numbers were also stale (`EmitAssemblySummary` is at `:187-242`, not `:151-170`; `KOAN_VERBOSE_ASSEMBLIES` already gated the per-assembly DETAIL listing — only the summary line + JSON were still unconditional). | Did the genuine remaining subset: item 2 (gate the still-unconditional `ASSEMBLIES\|loaded=` summary + the `assembly-scan` JSON behind the verbose flag) and item 3 (the probes-pending health-line race). Verified item 1 unchanged + still green. Pattern repeat: an H/DX card's "render X" can already be satisfied by an earlier card (F1 here) — re-derive before implementing. |
| 2026-06-15 | G2 FluentApi fork resolved | Architect approved the relocate-then-cut. Surprise that simplified it: ServiceModels.cs already declared `namespace Koan.Core.BackgroundServices` (not `…FluentApi`), so relocating the live IServiceRegistry + ServiceLocator into the new BackgroundServices/ServiceRegistry.Contracts.cs was a PURE FILE MOVE — zero API change, no consumer using-edits. (Aside: the user first wondered if FluentApi was a wrapper of a relicensed lib with an "Awesome*" fork — that's AwesomeAssertions/FluentAssertions, the unrelated test-assertion lib; this FluentApi is homegrown dead code.) | Dead fluent DSL deleted (KoanServices.Do/On/Query + ServiceBuilder + IService*Builder + the 4 fluent-only DTOs; the two outside "matches" were false positives — KoanLog.ServiceInfo + DescriptorModel.ServiceHealth), −449 LOC, `0b890f9e`. JsonPathMapper stays (not dead). G2 fully done; build green, Core suites 179/179 + 43/43. |
| 2026-06-17 | E8c | The card (echoing `04-recommendations.md` §6) framed Redis's `FilterSupport.Full` as dishonest ("stop advertising Full for scan-all, **or** add cursor paging + TTL, **or** demote"). Re-derived empirically — the **premise is STALE**. `FilterSupport` models operator-correctness, NOT pushdown efficiency (`FilterSupport.cs:8,27` — `Full` is explicitly "in-memory / oracle adapters that evaluate the AST directly"); the InMemory reference adapter (DATA-0081) declares `Full` identically; on the entity path a residual is an in-memory floor (`FilterPushdownCoordinator.Finalize` via `Data.cs:99-101`), so declaring `None` would NOT error — it would route the SAME in-memory filtering through the coordinator (behaviorally identical, but inconsistent with InMemory/Json and contradicting the model). The "hard error on residual" warning is the VECTOR path only (`FilterSplitter:76`). The genuine gap was native TTL (Mongo declares `Retention.TtlIndex`; Redis didn't). | STOPPED and surfaced the correction to the architect BEFORE implementing (the framing the architect had *just approved* was itself wrong) → architect re-chose "keep Full + add native TTL + fix docs" (ADR DATA-0101). Kept `Full`; implemented TTL; corrected the stale framing in SURFACES/cartography/recommendations. Adversarial code-review then caught a REAL non-atomic SET+EXPIREAT crash-leak in the first impl → fixed via one atomic `SET PX` in a shared `WriteWithTtlAsync` (both write paths); REJECTED the review's "subtract past-expiry from the upsert count" as Mongo-divergent (upserted ≠ currently-live). Lessons: (1) an assessment doc's own "gap" can rest on a misread of the framework's capability SEMANTICS — re-derive the semantics, not just the mechanics; (2) a frontier card's already-approved framing is still falsifiable mid-flight — surface it. |
| 2026-06-17 | G1 | The card's core premise (OTel wiring localized to one file, `AddKoanCore` never calls it, health/probes stay, a registrar re-enables on reference) was CONFIRMED — but two things were off + a third surfaced mid-impl. (1) "Move `ObservabilityOptions` too" is WRONG: it's a pure POCO type-referenced by `AppRuntime` (in Core), so moving it creates a `Core↔Observability` CYCLE — only the SDK wiring (the 5 packages + `ServiceCollectionExtensions.cs`) can move. (2) The opt-in benefit was only HALF-real: `Koan.Web` carried 3 OTel package refs with ZERO code usage, so web apps would still transitively pull OTel. (3) Big surprise during impl: removing OTel from Core also removed the `Microsoft.AspNetCore.App` + `Microsoft.Extensions.Http` FrameworkReferences that `OpenTelemetry.Instrumentation.AspNetCore`/`.Http` provided TRANSITIVELY through Core — **4 projects** (Koan.Data.AI, Koan.Cache, Koan.Data.Backup, Koan.AI.Connector.LMStudio) were FREE-RIDING on that leak and stopped compiling. | Architect chose full opt-in → stripped Koan.Web's dead refs too. Kept `ObservabilityOptions` in Core (corrected the card). Fixed the 4 free-riders with MINIMAL-correct deps (`Microsoft.Extensions.Diagnostics.HealthChecks` for the two health-check projects, `Microsoft.Extensions.Http` for the IHttpClientFactory consumer, one genuine `FrameworkReference Microsoft.AspNetCore.App` for the endpoint-mapping project) — NOT framework-on-all, staying true to the diet. Adversarial review then caught a real double-OTel-pipeline trap (registrar + a still-allowed explicit `AddKoanObservability()` call stack `WithTracing`/`WithMetrics`) → added a marker-based idempotency guard + honest docs. ARCH-0079 spec boots real `AddKoan()` and asserts the registrar wired a TracerProvider (mutation-checked). **Lessons: (1) a "move X" in a package-split must check whether X is type-referenced from the SOURCE package (cycle risk); (2) extracting a package can REMOVE transitive FrameworkReferences that unrelated projects silently depend on — only a full-sln build surfaces the free-riders; fix each with its minimal real dep, not the whole framework.** |
| 2026-06-17 | E8b | The card framed it "CAS + FastRemove, or ADR note why not (it has real transactions, so CAS is implementable)." Re-derived: Couchbase has NATIVE version-CAS (`ReplaceOptions.Cas`) + real transactions → CAS is cleanly implementable (the hint held — but native CAS is cleaner than a transaction). FastRemove is the genuine fork: `RemoveAll` already issues a server-side N1QL `DELETE` for all strategies; the only TRUNCATE/DROP analog (drop+recreate collection, like Mongo) pays a multi-second `CREATE PRIMARY INDEX` warmup (the adapter waits up to ~15s for online) → SLOWER than the existing DELETE in the common case + brief index unavailability; bucket-flush needs admin. | Architect chose CAS-implement + FastRemove-decline. Implemented `ConditionalReplaceAsync` (native CAS, no transaction); declared `Write.ConditionalReplace`. ADR DATA-0102 declines FastRemove with the index-tax reasoning. **Self-review caught a real latent bug**: I derived the key with `GetKey(model.Id)` BEFORE `PrepareEntityForStorage` (which reformats a GUID id to dashless "N"), but Upsert prepares FIRST — so a dashes-format claim id would target the wrong key and silently return false; reordered to match Upsert (the happy-path test passed regardless because `saved.Id` is already N-format, hiding the bug). Verified against REAL Couchbase (the suite's known 1/8 Testcontainers-networking flake didn't block — the container works via the docker-run fallback): 1/1, mutation-checked. Also: `CasMismatchException` is in `Couchbase.Core.Exceptions`, NOT `.KeyValue` (grepped the 3.8.1 package XML). Lessons: (1) prefer the store's native CAS over a transaction when it exists; (2) a passing happy-path test can hide an id-format bug — match the sibling write method's key-derivation order exactly. |
| 2026-06-17 | S3 | The card said "AI pillar consolidation 19→~8: fold Orchestration+Agents, demote Training/Eval/Compute, keep Prompt, decide Models+HuggingFace, park ZenGarden — migrate AI capability decls to ARCH-0084." Two-round deep eval (surface map of 17 projects, then a per-project migration dossier reading Koan + Agyo + zen-garden) found the premise STALE: the AI "vertical" is **real and tested now** — a new sample `S18.Prism` + a real AI test suite (`Koan.AI.Integration/EndToEnd/Eval/Models/Review.Tests`) exercise Orchestration/Compute/Eval/Review/Models; `Koan.AI.Review` isn't even in the card. So the card's fold/demote/cut would LOSE real capability. The user pushed the right reframe: **Agyo** (AGYO-0001: "not core, yet valuable → sibling repo"). | Reframed S3 as an **AI pillar DISSOLUTION** (ADR ARCH-0089): Koan keeps the 8-project entity-AI core; agentic/RAG/quality → **Agyo** (Orchestration+Agents fold `Agyo.Rag`; Eval, Review standalone); ML substrate → **Zen Garden** (Compute cut-in-favor-of, since ZG already has hardware discovery; Models port w/ `ModelEntry` ORM decouple; HuggingFace follows); archive ZenGarden-connector + Training (vaporware). The "19→~8" is hit by MIGRATION, not cutting. Dossier verified **zero core→vertical reverse-deps** (clean extract) and that **Zen Garden is Rust** (the ZG half is a re-implementation, not a lift). Delivered the card's mini-plan + ADR; execution is a follow-on 3-repo program → card `X-ai-dissolution-migration`. **Lessons: (1) a consolidation card premised on "X is dead" can be flat wrong if a sample/test surface materialised since the card was written — re-derive vitality before folding/cutting; (2) "real but not core to Koan's identity (data/web/cache/jobs/mcp/auth/storage)" is a MIGRATE-to-sibling signal, not a keep-or-cut one; (3) read the destination repos — ZG already owning hardware discovery turned a "port" into a cheaper "cut-in-favor-of", and ZG being Rust reshaped the whole effort.** |
| 2026-06-17 | E5 | The card framed a mechanical "swap AuthController → the dead `AddKoanWebAuthAuthentication` handlers, delete ~300 LOC + add a spec." A 5-part re-derivation found it materially deeper: (1) the handler path was NOT at behavioral parity — it dropped `UserInfoMapper` role/perm/extra-claim mapping (an **authz regression**) + the `IExternalIdentityStore.Link`, both of which the hand-rolled callback did; (2) the provider set is composed at RUNTIME (config + `IAuthProviderContributor` defaults + **ownerless config ids**) but ASP.NET handlers register statically, and `AddKoanWebAuth` has no ordering vs the auth connectors → a `BuildServiceProvider()` at registration would miss contributed providers; (3) the old callback had a latent `sub=null → SHA256("")` account-collision bug in a swallowing catch; (4) SAML was **7** dead fields not 6. Cleanup half (SAML excise + merge dedup + the whole `IKoanAuthEventContributor` retirement) landed first (`1540fe05`, `244681c2`); the security-critical swap was done under explicit break-and-rebuild authorization. | Architecture chosen via a **research + adversarial-decision workflow** (3 specialists: ASP.NET-internals · prior-art · security-threat-model) → **Option A: framework dynamic scheme registration** (ADR WEB-0071) — the only option that handles ownerless config providers + may legally read the scoped registry; zero connector blast radius; it is Duende's production "Dynamic Providers" pattern. Seeder (`AuthSchemeSeeder` in the existing startup filter) uses the empirically-verified recipe: manual `*PostConfigureOptions.PostConfigure` BEFORE `IOptionsMonitorCache.TryAdd` (TryAdd bypasses post-configure → handler NPEs at challenge), then `AddScheme`. Parity hooks ported into the handler events; fail-closed on missing sub. Test provider extended into a mini OIDC IdP (discovery + JWKS + ES256 id_token + nonce). **Three real bugs surfaced + fixed during the e2e bring-up** (the new real-Kestrel OAuth2+OIDC round-trip spec — the one that would have caught OIDC-501): (a) the correlation/nonce cookie is `SameSite=None+Secure` → never sent over the http loopback ("Correlation failed") → relaxed to Lax+SameAsRequest for http dev providers (prod https keeps None+Secure); (b) OIDC userinfo must return `sub` (IDX21345) — the Test userinfo returned only `id`; (c) the Test endpoint mapping is opt-in (`TestProvider:Enabled`), SEPARATE from the Development provider auto-enable (SEC-0003 split). Full-sln 0 err; e2e(2)+dispatcher(18)+bootstrap-auth(4)+Security.Trust(12) green; role-mapping mutation-verified. **Lessons: (1) "swap to the maintained handler" hides application-policy controls the handler structurally does NOT own (claim mapping, identity link, return-url allow-list) — port them into the handler events or they silently regress; (2) when an architecture choice is genuinely uncertain (3 viable options), a research+adversarial-decision workflow that grounds the API facts empirically is worth it before writing security-critical code; (3) maintained-handler dynamic registration has two killer traps — PostConfigure-before-cache-seed, and SameSite=None+Secure cookies over http — both invisible until a REAL round-trip e2e exercises them.** |
| 2026-06-17 | E6 | Unusual for a frontier card: the premise was ALREADY a deep adversarially-corrected re-derivation (`es-os-merge.json` — measured normalized diff, file:line evidence, 5 named drift bugs). So my job was VERIFICATION, not re-design. Empirically confirmed it still holds: docker UP (the ES+OS VectorAdapterSurface Testcontainers matrix — the regression net — is runnable); drift bug 1 (OS factory `CanHandle` cross-claiming provider `elastic` at the same ProviderPriority(20)) live at line 38; both repos read in full = byte-identical skeleton with the seam being EXACTLY 3 members + error-prefix + telemetry-class; OS genuinely missing ExportAll/GetCount/IndexStats. The card explicitly named the Testcontainers matrix as the regression net. | Delegated the (large, mechanical, well-mapped) extraction to a focused high-effort agent with a precise spec built from my reading, run in the background; then REVIEWED the diff myself (the two dialects + the shared base are byte-faithful extractions of the original query-translation; factory rewiring correct; all 5 drift-bug fixes grep-confirmed: OS `elastic` clause gone, `DefaultTopK/MaxTopK`=0, OS `sourceKey`=7=ES, `search` alias dropped from both, ApiKey kept-with-note) and **independently re-ran BOTH Testcontainers suites** (ES 25/4skip, OS 25/4skip — identical, OS now exercising the 3 new ExportAll/Stats specs through the shared base = the gap-closed proof). Committed `6780772c`; DDR DATA-0103 supersedes DATA-0097 §6. The agent also added the 3 ExportAll/Stats TestKit specs (the matrix had declared `SupportsExportAll` but no spec consumed it — so the OS gap was previously unprovable). **Lessons: (1) when a card's premise is itself an adversarial re-derivation with measured evidence, re-derivation collapses to a few empirical spot-checks (docker-runnable? bug still live? seam exactly N members?) — don't re-do the analysis; (2) a delegated adapter-refactor is safe to trust ONLY through its regression net — here the real-container ES+OS matrix re-run by the orchestrator, not the agent's self-report, is what closes it; (3) a capability flag with no consuming spec (OS `SupportsExportAll`) is a silent gap — the fix must ADD the consuming spec, or "closed" is unprovable.** |
| 2026-06-17 | X-data-direct-fold | The card (ARCH-0090 §1) said "move ~513 LOC, register at `ServiceCollectionExtensions.cs:55`, feasibility confirmed." Re-derived empirically — the premise was STRONGER, not weaker: `Koan.Data.Direct` had **zero `ProjectReference`s anywhere in the repo** (only `Koan.sln` listed it), a true orphan — its `KoanDataDirectModule` was never even loaded, so `.Direct()` threw "AddKoanDataDirect() required" by default everywhere. The 3 connector matches for "Koan.Data.Direct" (SqlServer/Sqlite/Postgres registrars) were DATA-0053 provenance **comments**, not assembly refs. `Koan.Data.Core` already had Newtonsoft transitively (2 existing files) → no csproj package add. Direct had **zero test coverage** repo-wide. | Folded as specified (registration landed at `:55`, next to `AddSingleton<IDataService>`); made the impls `internal` (no external type consumer — verified via the csproj grep); flattened `ConfigurationConstants` into `Koan.Data.Core.Direct` (dropped its `Infrastructure` sub-namespace) to avoid an `Infrastructure` name-resolution clash with Core's own. Added the missing guard (`DirectDataAccessSpec`, mutation-verified) so the now-default-on feature has a tripwire. **Lessons: (1) a "feasibility-confirmed" fold can still be UNDER-stated — an orphan package (zero ProjectReferences) means the feature was already dead-by-default, making the fold a strict capability ADD, not just a move; (2) fold a feature's FIRST test alongside the move when the source package shipped untested — the behavioral delta (default registration) is exactly what needs a mutation-checked guard.** |
| 2026-06-17 | S4 | An ADR-decision card (4 public-surface trims). A 4-reader empirical surface-map flipped **2 of 4 premises**: (3) `Vector<T>`/`VectorData<T>` "merge" is STALE — they are a deliberate facade/engine split, and `Vector<T>.IsAvailable` / `Search(float[], VectorRetrieveOptions, ct)` are **frozen name-bound reflection targets** in the AI pillars (`ChainExecutor` RAG + `EntityToolGenerator`) → a literal merge silently breaks RAG + tool-gen at runtime with no compile error (KEEP); (4) the Vector-workflow "cut" was right but the framing wrong — it is not dead, it is wired into `VectorData.Save` + unit-tested but **never *activated*** (no sample registers a profile; speculative generality). The other two: the auth connectors are post-E5 ~77-LOC clones (consolidate), and Roles is foldable but the fold would couple the data-store-agnostic auth pillar to `Koan.Data.Core`. | ADR **ARCH-0090** records all 5 decisions; the **architect decided each fork**: Data.Direct fold-hard, connectors shrink-but-keep-packages (preserve Reference=Intent), Roles keep-separate (preserve Auth's data-independence), Vector-workflow cut, Vector facade keep. Landed: connector-shrink (`b6fb0c19`, new `AuthProviderDefaults` helper) + vector-workflow cut (`55135939`, −8 files; full-sln 0 err + Data.Core vector 41/42, the 1 fail the pre-existing `VectorConcurrencySpec` SQLite flake). Data.Direct fold-hard (a cross-assembly move; feasibility confirmed — Direct refs only Core, register at `ServiceCollectionExtensions.cs:55`) deferred to card **X-data-direct-fold**, not rushed at the tail of a long session. **Lessons: (1) a "merge two facades" premise can hide a deliberate two-tier split + name-bound reflection contracts — grep for reflection targets before collapsing a public type; (2) "never used" has two flavours — dead (cut freely) vs wired-but-never-activated (a public + tested surface → cut still needs the external gate); (3) the C9 external-consumer gate applies to EVERY Periphery-package retirement — in-repo zero-consumers ≠ safe-to-delete.** |
| 2026-06-17 | X-entitycontroller-query-parse | The card (flagged `stopped-ambiguous` by F2-web) said POST `/query` swallows parse failures "while the sibling PATCH returns BadRequest → fix is BadRequest, needs a decision." Re-derived: the bare `catch {}` swallowed `JToken.Parse` + the `(int)page`/`size` casts → DEFAULTS at 200; worst case a non-object body **drops the filter → UNFILTERED results**. But the framing was incomplete — the GET-LIST sibling is itself LENIENT (uses `int.TryParse`: non-numeric page/size → default; it 400s ONLY on a negative page), so "BadRequest on every bad input" would make POST *stricter* than GET — a NEW inconsistency in the other direction. | Surfaced the real fork via AskUserQuestion → architect chose **GET-parity**: 400 only on a non-object body + negative page; non-coercible page/size ignored like GET; new non-throwing `TryCoerceQueryInt`; bare catch removed. `QueryParseSpecs` (5, real WAF + in-memory store), mutation-verified. Landing them exposed + fixed **2 pre-existing latent Sort.Tests infra bugs**: missing `GenerateProgramFile=false` (CS7022 top-level-Program vs test-SDK-Main, fatal under TWAE — surfaced only when my Koan.Web change forced a full rebuild; the fix WellKnown.Tests already had) and a cross-class parallelism race on the suite's process-global in-memory store (adding a 2nd test class exposed it → assembly `DisableTestParallelization`). **Lessons: (1) "swallows X → return BadRequest" can be too blunt — check what the SIBLING path actually does and match it (GET was itself lenient) rather than inventing a third behavior; (2) adding a 2nd test class to a suite with process-global state needs explicit serialization — single-class suites hide the coupling; (3) a referenced-project edit forcing a full rebuild can surface a latent CS7022/TWAE entry-point error that incremental builds had been skipping.** |
| 2026-06-17 | C20a follow-up (Swagger test net) | C20a merged Swagger into `Koan.Web.OpenApi` + added the `Koan:OpenApi:EnableUi` UI gate but shipped it with NO test ("guarded only by review, not CI" — flagged in C20a's own row). No existing test project hosted `Koan.Web.OpenApi`. | Stood up a dedicated `Koan.Web.OpenApi.Tests` WAF suite (modeled on the WellKnown/Sort harness — marker `.sln` + content-root attr + `GenerateProgramFile=false`), **added it to `Koan.sln`** (root-level like WellKnown.Tests) so it runs in the canonical CI pass, and pinned the gate with 5 specs: EnableUi false / legacy-false → `/swagger` 404; EnableUi true / default-outside-prod → 200; EnableUi-true-overrides-legacy-false. Mutation-verified (force gate always-on → both disable specs fail). Key enabler: the `EnableUi` branch short-circuits BEFORE `KoanEnv.IsProduction` (process-sticky), so both gate directions are testable in ONE environment via `WithWebHostBuilder().UseSetting(...)`. **Incidental find: `Koan.Web.Sort.Tests` is NOT in `Koan.sln`** (WellKnown is) — which is why its latent CS7022 (fixed under X-entitycontroller-query-parse) was never caught by the sln build. **Lessons: (1) a config-gate that short-circuits before an env check is cleanly testable in a single process — no env juggling; (2) `WithWebHostBuilder().UseSetting` overrides compose correctly even with a custom `CreateHost` override, as long as the base config doesn't set the same key.** |
| 2026-06-18 | B2 | The recipe's step 2 said "create `pr-gate.yml` with INLINE steps (`dotnet build`, `dotnet test`, then `docs-lint.ps1` + `validate-code-examples.ps1`)" — but step 1 said to READ `green-ratchet.ps1`, which already IS "the single merge gate" composing EXACTLY those 4 legs (build / test `--no-build` / docs-lint / doc-code), exit 0 only if all pass. Re-inlining would duplicate it → CI-gate-vs-local-gate drift. Premise itself CONFIRMED accurate (rare — most premises this campaign were stale): the workflows really are disabled noops. | Wired `pr-gate.yml` to INVOKE `green-ratchet.ps1` rather than re-inline — single source of truth, CI==local. Verified the 3 fast legs GREEN locally + the test-leg `--no-build` mechanics on an in-sln suite (5/5). **Design note surfaced: ubuntu-latest ships Docker, so the card's "container suites self-skip without Docker" is FALSE in CI — they RUN (slow; the dead 1.7.0-beta Testcontainers stack flagged by X-testcontainers-modernize may flake). The gate is correct; that flakiness is pre-existing infra debt the gate will now SURFACE.** **Lessons: (1) when a card says "read script X" in step 1 then "inline its behavior" in step 2, the intent is usually REUSE X — a canonical gate should be the single definition, not re-implemented in YAML; (2) "self-skips without Docker" breaks on hosted runners that ship Docker — a full-matrix PR gate is slower and exposes infra debt, which is the gate doing its job.** |
| 2026-06-18 | B3 | The card framed 3 tasks citing `testsBuild.json`: (1) rework `release-on-main.yml` NBGV-native, removing the "versions.props + Update-Versions.ps1 path it STILL drives"; (2) read `release-from-dev.ps1` + `Initialize-NbgvBaseline.ps1` as "the newer intended path"; (4) fix `packaging/*.nuspec` metadata. TWO premises STALE: (a) `release-on-main.yml` is ALREADY NBGV-native — the evidence JSON described the OLD workflow; `build/versions.props` + `Update-Versions.ps1` are gone (only a `Directory.Build.props` comment remains). (b) `release-from-dev.ps1` is DEPRECATED (hard `exit 1`), not the newer path. And the metadata bug is BIGGER than "packaging/*.nuspec": the stale `sylin-labs` URLs live at the ROOT in `Directory.Build.props` (stamps every Sylin.Koan.* package) + 4 PACKED src READMEs. | Did the genuine remaining work: added the missing `dotnet test` release gate; fixed `net9.0`→`net10.0` + `sylin-labs`→`sylin-org` across the metadata ROOT (`Directory.Build.props`) + 2 nuspecs + 4 packed READMEs — not just the 2 files the card named. Preserved "Sylin Labs" authors (canonical, not a URL). Verified by PACKING a real package and reading its emitted nuspec URLs (gold-standard — the metadata change is pack-time-only, so a build wouldn't exercise it). **Lessons: (1) a release/versioning card's premise is doubly likely to be stale — the pipeline evolves between the assessment snapshot and execution; re-derive from the ACTUAL workflow file, not the evidence JSON; (2) "fix the nuspec metadata" usually means fix `Directory.Build.props` (the root that stamps 99% of packages) — the nuspecs are only the hand-written meta-packages; (3) verify pack-time metadata by PACKING + reading the emitted nuspec, not by building.** |
| 2026-06-18 | H6 | The card ("sweep ALL docs+skills+sample-READMEs; normalize Upsert/Delete/Where-used-as-canonical → Save/Remove/Query; docs only") is a judgment-heavy sweep: the synonyms appear in 100+ files but MOST are legit (LINQ `.Where(`, `UpsertMany`, HTTP DELETE, query-builder `q.Where`). Scoped to 29 in-scope candidate files (excluded archive/assessment/decisions/JOBS). | Ran a guarded multi-agent workflow (per-file conservative normalizer → fresh-eyes adversarial over-reach reviewer). 20 candidate rewrites across 8 files; **the workflow hit API RATE-LIMITING that killed ~19 review agents (the safety net) + 1 normalizer** → so the orchestrator BECAME the reviewer: reviewed the full aggregate diff line-by-line, confirmed 19 rewrites correct, REVERTED the 1 real over-reach — `canon/index.md`'s `Document.Where(...).Pipeline()` (the streaming Semantic-Pipelines DSL, parallel to `AllStream().Pipeline()`; `Query()` materializes a list and can't be stream-`.Pipeline()`d) — and manually confirmed the rate-limited sample README needed 0 changes. Final: 7 files, 21 line-replacements, docs-lint + snippet-lint green. **Lessons: (1) a verb-normalization sweep is mostly a LEAVE exercise — the synonyms are real APIs/LINQ far more often than the entity verb; per-file agent w/ strict bias-to-leave + adversarial reviewer is the right shape; (2) when a workflow's review stage is DEGRADED (rate-limiting), the orchestrator MUST run the safety net itself before trusting the edits — never commit unreviewed agent edits; (3) the trickiest over-reach is a same-named token in a DIFFERENT DSL (`Where` as a stream-filter vs the entity query verb) — what the downstream call needs (`.Pipeline()` wants a stream) decides, not the token.** |
| 2026-06-18 | H4 (rest) | After the architect approved the `data.md` frontier format ("perfect"), the remaining 7 cards were T2 template work. mcp + jobs are under concurrent-work boundaries — a docs card ABOUT them is fine, but their source is in flux. | Authored `web.md` by hand; the other 6 (cache/jobs/vector/ai-data/mcp/auth) via a guarded Ultracode workflow (per-pillar research→author→adversarial-review; 12 agents, no rate-limiting this run). Orchestrator was the FINAL reviewer (the H6 lesson applied pre-emptively): read all 7 cards, re-grepped identifiers (ai-data's `Client` static, the Qdrant connector, mcp/jobs attributes — all real), confirmed jobs+mcp agents made ZERO edits under `src/Koan.Jobs`/`src/Koan.Mcp` (docs-only boundary held), and ran docs-lint (0 err — the authoritative link check). The agents showed strong discipline — OMITTING dead/experimental surface flagged in the evidence JSONs (vector's VectorWorkflow/IVectorFilterTranslator/VectorSchema) and sourcing every claim to file:line. Side-fix: `docs-lint.ps1`'s allowed-domains taxonomy was missing the real `jobs`+`mcp` pillars → added them. **Lessons: (1) once a frontier format is approved, a research→author→adversarial-review workflow templates the rest cleanly — but the orchestrator must STILL be the final reviewer (grep-confirm identifiers + run the authoritative lint), never the agents' self-verdicts alone; (2) a docs card about a concurrent-boundary pillar is allowed iff it edits zero files in that pillar's tree and sources only from current code; (3) when accurate metadata trips a lint taxonomy, EXTEND the taxonomy — don't mislabel the content.** |
| 2026-06-18 | H1 | Net-new (no templates existed — only an ORPHANED `scripts/pack-templates.ps1` pointing at a `templates/pack/*.nuspec` that never existed). Two forks architect-decided (AskUserQuestion): package-refs = META-packages (`Sylin.Koan.App`/`Sylin.Koan`, §8.5 concept budget); scope = both templates. Then a deeper finding: the meta-packages the user chose **are not published by anything** — `release-on-main` packed only `src/*.csproj`; the meta-packages exist solely as `packaging/*.nuspec`, unpacked by any flow. | Architect chose to **wire meta+templates into release** (close the pre-existing gap). Built the content-only template pack — had to **isolate it from the repo-root `Directory.Build.props`** (empty `templates/Directory.Build.props`+`.targets`): the root props' SourceLink/analyzer/compat-ranges/pack machinery triggers NU5017 on a content-only pack. Also fixed: the double `content/content/` nesting (template dirs moved up a level) + the meta-nuspecs' **broken icon ref** (`icon.png` didn't exist → the real `resources/image/0_2.jpg`, packed as `icon.jpg`) which had blocked packing. Added the release step (`nuget pack` the 2 nuspecs w/ version-from-leaf + compat dep-range + `dotnet pack` the template; `NuGet/setup-nuget` for the CLI). Deleted the orphaned script. Verified short of the actual publish: pack/install/instantiate/rename all work; a generated koan-web BUILDS clean against live src. **Lessons: (1) a "create the artifact" card can expose that the artifact's DEPENDENCIES aren't even shippable (the meta-package publishing gap) — verify the whole chain restores, not just that your files exist; (2) content-only NuGet packs (PackageType=Template) must be ISOLATED from a framework's root build props — SourceLink/analyzers/pack-rewrites fight content-only packing (NU5017); (3) verify a template by BUILDING a generated instance against live src, not just inspecting; (4) `$(pwd)` in git-bash yields `/f/...` which MSBuild-on-Windows can't resolve — use the drive-letter path in throwaway csprojs.** |

| 2026-06-18 | H8 | The card's sub-task 3 ("sweep skills for `0.6.3` pins AND stale package ids, e.g. `Koan.AI.Ollama → Koan.AI.Connector.Ollama`") was PARTIALLY STALE on the package-id half. Re-derived empirically (glob + grep the real csprojs): the AI-Ollama rename is genuinely done (`src/Connectors/AI/Ollama/Koan.AI.Connector.Ollama.csproj`), and the data connectors live under `src/Connectors/Data/` — but EVERY package id referenced across the 13 skills already resolves to a real project (`Koan.Core`, `Koan.Data.Core`, `Koan.Data.Connector.{Json,Postgres,Mongo}`, `Koan.Web`, `Koan.AI`). No skill references the old `Koan.AI.Ollama` id (or any Ollama package id; ai-integration's `"Type":"Ollama"` is config). The only real staleness was 3 version/date pins. | Authored the koan-jobs skill (modeled on koan-caching as instructed) — every API identifier grep-verified against current `src/Koan.Jobs`, then a 3-agent adversarial panel independently re-confirmed all of it (0 problems). Fixed the 3 pins; left the (already-correct) package ids untouched (no invented work). Skill→docs links use `../../docs/` to MATCH koan-caching (the prescribed model) even though that depth is one level shallow by strict path math — fixing it fleet-wide is out of H8's scope; flagged here. **Lessons: (1) a "sweep for stale ids" card can be half-stale itself — the rename it cites may be real yet absent from the target files; grep the actual csproj names before "fixing" anything, and don't manufacture edits the reality doesn't need; (2) for a hand-authored API doc, the orchestrator's own exhaustive grep + an independent adversarial panel is the right belt-and-suspenders — the panel surfaced exact file:line for every member, including ones I hadn't personally checked (`JobAction.OnFailure/Lane/MaxConcurrency`, the `JobOps<T>`/`JobStatics<T>` extension shapes).** |

| 2026-06-18 | H10 (genesis) | After H8, the user asked to evaluate the whole skill set's quality/feasibility. A 13-agent full source-accuracy audit (each skill's every identifier grep-verified vs current src, cross-checked by the orchestrator's own ghost-API scan) found **all 13 graded `refresh`** — good structure + right topics, but pervasive API drift: the *first copy-paste example* in most skills no longer compiles. Worst: mcp-integration (22, central `IMcpTool` example fully fictional), vector-migration (38, `EmbeddingCache` is sample-only sold as framework), relationships (42, ignores `[Parent]`/`Relatives()` — contradicts DATA-0072 Accepted), ai-integration (46, 7 high-severity). The drift is ONE event (recurring ghosts: `QueryCaps`/`HasFlag`→`CapabilitySet.Has(DataCaps…)`, `DataQueryOptions`→`QueryDefinition`, `[VectorField]`→`[Embedding]`, manual-FK→`[Parent]`, `.SaveAsync()`→`Save()`, `List<T>`→`IReadOnlyList<T>`). **Second-order finding: the rot also lives in the feeding GUIDES** (building-apis `IPayloadTransformer`, data-modeling `EntityLifecycleBuilder`, ai-vector-howto `EmbeddingCache`) — the skills faithfully copied ghosts from the docs; nothing compiled skill examples so it drifted invisibly. Feasibility: only koan-caching (82) + koan-jobs (just authored) are healthy; dir!=name for 12 of 14 (only those two match). | Spawned the **H10 program**: anchor skills to the H4 cards (API-truth) so they stop re-stating + rotting the API; realign the surface (5 data skills→1 card, auth card→0 skills, 5 uncovered pillars); add a permanent `<!-- validate -->` compile-gate + skills-lint so drift breaks the build. Orchestrator caught 2 agent errors during synthesis: `dir_equals_name` misreported `true` for data-modeling/entity-first (their dirs are bare — my own scan is authoritative), and one "migrated-to-agyo" flag was actually a DATA-0103 shared-core consolidation (agent self-corrected). **Lessons: (1) a DX surface with NO compile gate WILL drift silently — the gate is the deliverable, not the one-time scrub; (2) skills that re-state API inline inherit every downstream rename; anchor them to a single verified source (the card) and carry only the compile-gated canonical pattern; (3) the rot's root is usually upstream (the guide), so fix the feeder in the same pass or the skill re-rots.** |

| 2026-06-18 | H10 | The full DX/AX skill-set overhaul ran as a 6-phase, workflow-driven program (13 commits, ~30 subagents). The **compile-gate** (extended `validate-code-examples.ps1` over `.claude/skills`) proved the thesis by catching 3 real defects that would otherwise have shipped: quickstart's canonical block mixed a class declaration + top-level statements (CS8803); performance misused `QueryDefinition.WithSort(lambda)` (WithSort takes `IReadOnlyList<SortSpec>`; the typed builder is the Entity-level overload); and — the biggest — `CacheableAttribute`'s `init`-only TTL setters make its OWN documented `[Cacheable(…, AllowStaleForSeconds = N)]` syntax uncompilable (a framework bug the human audit + the authoring agent both missed). Two subagents also wrote their target dirs directly despite a read-only instruction. | Orchestrator was the integrator throughout: extracted agent output to files under control, ran the validate gate + skills-lint on EVERY skill, fixed the 3 compile defects, **reverted a cascading framework init→set attempt** (kept `src/` clean) and filed it as [[X-cacheable-attribute-init]], retired the folded dirs, and flipped skills-lint to `-Strict`. Final: 19 skills + 13 cards, validate sweep 22/22, skills-lint -Strict 0/0. **Lessons: (1) a docs-code COMPILE gate over a DX/AX surface is the single highest-leverage anti-drift control — it caught a framework bug 4 agents + the human audit all missed; (2) author skills from SOURCE under the gate, never from the (also-drifting) guides — and fix the feeders in the same program; (3) a self-contained canonical example must be pure-declarations OR pure-statements (CS8803) with real APIs + correct usings; (4) when a gate-surfaced fix cascades across a type hierarchy, file it as its own card with a test pass rather than rushing it mid-program; (5) subagents may write files despite read-only instructions — the orchestrator must extract + verify under its own control.** |

| 2026-06-18 | X-testcontainers-modernize | The card (inheriting X-couchbase-testcontainers' framing) scoped a **fixture-only version bump**: swap the 7 shared `*ContainerFixture` classes off the dead `DotNet.Testcontainers 1.7.0-beta` (+ `Docker.DotNet`) onto `Testcontainers 4.x`. Reality: that dead stack was load-bearing for an entire bespoke `TestPipeline`/`TestContext` DSL consumed by ~85 test projects still on xUnit **v2**; a fixture-only swap would have left a half-migrated harness with no path to v3 (the v2/v3 `Xunit.*` assembly split blocks coexistence). | Escalated to an architect-ratified **full harness redesign (ADR ARCH-0091)**: retire the bespoke DSL; migrate ALL non-fenced projects to xUnit **v3** on a new `Koan.Testing.Containers` (Testcontainers 4.x module builders) + the xUnit-agnostic `Koan.Testing.Hosting` (so fenced v2 Jobs and v3 suites share one `KoanIntegrationHost`); staged in waves, each **real-store-oracle-verified**. This session's close-out: migrated the last 3 in-sln MCP sample projects, deleted 40 bespoke type files (kept `Koan.Testing` as a Hosting shim for the FENCED v2 `Koan.Jobs.TestKit`), deleted 4 orphan dead dirs, rewrote `tests/README`; full `Koan.sln` build 0 err. **Lessons: (1) a "version bump" card can hide a whole-subsystem redesign — the dead dependency was structural, not cosmetic; (2) a v2→v3 migration is all-or-nothing per assembly but can be staged per-project IF a shared xUnit-AGNOSTIC host lets v2 and v3 coexist behind one boot path; (3) the real-store oracle (`dotnet test` vs the live backing store) is the only trustworthy arbiter — agent compile-green repeatedly hid faithful-migration breakage.** |

| 2026-06-18 | X-testcontainers-modernize (completion) | The card was marked `done` (`cbe75e5f`) claiming "ALL non-fenced/in-sln on v3" — but that was OVERSTATED. Re-derived by ENUMERATION (`grep -rl '"xunit" Version="2'` + Koan.sln membership), the migration waves had silently missed (a) every `Sdk.Web` `WebApplicationFactory<Program>` suite — AdapterSurface ×8 + OpenApi/Sort/WellKnown — and (b) 26 plain-v2 unit/integration suites. The WAF suites were stuck because xUnit-v3's OUT-OF-PROCESS runner and WAF both demand the assembly entry point, irreconcilable for minimal-API apps (WAF's HostFactoryResolver needs the minimal-API `Program` to BE the entry point). | Follow-on branch `feat/web-adaptersurface-v3` (3 commits): (1) dropped WAF for a direct in-memory TestServer host across all `Sdk.Web` WAF suites (`Host.CreateDefaultBuilder().ConfigureWebHost(UseTestServer)` + `AddKoan` + `AddKoanControllersFrom<T>` + empty `Configure`→`KoanWebStartupFilter`; AdapterSurface DRY'd into a shared `AdapterTestFactoryBase`), oracle-verified vs EVERY real backing store (`15cb4337`+`d2e8c1d9`); (2) mechanical `xunit`→`xunit.v3 3.2.2` + `OutputType Exe` sweep of the 26 stragglers + `net9.0`→`net10.0` on `S16.PantryPal.McpHost.Tests` (`fbf01f4c`). NOW LITERALLY ZERO in-sln non-fenced xUnit-v2 (fenced Jobs stay v2 by design); full `Koan.sln` 0 err. **Lesson: a sweeping "all X are Y" completion claim must be re-derived by ENUMERATION (grep + membership), never trusted from a wave-ledger — the enumeration found 30+ projects that two prior "all done" passes had missed.** |

When a card surfaces a post-merge action that lives outside the repo (a prod migration, a CI
secret, a manual deploy step), append a `### <ID> operator gate` subsection here describing it,
so the operator has a single place to find pending out-of-band work. None yet.
