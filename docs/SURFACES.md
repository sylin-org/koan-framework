# Surface Ledger

> **Rotation contract.** Before the lane leaves this repo or surface: tag; CI green;
> a tripwire exists for every surface the departing work was exercising; status
> endpoints tell the truth; this ledger is updated. Leave a guard at the door when
> you leave the room.

This is the mechanical memory behind the maintainer's serial-lane model (Epic E02).
Each row records a pillar/plane-level **surface**, **who exercises it**, **when it was
last exercised**, and **what guard protects it**. When the focus lane rotates away,
unguarded surfaces rot silently — the ledger turns that from a discovered fiction into
a known risk.

**Columns.** `Exercised by` ∈ {a named in-repo test/sample suite, `zen-garden`, `koan`,
`private downstream solution`, `none`}. `Guard` ∈ {test/CI job name, `none`}.
`Last exercised` is a real date (`YYYY-MM-DD`) or `unknown since <date>` — never a
guessed "works".

**Honesty notes for this repo.**
- **Dates** are seeded from `git log -1 --format=%as <paths>`. The uniform `2026-06-11`
  stamps come from squash-merge `ee08fa53` (`Dev #90`, 1507 files), i.e. *last landed in
  tree* — so dormant/untested pillars are rendered `unknown since 2026-06-11` per the
  honesty rule, not "exercised on 2026-06-11".
- **Guards marked `(local)`** run only in the local green-ratchet. `pr-gate.yml` protects pull
  requests and `release-on-dev.yml` builds, tests, and proves package artifacts before publication.
  A standalone `surfaces.yml` lints *this ledger*; it does not run the suites.

| Surface | Exercised by | Last exercised | Guard | Notes |
|---|---|---|---|---|
| First-use source/package/operator/agent contract | `samples/FirstUse` + `Koan.Packaging.Tests` + release clean room | 2026-07-14 | source suite 13/13; external package probe 8/8 (local and `release-on-dev`) | One public application body proves SQLite election, startup reporting, REST business output, identical Web/MCP facts, origin-aware tool visibility, dry-run, and agent mutation. Package evidence records environment and observed durations; public availability and universal timing are not implied. |
| Data inner ring (Abstractions/Core/Relational + filter pipeline) | Data suites (FilterConvergence oracle, DATA-0100) | 2026-07-15 | Data.Core 349/349; filtering foundation 92/92; convergence 19/19 (local) | Strongest pillar; canon-grade tests |
| Provider-bounded Entity streams (R07-02) | Data.Core stream specs + AdapterSurface shared cells + SQLite focused provider spec + Data.Backup acceptance | 2026-07-15 | Data.Core 42/42 focused and 325/325 full; SQLite 1/1; qualified adapters 6/6 cells; fail-closed adapters 3/3 cells; Backup 5/5 acceptance and 7/7 full (local) | **Passed.** One internal coordinator keeps `IAsyncEnumerable<TEntity>` consumer-paced and count-free, fixes routed source/partition/registered context for one enumeration, and records selected/rejected runtime facts. SQLite, PostgreSQL, CockroachDB, SQL Server, MongoDB, and Couchbase earn `ProviderBoundedPaging`; the shared proof covers top-level non-nullable `bool`, `byte`, `sbyte`, `short`, `ushort`, and `int` caller sorts, while only the usual string Entity id is admitted as an opaque provider-stable tie-breaker. InMemory, JSON, and Redis reject before yielding rather than scan/materialize silently. Backup proves real SQLite pages `2/2/1`, cancellation during page 2, and resident-adapter early rejection. Snapshot consistency, mutation-safe iteration, resumability, cross-provider collation, and opaque-driver constant memory are not claimed. |
| Entity language composition (module-owned C# 14 facets) | `Koan.Tests.EntityLanguage` consumer compiler + runtime specs | 2026-07-16 | Entity language suite 25/25 (local) | Proves absence without a module, presence through the canonical Entity/Data namespaces, invalid-receiver rejection, current Cache/Jobs/SoftDelete/Events/Transport collision safety, module removal, provider-neutral explanations, current-host resolution, typed missing-service failure, scalar/set/stream Communication, inferred scalar/set/stream/custom-key `Relatives`, and constrained Jobs scalar/set/stream submission. Future public module-owned facets must join the all-modules consumer cell. |
| Bounded direct relationship negotiation and enrichment (ARCH-0112 / R07-13) | Data.Core + Web InMemory + MCP relationship suites | 2026-07-16 | Data.Core Relationships 10/10; Web relationship 7/7; MCP relationship 2/2 + conformance 73/73 (local) | One executor owns child-edge selection/facts while one internal graph loader owns scalar/set/stream source batching. InMemory, JSON, and SQLite cells prove native/in-memory/bounded/rejected modes; pointwise cells prove inferred custom keys, source order/multiplicity, parent `GetMany`, child batching, and corrective cross-key rejection. Web/MCP retain governed authorization. Other providers are not fleet-certified; recursive depth, index sufficiency, and provider-fleet performance remain open. |
| Database-mode source routing (ARCH-0102 §3 auto-routing) | Koan.Data.Axes.Integration.Tests | 2026-06-25 | MultiDatabaseRoutingSpec (local; 2 SQLite file DBs, RED→GREEN + fail-closed) | A Database-mode `[DataAxis]` derives the data source from the ambient via `DatabaseRouteRegistry` (copy-on-write) + `AdapterResolver` Priority-1.5, after an explicit `EntityContext.Source` (caller override wins). Off = byte-identical (`IsEmpty` gate, FC-5; data-core 273 off-proof); unconfigured source fails closed self-explaining (`ProvisioningPosture.ExternalOnly`, FC-7). Phase-3 pinned: `NullKeyBehavior.FailClosed` for strict isolation; overlapping-route boot detection |
| AODB adapter isolation conformance (RowScoped/ContainerScoped/DatabaseScoped, ARCH-0103 §6 P5) | `AodbConformanceSpecsBase<TFixture>` × 8 adapter cells | 2026-06-26 | conformance kit (local; 8 surfaces × 4 facts: Declares/Shared/Container/Database) | The AODB three-mode ledger: every conformant adapter declares all three isolation tokens (`DataCaps.Isolation.*`), each **co-defined with its proof** (ARCH-0094 — declare-but-not-realize ⇒ red; realize-but-not-declare ⇒ red). Cells: Mongo + Couchbase (document), InMemory/Json/Redis (KV), SQLite/Postgres/SQL Server (relational). Postgres/SQL Server Database cells provision a fresh physical DB per routed source; the **Couchbase** Database cell provisions a per-source **bucket** (Container = native scope per partition, Database = native bucket per source). The shared discoverable shard axis lives in the TestKit (adapter-test-only — never data-core/tenancy, so off-proofs unaffected). Each adapter's boot report notes its declared modes. **Couchbase FOLDED onto `DocumentStore` (2026-06-26, ARCH-0103 P4)** — now declares all three tokens + passes all four cells (the document family is complete). **Per-source provider pooling GUARDED (ARCH-0103 §9.15)**: `MongoSourceProviderDedupSpec` + `CouchbaseSourceProviderDedupSpec` (Docker-free, via the internal `SourceProviderCount`) — a Default-coinciding routed source reuses the DI-managed provider (no duplicate `MongoClient`/cluster connection); pooling is by distinct physical source (Mongo conn+db; Couchbase conn+bucket+creds), lifetime = factory, no eviction by design (cached repos hold the provider). **ARCH-0094 Phase 1**: the kit is now capability-driven — `CapabilityConformanceGate` (link-compiled from `tests/Suites/_shared/`) dispatches each cell from a `(token, disposition)` table; the 3 record isolation tokens are `Required` (under-claim fails the declares cell, over-claim fails the realization cell). Same-outcome re-run verified |
| Vector AODB isolation (Shared/Container/Database, ARCH-0103) | `VectorAodbConformanceSpecsBase` cells across ALL 7 adapters — `Qdrant`/`Weaviate`/`ElasticSearch`/`OpenSearch`/`MilvusVectorAodbConformanceSpec` (live, Testcontainers, kit subclasses) + InMemoryVector + SqliteVec (Docker-free) — plus `VectorTenantIsolationSpec` (InMemory) + `VectorDatabaseRoutingSpec` (InMemory/SqliteVec) | 2026-06-26 | local; HTTP-fleet Testcontainers (each 4/4) + Docker-free in-proc | Container realized fleet-wide via the name-mangling floor (partition→collection); Database via the source-fold (routed source→collection, `e196c97c`, fail-closed injective guard); Shared via `ScopedVectorRepository` (`__koan_tenant` stamp+filter). **Overlay-naming security flag TEST-VERIFIED on the WHOLE HTTP fleet** (Qdrant + Weaviate + ES + OpenSearch + Milvus — `__koan_tenant` round-trips + isolates a kNN); only Weaviate's GraphQL reserves `__` (declares `IOverlayNamingAware`), the others use faithful nested JSON keys. Specs surfaced + fixed real bugs: empty-`CollectionName` (Qdrant + Milvus, full-AddKoan binds absent key to `""`); **two Weaviate naming bugs** (nested-type `+` → invalid GraphQL class name; source-fold lowercase particle vs Weaviate first-letter auto-capitalization → silent-empty cross-source read) — both adapter-local in `WeaviateVectorRepository.SanitizeClassName`, byte-identical for top-level names (Weaviate full project 33/33). **Tokens DECLARED** (ARCH-0103 §6/9.11): `ScopedVectorRepository.Describe` declares `DataCaps.Isolation.ContainerScoped`+`DatabaseScoped` (universal name-fold floor) + `RowScoped` (gated on inner `VectorCaps.Filters`); co-defined by **`VectorAodbConformanceSpecsBase`** (4 cells: Declares + Shared-fail-closed-aware + Container + Database) wired on **ALL 7 vector adapters (ARCH-0103 §9.16)** — InMemoryVector (4/4) + SqliteVec (4/4, §9.13 — the first non-filtering twin: pure-KNN, so the Shared cell drives the under-claim/fail-close branch, co-defining `RowScoped` from both sides) + the 5 HTTP adapters live (each 4/4, the bespoke `*VectorIsolationSpec`s replaced by thin `*VectorAodbConformanceSpec` subclasses). Container/Database now also prove by-id physical separation where the adapter supports `GetEmbedding` (Qdrant/Weaviate/in-proc; ES/OpenSearch/Milvus throw NotSupported → kNN Search proves it instead — gap caught by live Docker). DatabaseScoped's fail-closed-on-unconfigured clause carved out as record-plane-only (vectors name-fold lazily). **Pin footgun GUARDED** (ARCH-0103 §9.14): a static `CollectionName` (Qdrant/Milvus) / `IndexName` (ES/OpenSearch) bypasses the partition+source fold ⇒ defeats Container/Database isolation; `VectorAdapterNaming.WarnIfPinnedNameDefeatsIsolation` emits a one-per-type `ConfigWarning` when the pin actually bites (partition / routed source / Container-axis particle in scope — the three discriminators `StorageNameGenerator` folds; review caught a HIGH false-negative pre-commit when the Container-axis particle was initially missed) — warn (deliberate override), not fail-close (`VectorNamePinWarningSpec` + `VectorNamePinWiringSpec` 9/9). **Follow-ons COMPLETE** (ARCH-0103 §9.13–9.16: SqliteVec cell · warn-on-pin · pool dedup · HTTP fleet on the kit). **ARCH-0094 Phase 1**: the same `CapabilityConformanceGate` now drives the vector cells from a `(token, disposition)` table — Container/Database `Required`, `RowScoped` `FailClosed` (declared ⇒ overlay isolates; under-claimed ⇒ scoped read fails closed, the SqliteVec path). The `Skip` disposition is built-but-inert (proven loud by unit test), the slot a Phase-5 non-safety token drops into |
| Adapter Forge — Blueprints (ARCH-0094 Phase 3) | `blueprints/` tree + `scripts/blueprint-lint.ps1 -Strict` (green-ratchet Leg F) | 2026-06-26 | blueprint-lint Leg F (local; in `pr-gate.yml` via green-ratchet) | The EXTEND-a-pillar authoring guides (parallel to `.claude/skills` cards = USE). First: `blueprints/data/sql/BLUEPRINT.md` (relational, distilled from the shipped Postgres/SQLite/SqlServer adapters + `Koan.Data.Relational`). Anti-drift = `<!-- obligation: Type.Member @ file -->` tokens grep-verified alive in the cited shipped source (proven both ways: green on the real blueprint, RED on a fictionalized member). `name` == `blueprints/`-relative path joined by `-`; required `pillar`/`type`/`grounded-in`; catalogue parity vs `blueprints/BLUEPRINTS.md`. The Conformance Gate (the verifier half) is the ARCH-0103 conformance kit, now generalized into the capability-driven `CapabilityConformanceGate` (**Phase 1 DONE** — `(token, disposition)` dispatch: Required/FailClosed/Skip; the AODB rows above are its first consumers) and made orchestrable by **`scripts/forge-verify.ps1`** (**Phase 2 DONE** — runs the conformance kit per adapter via `dotnet test`+TRX, emits a tri-state GREEN/RED/SKIPPED verdict + `-Output json` for the Phase-4 agent loop, exit 0/1/2; GREEN verified live on the 5 Docker-free surfaces, RED/SKIPPED induced-and-reverted; a script not a CLI verb because the orchestration CLI is on the ARCH-0077 Aspire-deprecation path). Deferred: green-ratchet Leg G (local, Docker-gated). **Phase 4 DONE** — the agent→blueprint→gate→retry loop proven end-to-end: an agent authored a CockroachDB adapter from the blueprint, the gate caught a real `ctid` delta (42703, no system column), the agent fixed it (`ORDER BY ctid`→`"Id"`), GREEN (independently re-verified). Harvested to the canon shape — a Postgres `StableOrderClause` seam (unseal + `protected virtual` + IVT, byte-identical, Postgres re-verified GREEN) + a **45-LOC `CockroachRepository : PostgresRepository`** (the agent's copy was ~1200 LOC — the Postgres repository it now inherits); Cockroach gate-GREEN, in `Koan.sln`. The `koan adapter new` scaffold CLI + full-fleet coverage = follow-ons |
| CockroachDB data connector (Forge-authored, ARCH-0094 Phase 4) | `CockroachAodbConformanceSpec` (Testcontainers `cockroachdb/cockroach`) | 2026-06-26 | `forge-verify.ps1 -Adapter Cockroach` (local, Docker; 4/4 AODB cells) | The first **agent-authored** adapter — a thin pg-wire reuse: `CockroachRepository : PostgresRepository` overrides only `StableOrderClause` (CockroachDB has no `ctid`). AODB-gate-conformant (all 3 isolation modes). It inherits the Postgres repository wholesale, so the *shared* logic is already covered by Postgres's auxiliary specs (filter-convergence / encoding / redaction) — porting Cockroach copies is parity, not a correctness gap. Aspire registrar = N/A by design (no `Aspire.Hosting.CockroachDB` provider; ARCH-0077 deprecates orchestration). Honest scope, not yet full-fleet parity |
| Data connectors (10 under Connectors/Data) | AdapterSurface suites | 2026-06-11 | connector suites (local; 39/87 not in sln) | Redis FilterSupport.Full is correct (in-memory-eval adapter, ARCH-0084) — scan-backed cost is documented, not a lie; native key TTL added (DATA-0101; Redis caps+TTL specs 2026-06-17) |
| Web nucleus (EntityController to hook pipeline) | Web AdapterSurface x8 | 2026-06-11 | Web suites (local) | Load-bearing; MCP executes through it |
| Runtime fact envelope and projections (ARCH-0111) | Core runtime-facts + Bootstrap + Data.Core + Web WellKnown + MCP conformance | 2026-07-14 | Core 211/211; Bootstrap 17/17 + 16/16 + 7/7; Data.Core 299/299; Web WellKnown 3/3 + relationship 7/7; MCP relationship 2/2 + conformance 73/73 (local) | Schema-1, redacted, host-owned facts prove module activation/rejection, default data election, and bounded child-edge decisions through startup, exception, health, lockfile, gated `/.well-known/Koan/facts`, and `koan://facts`. This remains vertical-slice evidence, not exhaustive provider/runtime coverage. |
| Web SSE projection (Koan.Web.Sse) | **Koan.Samples.SnapVault.Tests** (`SnapVaultUploadProgressSpec` — upload progress as a jobs-ledger read-projection, real `AddKoan()`, in-memory, no Docker) | 2026-07-18 | `SnapVaultUploadProgressSpec` (local, 3/3), `Koan.Web.Sse.Tests` (5/5), MCP conformance (75/75) | Upload progress is a read-projection of the durable jobs ledger (no SignalR hub/groups/push, no client library): the batch is the `PhotoProcessingJob`s sharing a `BatchJobId`, joined to each job's ledger row, and streamed through the single `Sse.Stream(...)`/`SseResult` model as `PhotoProgress`/`JobCompleted` frames. The browser holds one native `EventSource`; request cancellation ends the stream. A reusable `Koan.Web.Sse × Koan.Jobs` batch-watch primitive remains a framework-lift candidate only after a second consumer establishes the general contract. |
| Identity — durable person + reconciliation + management APIs + access + day-2 power + explicit account-linking + membership/tenant-resolution/deprovisioning (SEC-0007 P0–P3-core + P4) | `Koan.Identity.Tests` (91 facts, real `AddKoan()`, offline) | 2026-07-18 | Koan.Identity.Tests (local) | `src/Koan.Identity` + `src/Koan.Identity.Web`. P0: `Identity`/`IdentityEmail`/`ExternalIdentityLink`/`AuditEvent` (`IAmbientExempt` global plane), idempotent reconciliation through the real Web Auth sign-in seam, dev-open/prod-closed posture, and offline dev seed. P1: enforceable durable cookie sessions, lifecycle management, and best-effort audit. P2: global standard role claims plus contributed effective-access explanation. P3-core: dual-control impersonation, JIT grants, actor attribution, and optional tamper-evident audit chaining. P4: explicit provider linking plus the optional Identity-Tenancy bridge for invite binding, membership-authorized request resolution, tenant-role projection with host-role stripping, and verifiable deprovisioning. Local passwords, MFA, recovery, and step-up are not V1 capabilities: the partial packages were retired because no supported authentication ceremony connected proof, continuation, factor verification, and cookie issuance. Re-entry is tracked by PMC-034. Deferred: DNS custom-domain routing, tenant-suspension enforcement, bearer-token revocation, and complete account-security factors. |
| Cache (L1/L2 + coherence) | Cache CrossEngine oracle + KOAN0001 analyzer | 2026-06-11 | Cache suites + analyzer (local) | Reference pillar (ARCH-0075/0078) |
| Observability (OTel Reference=Intent) | Koan.Observability.Tests | 2026-06-17 | ObservabilityReferenceIntentSpec (local, mutation-checked) | Extracted from Koan.Core into an opt-in leaf package (ARCH-0088); registrar wires OTel on reference. OTel no longer hard-pulled by any framework package |
| Jobs (JOBS-0005, 5-tier) | Jobs TestKit + pointwise source contract | 2026-07-16 | Jobs core 82/82; last full SQLite 79/79; R07-14 SQLite 2/2 + tenancy 1/1 (local) | Scalar and finite/async source submission share one ledger-acceptance chokepoint. Source operations are one-pass, source-ordered, sequentially backpressured, context-sealed, and return a fixed-size summary with typed partial failure/cancellation; they are not collection-atomic and do not bound ledger growth. Existing host-owned placement/lifecycle, lane-fair dispatch, and cheap aggregate health remain unchanged. |
| Relational schema indexes (composite [Index] groups) | Jobs SQLite HighVolumeScanShapeSpec | 2026-06-17 | Jobs Adapter.Sqlite suite (local) | JOBS-0008 fix: `RelationalSchemaOrchestrator` now creates declared per-column AND composite indexes at table-create time (latent gap: fresh relational tables previously had no secondary indexes). Mongo index field-name (PascalCase→camelCase) also fixed |
| Vector and search | VectorAdapterSurface matrix | 2026-06-11 | Vector surface matrix (local) | PGVector does not compile (parked on a branch) |
| AI core (Contracts to AI to Data.AI + Ollama/LMStudio + AI.Web) | 5 samples + 2 services + tests | 2026-06-11 | AI core tests (local) | Settled, consumed |
| AI vertical (Agents/Eval/Orchestration) | none | unknown since 2026-06-11 | none | Born 2026-05-16; Eval remains unevidenced; ~0 consumers. Providerless Training and the misleading local-only Compute facade were retired from Koan V1. |
| MCP server | MCP suites + AS-edge specs + Streamable e2e | 2026-06-20 | MCP suites + Koan.Mcp.Streamable.IntegrationTests + Koan.Web.Auth.Server.IntegrationTests (local) | Real and consumed (6.4k LOC). AI-0037: the HTTP edge is now **Streamable HTTP** (single `{baseRoute}` POST/GET/DELETE, spec 2025-06-18) by default — one dispatch+session core; the legacy `/sse`+`/rpc` is a deprecated byte-faithful shim (opt-in `EnableLegacySseTransport`), pinned by the golden `LegacyHttpSseWireSpec`. SEC-0006: the edge is an OAuth resource server (Koan.bearer ES256, RFC 9728 WWW-Authenticate, RFC 8707 audience via McpEdgeAuth/McpResourceIdentity), guarded by McpAuthRampSpec/McpConfiguredResourceSpec + StreamableAuthSpec (same-principal crux). |
| Security.Trust (SEC-0001 / KSVID + SEC-0006 ES256 tier) | unit + integration suites | 2026-06-20 | Security.Trust suites (EcdsaIssuerTests/KoanBearerSchemeTests) (local) | Best-engineered small project; SEC-0006 added the real ES256 asymmetric issuer tier (EcdsaIssuer/IAsymmetricIssuer, persisted+rotating JWKS keys, fail-closed boot guard) + the Koan.bearer scheme validating BOTH issuer tiers |
| Web.Auth flows | Koan.Web.Auth integration + e2e | 2026-06-20 | Koan.Web.Auth.Integration.Tests + Koan.Web.Auth.Tests (local) | WEB-0071: maintained OAuth2/OIDC handlers via dynamic scheme registration — the OIDC-501 was fixed; real-Kestrel OAuth2/OIDC e2e (id_token validation) |
| Web.Auth.Server (OAuth 2.1 AS, SEC-0006) | Koan.Web.Auth.Server.IntegrationTests | 2026-06-20 | Koan.Web.Auth.Server.IntegrationTests (43 specs, local) | Embedded OAuth 2.1 AS — the MCP auth on-ramp: /oauth authorize+token+device+register, RFC 8414/9728/8707 discovery+audience, rotating refresh w/ reuse-detection over AgentGrant, ES256 issuer/JWKS; 5 mutation-verified cruxes |
| ZenGarden bridge (Koan.ZenGarden.Contracts + mainline connector refs) | zen-garden + private downstream solution | unknown since 2026-06-11 | none | Inert contracts keep layered integrations decoupled; S3 presign requires Moss. The reverse Koan AI connector was retired; orchestration belongs to Zen Garden. |
| Communication — Entity Events and Transport (ARCH-0113, R07-10–12, R07-18) | Koan.Communication.Tests + RabbitMQ connector conformance | 2026-07-16 | Communication 37/37 local + RabbitMQ 9/9 real broker | Foundation `AddKoan()` supplies the complete process-local ring. Startup-declared business channels elect independently per Events/Transport lane, bind typed groups once, reject unknown routes before enumeration, and report the plan. Real-broker proof covers a named RabbitMQ channel beside a local default plus confirmed publication, stable-group isolated fan-out, authenticated context, mandatory no-route, health, orchestration intent, framework signals, and every-node broadcasts. Retry/dedupe, remote settlement, RabbitMQ Events, dynamic channels, automatic branching, and exactly-once effects remain unsupported. |
| Messaging (Messaging.Core, legacy) | bootstrap smoke only | 2026-07-15 | bounded legacy bootstrap tests | Deprecated pre-redesign mechanism retained temporarily for Cache coherence and other surviving consumers; Jobs wake has migrated to Communication. Not the implementation behind Entity Transport and carries no broker-neutral delivery claim. |
| Scheduling | none (2 trivial consumers) | unknown since 2026-06-11 | none | OPS-0050 fragment; subsumed by JOBS-0005; zero tests |
| Orchestration (CLI/Generators) | none | unknown since 2026-06-11 | none | Condemned by ARCH-0077 (retire for Aspire); tests deleted |
| Media / Storage | Storage routing/core + Local provider + StorageTenantIsolationSpec + **Koan.Media.Web.Tests** (MediaEntitySource + derivation cache, real `AddKoan()` + Local storage) | 2026-07-17 | Storage routing 15/15 plus replication failure paths 3/3; Local provider 23/23; Storage tenant isolation 12/12; MediaEntitySource local proof | Storage now compiles provider placement, priority, unified `StorageCaps`, profile routes, replication, and receipts once. `ScopedStorageService` remains the all-verbs segmentation chokepoint. Local has direct safety/capability proof; S3 has warning-clean build and package artifact proof, while engine-backed S3 behavior remains dogfood/integration evidence rather than a hermetic connector suite. |
| Tenancy (hard segmentation + posture + active-member bridge) | Koan.Tenancy.Tests (87, local) + Koan.Identity.Tests (85, local) + **Koan.Samples.SnapVault.Tests** (33, SQLite/local dogfood) | 2026-07-18 | Tenant isolation/self-proof suites + `TenantResolutionSpec` + `DeprovisioningSpec` + the real `AddKoan()` SnapVault lifecycle suite | Reference=Intent isolation across active Data, Storage, Cache, Communication, and durable-context pillars. Core owns one cached posture and segmentation contribution; it has no HTTP-resolver prerequisite. `Koan.Identity.Tenancy` owns request carriers and scopes only an active durable member while stripping host roles. Full person deactivation closes Koan cookie sessions and tenant seats through ordered idempotent writes; it is not a cross-provider transaction or bearer-token revocation. Invitation, tenant lifecycle/data erasure, and verified domains remain unsupported. |
| Classification (Entity field-at-rest protection) | `Koan.Classification.Tests` + focused Data pipeline/axis and Cache topology cells | 2026-07-18 | Classification 55/55; Data pipeline 17/17; Data axis 9/9; Cache topology 7/7 (local) | `[Pii]`/`[Phi]`/`[Pci]`/`[Secret]`/`[Classified]` protect writable string properties through one host-owned Data transform plan. AES-256-GCM envelopes cover supported Entity writes/reads while preserving caller plaintext; classified types are excluded from distributed Entity cache. Core segmentation derives opaque key scopes automatically, including Tenancy. Development supplies ephemeral in-memory custody; non-Development startup refuses it and requires `IClassificationKeyProvider`. Searchable ciphertext, tokenization, caller masking, non-Data redaction, backfill, erasure, production custody, and direct-provider interception are not claimed. |
| Tenancy registry and membership operator console | `Koan.Tenancy.Web.Tests` (13 facts, real `AddKoan()`, SQLite, no Docker) | 2026-07-18 | TenancyConsoleSpec (local, 13/13) | Referencing `Koan.Tenancy.Web` mounts the bundled `/tenancy` UI and `/api/tenancy/admin` controller for tenant create/inspect/rename, deterministic known-subject membership grant/revoke, and bounded audit reads. One administration service owns normalization, duplicate-code denial, reserved-host-role rejection, and mutation audit. Exposure (`Enabled`, hosts, header) 404s before routing and never grants authority; Closed posture requires the configured host identity/role. The package depends on Web + Tenancy, not Jobs or Identity. Invite, suspend, erase, operation-job, and fake act-as surfaces were removed because registry-only mechanics did not create those guarantees. |
| Access scoping data-axis (SEC-0008: ambient subject + access read-axis) | `Koan.Data.Access.Tests` (7 facts, real `AddKoan()`, InMemory, no Docker) | 2026-06-27 | AccessAxisSpec + AccessFailOpenSpec (local) | `src/Koan.Data.Access` — the **third data-axis** (after `tenant`, `soft-delete`) on the DATA-0106 read-filter seam, resolving SEC-0004 §128 (the deferred core-ward read hoist) + ambient-charter Q5. `[AccessScoped(Field, ScopePrefix)]` opts an entity in; every read (incl. key-op IDOR lowering) narrows to the ambient `Subject`'s matching scope tokens via a **pushable `Filter.In`** (empty ⇒ match-nothing `1=0`/`$in:[]`, verified cross-adapter in the review), **fail-closed on an absent subject** (`AccessOptions.FailClosedOnAbsentSubject`, default true; posture toggle proven). The ambient subject (`SubjectContext` 4-state: absent⇒deny / `Subject.System()`⇒elevated / `Subject.Unconstrained(id)`⇒full / `Subject.Use(id,scopes)`⇒narrow) rides the async hop via `SubjectContextCarrier` (ARCH-0100) ⇒ **a guest-triggered JOB is inherently scoped** (the killer property a web hook structurally cannot give — proven by `Subject_carrier_scopes_a_job`). The **split**: only read-row-scoping is here; the gate (401/403) + `can:[]` projection + write create-stamp stay at the web surface (SEC-0004 unchanged). Opt-in = the recursion guard (grant/control-plane tables aren't `[AccessScoped]`) + cache-exclusion paid only by opted-in types. 5-lens review: **sound, 0 CRITICAL, no read-path fail-open**; fixed a latent field-casing silent-deny (normalize `Field`→CLR property + fail-loud at first touch), a carrier separator-injection (reject U+001F in scope tokens), and made the dangerous full-access verb explicit (`Subject.Unconstrained`). Consumer of record: SnapVault guest galleries (the planned web `IRequestOptionsHook` dropped). Deferred (tracked): the write-half guard, a cross-adapter empty-`In` FilterConvergence row, the vector read path (off the fold today). |

---

*Seeded by Epic E02 (2026-06-13). Every lane that touches a surface above updates its
row — `Last exercised` to today, `Guard` to the tripwire it left — before it leaves.*
