---
type: GUIDE
domain: data
title: "Data Adapter Conformance Current Handoff"
audience: [architects, maintainers, developers, ai-agents]
status: current
last_updated: 2026-07-28
framework_version: v0.20.0
validation:
  date_last_tested: 2026-07-28
  status: reviewed
  scope: initial initiative handoff
---

# Data Adapter Conformance — current handoff

## Current state

DAC-00 and DAC-01 passed against the frozen bootstrap checkpoint with source fingerprint
`e35b9f8eb9b6e4ea6f49e1bdcb29710ab18cd6af026236f0135db2a4e94820df`. DAC-01 classified 2,311 public source
declarations into 52 owned surfaces, dispositioned all 81 primer IDs, and froze 18 finite contract decisions. DAC-02
ratified all 18 into the exact consumer contract. DAC-14 retired source-quarantine scaffolding that could not prove
cognitive isolation and froze the minimum-meaningful-parts rule. DAC-03 now projects the primer into an executable
81-cell/27-profile base protocol with deterministic packets, strict Forge outcomes, and dependency invalidation.
DAC-49 is human-ratified: the primer now adds the provider-neutral Vector experience, 12 Vector profiles, and V-01
through V-24 without treating current adapters as normative authority. DAC-50 projects all 105 cells and 39 profiles
through the same manifest, packet validator, TestKit boundary, and Forge runner. Current Vector providers remain
DEFERRED on 24 loud proof seams; absence cannot read as certification.

DAC-04 completes the Framework source foundation. Lifecycle, access, read lanes, routing identity, and credential
identity compile into one immutable redacted plan; Entity, batch, instruction, and Direct paths gate typed effects
before callbacks/readiness/resources; external semantic delete remains legal while structural removal rejects. Shared
business-operation catch/provision/replay and message-text shape classification are gone. Three bounded host-owned
stages now separate reachability, declared-shape validation, and authorized provision-plus-post-validation. The owned
matrix is 89/89 and the solution builds with no warnings. Provider-native proofs remain on their explicit adapter
cards; no adapter production implementation was changed by DAC-04.

DAC-05 completes the Framework Entity execution boundary. Get-many is positional; query/count handled facts are
validated per axis; bulk work prepares once and dispatches once; atomicity, complete item outcomes, conditional
replace, and exact mutation outcomes require matching native seams; and load Lifecycle observes only final visible
rows. Sequential transaction coordination no longer claims native/local atomicity. The owned Core matrix is 76/76,
the adjacent framework matrix is 62/62, convergence is 19/19, and the solution builds cleanly. Existing SQLite now
has one explicit provider RED for an omitted handled-filter receipt; it is assigned to the empty-root gold card.

The gold path is fixed as harvest → contract gate → two empty-root implementations → atomic legacy
retirement → independent certification. It is not an audit-to-remediation path for the current SQLite or MongoDB code.

DAC-06 completes Source Integration: source-only activation, provider-neutral inspection, bounded `RecordSet`, shared
compiled DTO projection, and immutable registered reads. DAC-07 completes aggregate-to-record mapping and the
Relational Family substrate. The compact four-shape grammar compiles into one bounded host-owned plan used by every
mapping consumer; relational adapters receive symbolic commands, exact physical paths/values, schema definitions,
and proof requirements. No connector production implementation changed, and provider-native projection/index/TTL
proofs remain explicitly pending.

DAC-08 completes diagnostics, claims, lifecycle ownership, and performance evidence. Describe/Explain are pure over
frozen decisions; Doctor is active and non-mutating; one deterministic claim set feeds runtime and TestKit; failures
separate safe public facts from bounded restricted native evidence; mutable catalogs/caches/integrations belong to one
host; and the shared TestKit owns eight scenarios plus a pinned four-metric observation grammar. The focused Framework
matrix is green. Twenty-two strict receipt REDs in legacy adapter-backed paths remain visible for gold/fleet cards.

DAC-09R-01/R01A through R04 are green. The latest slice removes exception-driven clear fallback and every duplicate
partition-transfer path. `Entity<T>` and `Data<T,TKey>` share one compact, provider-paged `Copy`/`Move`/`Mirror`
builder; `.Batch(int)` bounds candidates, writes, deferred deletes, and conflict results; exact receipts and caller
cancellation are enforced; ambiguous faults are never replayed. Legacy SQLite's false paging receipt remains an
adapter RED for the empty-root replacement rather than weakening the Framework gate.

## Validation

The repository-owned bootstrap derives 16 adapters and five family seams, creates 22 evidence packets, validates the
41-card DAG against the 105-ID primer catalog, and passes 15 focused mutation cases. Exact-class Forge selection removed
the substring contamination recorded at bootstrap. The generated catalog, protocol meta-suite, shared TestKits, and
full solution build pass; strict Forge returns DEFERRED when an otherwise-passing adapter lacks its certification
packet. DAC-08 adds a 140/140 focused Core gate, 33 passing TestKit cases, 16/16 Relational placement, 56/56 Axis
ownership, five standard-host lifecycle cases, and a clean solution build. See
[the DAC-08 verification](evidence/framework/DAC-08-verification.md).

## Next action

Execute [DAC-11](work-items/DAC-11-sqlite-greenfield-gold-rewrite.md) as the first thin vertical slice. R01/R01A
through R04 remain frozen because they corrected observable Data semantics. R05 through R07 and the broad DAC-09
recertification are deferred until SQLite and MongoDB prove that a shared seam is necessary. SQLite starts from an
empty implementation root; current code contributes only public facts, provider lessons, and black-box failures.

## Guardrails

- Treat the primer as the desired semantic authority, not current implementation as authority.
- Preserve unrelated worktree changes.
- Do not expand Framework or TestKit machinery without a failing gold-adapter case that requires the shared seam.
- Harvest current SQLite and MongoDB code only for provider facts, compatibility decisions, negative lessons, and
  black-box cases. Do not reuse their structure, helpers, control flow, or tests in DAC-11/DAC-21.
- Empty each gold implementation root before authoring its replacement; preserve only ratified public identities and
  implementation-neutral provider facts.
- Require one necessary contract, repeated-mechanics, or measured hot-path reason for every new moving part.
- Certify both golds only on the single composed checkpoint produced by DAC-23.
- Do not implement Vector conformance tooling before DAC-49 ratifies a primer annex.
- Do not call an adapter gold, supported, or certified based on present package status.
- Invalidate every packet that consumed a changed owner/path/tool/profile/fixture, including prior or sibling verdicts.
- Use a sealed source checkpoint when the initiative tree is uncommitted; commit only with explicit authorization.
- Keep changes local and uncommitted unless explicitly requested.
