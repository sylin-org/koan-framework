# Sylin.Koan.Data.Analytics.Web — technical contract

Owner of the `Sylin.Koan.Data.Analytics.Web` package. Source lives at `src/Koan.Data.Analytics.Web/Koan.Data.Analytics.Web.csproj`; the assembly carries exactly one `KoanModule` and activates through `AddKoan()` with no manual registration.

## Responsibilities

The analytics catalog and ask surface over HTTP and MCP: one vocabulary served to browsers, services, and agents.

## Failure boundary

Unsupported requests reject before provider work with a named capability and a correction. Facts, health, and lock evidence project the composed decision; no second activation owner exists for this assembly.
