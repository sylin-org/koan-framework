<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <Description>Dependency-free identities and result vocabulary shared across Koan-compatible model, dataset, compute, job, evaluation, and lineage workflows.</Description>
    <PackageTags>$(CommonPackageTags);ai;contracts;lifecycle;models;datasets;lineage</PackageTags>
  </PropertyGroup>
</Project>
