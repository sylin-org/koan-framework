<Project>
  <!-- Keep the template's build props isolated, but share the repository's package-train guard and
       pack policies so this content-only package cannot bypass a public release invariant. -->
  <Import Project="../Directory.Build.targets" />
</Project>
